# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cyber_recovery agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cyber_recovery.models import CyberRecoveryState
from shieldops.agents.cyber_recovery.nodes import (
    assess_damage,
    execute_recovery,
    report,
    select_recovery_points,
    validate_clean_room,
    verify_integrity,
)
from shieldops.agents.cyber_recovery.tools import CyberRecoveryToolkit
from shieldops.agents.runtime import END, Agent, edge


class CyberRecoveryAgent(Agent):
    """Declarative spec for ``cyber_recovery`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cyber_recovery"
    state_model = CyberRecoveryState
    toolkit_factory = CyberRecoveryToolkit

    nodes = {
        "assess_damage": assess_damage,
        "select_recovery_points": select_recovery_points,
        "validate_clean_room": validate_clean_room,
        "execute_recovery": execute_recovery,
        "verify_integrity": verify_integrity,
        "report": report,
    }

    edges = [
        edge("assess_damage", "select_recovery_points"),
        edge("select_recovery_points", "validate_clean_room"),
        edge("verify_integrity", "report"),
        edge("report", END),
        # TODO: conditional edge from "validate_clean_room"
        #   routed by should_execute_recovery() — wrap with cond(...)
        # TODO: conditional edge from "execute_recovery"
        #   routed by should_verify() — wrap with cond(...)
    ]

    entry = "assess_damage"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CyberRecoveryRunner`` keep working via this alias; new code
# should use ``CyberRecoveryAgent`` directly through AgentRuntime.
CyberRecoveryRunner = CyberRecoveryAgent
