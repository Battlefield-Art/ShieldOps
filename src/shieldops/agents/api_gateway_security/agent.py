# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the api_gateway_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.api_gateway_security.models import APIGatewaySecurityState
from shieldops.agents.api_gateway_security.nodes import (
    analyze_auth,
    detect_abuse,
    discover_apis,
    enforce_policies,
    generate_report,
    scan_endpoints,
)
from shieldops.agents.api_gateway_security.tools import APIGatewaySecurityToolkit
from shieldops.agents.runtime import END, Agent, edge


class ApiGatewaySecurityAgent(Agent):
    """Declarative spec for ``api_gateway_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "api_gateway_security"
    state_model = APIGatewaySecurityState
    toolkit_factory = APIGatewaySecurityToolkit

    nodes = {
        "discover_apis": discover_apis,
        "analyze_auth": analyze_auth,
        "scan_endpoints": scan_endpoints,
        "detect_abuse": detect_abuse,
        "enforce_policies": enforce_policies,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_apis", "analyze_auth"),
        edge("analyze_auth", "scan_endpoints"),
        edge("scan_endpoints", "detect_abuse"),
        edge("enforce_policies", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_abuse"
        #   routed by _needs_enforcement() — wrap with cond(...)
    ]

    entry = "discover_apis"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``APIGatewaySecurityRunner`` keep working via this alias; new code
# should use ``ApiGatewaySecurityAgent`` directly through AgentRuntime.
APIGatewaySecurityRunner = ApiGatewaySecurityAgent
