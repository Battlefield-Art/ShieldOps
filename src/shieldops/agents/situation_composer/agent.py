# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the situation_composer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.situation_composer.models import SituationComposerState
from shieldops.agents.situation_composer.nodes import (
    build_narrative,
    collect_alerts,
    correlate_signals,
    deduplicate,
    publish_situation,
    recommend_actions,
)
from shieldops.agents.situation_composer.tools import SituationComposerToolkit


class SituationComposerAgent(Agent):
    """Declarative spec for ``situation_composer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "situation_composer"
    state_model = SituationComposerState
    toolkit_factory = SituationComposerToolkit

    nodes = {
        "collect_alerts": collect_alerts,
        "deduplicate": deduplicate,
        "correlate_signals": correlate_signals,
        "build_narrative": build_narrative,
        "recommend_actions": recommend_actions,
        "publish_situation": publish_situation,
    }

    edges = [
        edge("collect_alerts", "deduplicate"),
        edge("deduplicate", "correlate_signals"),
        edge("build_narrative", "recommend_actions"),
        edge("recommend_actions", "publish_situation"),
        edge("publish_situation", END),
        # TODO: conditional edge from "correlate_signals"
        #   routed by has_correlations() — wrap with cond(...)
    ]

    entry = "collect_alerts"
