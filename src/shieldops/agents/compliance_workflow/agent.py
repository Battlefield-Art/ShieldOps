# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the compliance_workflow agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.compliance_workflow.models import ComplianceWorkflowState
from shieldops.agents.compliance_workflow.nodes import (
    assess_gaps,
    collect_evidence,
    generate_remediation,
    generate_report,
    identify_frameworks,
    map_controls,
)
from shieldops.agents.compliance_workflow.tools import ComplianceWorkflowToolkit
from shieldops.agents.runtime import END, Agent, edge


class ComplianceWorkflowAgent(Agent):
    """Declarative spec for ``compliance_workflow`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "compliance_workflow"
    state_model = ComplianceWorkflowState
    toolkit_factory = ComplianceWorkflowToolkit

    nodes = {
        "identify_frameworks": identify_frameworks,
        "map_controls": map_controls,
        "collect_evidence": collect_evidence,
        "assess_gaps": assess_gaps,
        "generate_remediation": generate_remediation,
        "report": generate_report,
    }

    edges = [
        edge("identify_frameworks", "map_controls"),
        edge("map_controls", "collect_evidence"),
        edge("collect_evidence", "assess_gaps"),
        edge("assess_gaps", "generate_remediation"),
        edge("generate_remediation", "report"),
        edge("report", END),
    ]

    entry = "identify_frameworks"
