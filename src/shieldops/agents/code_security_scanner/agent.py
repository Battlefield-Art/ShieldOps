# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the code_security_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.code_security_scanner.models import CodeSecurityScannerState
from shieldops.agents.code_security_scanner.nodes import (
    discover_repositories,
    generate_report,
    prioritize_findings,
    scan_application_code,
    scan_dependencies,
    scan_iac,
)
from shieldops.agents.code_security_scanner.tools import CodeSecurityScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CodeSecurityScannerAgent(Agent):
    """Declarative spec for ``code_security_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "code_security_scanner"
    state_model = CodeSecurityScannerState
    toolkit_factory = CodeSecurityScannerToolkit

    nodes = {
        "discover_repositories": discover_repositories,
        "scan_iac": scan_iac,
        "scan_dependencies": scan_dependencies,
        "scan_application_code": scan_application_code,
        "prioritize_findings": prioritize_findings,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_repositories", "scan_iac"),
        edge("scan_iac", "scan_dependencies"),
        edge("scan_dependencies", "scan_application_code"),
        edge("scan_application_code", "prioritize_findings"),
        edge("prioritize_findings", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "discover_repositories"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CodeSecurityScannerRunner`` keep working via this alias; new code
# should use ``CodeSecurityScannerAgent`` directly through AgentRuntime.
CodeSecurityScannerRunner = CodeSecurityScannerAgent
