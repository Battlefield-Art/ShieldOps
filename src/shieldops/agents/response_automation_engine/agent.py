# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the response_automation_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.response_automation_engine.models import ResponseAutomationEngineState
from shieldops.agents.response_automation_engine.nodes import (
    detect_trigger,
    document_actions,
    evaluate_playbook,
    orchestrate_actions,
    report,
    verify_response,
)
from shieldops.agents.response_automation_engine.tools import ResponseAutomationEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class ResponseAutomationEngineAgent(Agent):
    """Declarative spec for ``response_automation_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "response_automation_engine"
    state_model = ResponseAutomationEngineState
    toolkit_factory = ResponseAutomationEngineToolkit

    nodes = {
        "detect_trigger": detect_trigger,
        "evaluate_playbook": evaluate_playbook,
        "orchestrate_actions": orchestrate_actions,
        "verify_response": verify_response,
        "document_actions": document_actions,
        "report": report,
    }

    edges = [
        edge("document_actions", "report"),
        edge("report", END),
        # TODO: cond(...) from "detect_trigger" via _check_error()
        # TODO: cond(...) from "evaluate_playbook" via _check_error()
        # TODO: cond(...) from "orchestrate_actions" via _check_error()
        # TODO: cond(...) from "verify_response" via _check_error()
    ]

    entry = "detect_trigger"
