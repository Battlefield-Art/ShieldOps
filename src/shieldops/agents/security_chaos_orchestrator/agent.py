# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_chaos_orchestrator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_chaos_orchestrator.models import SecurityChaosOrchestratorState
from shieldops.agents.security_chaos_orchestrator.nodes import (
    analyze_resilience,
    define_blast_radius,
    generate_report,
    inject_failures,
    observe_behavior,
    plan_experiments,
)
from shieldops.agents.security_chaos_orchestrator.tools import SecurityChaosOrchestratorToolkit


class SecurityChaosOrchestratorAgent(Agent):
    """Declarative spec for ``security_chaos_orchestrator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_chaos_orchestrator"
    state_model = SecurityChaosOrchestratorState
    toolkit_factory = SecurityChaosOrchestratorToolkit

    nodes = {
        "plan_experiments": plan_experiments,
        "define_blast_radius": define_blast_radius,
        "inject_failures": inject_failures,
        "observe_behavior": observe_behavior,
        "analyze_resilience": analyze_resilience,
        "generate_report": generate_report,
    }

    edges = [
        edge("plan_experiments", "define_blast_radius"),
        edge("inject_failures", "observe_behavior"),
        edge("analyze_resilience", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "define_blast_radius" via _should_inject()
        # TODO: cond(...) from "observe_behavior" via _should_analyze()
    ]

    entry = "plan_experiments"
