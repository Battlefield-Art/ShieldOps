# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the health_check_orchestrator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.health_check_orchestrator.models import HealthCheckOrchestratorState
from shieldops.agents.health_check_orchestrator.nodes import (
    assess_health,
    correlate_issues,
    discover_services,
    probe_endpoints,
    report,
    trigger_remediation,
)
from shieldops.agents.health_check_orchestrator.tools import HealthCheckOrchestratorToolkit
from shieldops.agents.runtime import END, Agent, edge


class HealthCheckOrchestratorAgent(Agent):
    """Declarative spec for ``health_check_orchestrator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "health_check_orchestrator"
    state_model = HealthCheckOrchestratorState
    toolkit_factory = HealthCheckOrchestratorToolkit

    nodes = {
        "discover_services": discover_services,
        "probe_endpoints": probe_endpoints,
        "assess_health": assess_health,
        "correlate_issues": correlate_issues,
        "trigger_remediation": trigger_remediation,
        "report": report,
    }

    edges = [
        edge("trigger_remediation", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_services" via _check_error()
        # TODO: cond(...) from "probe_endpoints" via _check_error()
        # TODO: cond(...) from "assess_health" via _check_error()
        # TODO: cond(...) from "correlate_issues" via _check_error()
    ]

    entry = "discover_services"
