# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_lineage_tracker agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_lineage_tracker.models import DataLineageTrackerState
from shieldops.agents.data_lineage_tracker.nodes import (
    detect_anomalies,
    discover_sources,
    map_transformations,
    report,
    trace_lineage,
    validate,
)
from shieldops.agents.data_lineage_tracker.tools import DataLineageTrackerToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataLineageTrackerAgent(Agent):
    """Declarative spec for ``data_lineage_tracker`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_lineage_tracker"
    state_model = DataLineageTrackerState
    toolkit_factory = DataLineageTrackerToolkit

    nodes = {
        "discover_sources": discover_sources,
        "map_transformations": map_transformations,
        "trace_lineage": trace_lineage,
        "detect_anomalies": detect_anomalies,
        "validate": validate,
        "report": report,
    }

    edges = [
        edge("validate", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_sources" via _check_error()
        # TODO: cond(...) from "map_transformations" via _check_error()
        # TODO: cond(...) from "trace_lineage" via _check_error()
        # TODO: cond(...) from "detect_anomalies" via _check_error()
    ]

    entry = "discover_sources"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DataLineageTrackerRunner`` keep working via this alias; new code
# should use ``DataLineageTrackerAgent`` directly through AgentRuntime.
DataLineageTrackerRunner = DataLineageTrackerAgent
