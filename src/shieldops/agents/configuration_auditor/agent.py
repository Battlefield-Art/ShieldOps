# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the configuration_auditor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.configuration_auditor.models import ConfigurationAuditorState
from shieldops.agents.configuration_auditor.nodes import (
    collect_configs,
    detect_drift,
    parse_settings,
    recommend_fixes,
    report,
    validate_security,
)
from shieldops.agents.configuration_auditor.tools import ConfigurationAuditorToolkit
from shieldops.agents.runtime import END, Agent, edge


class ConfigurationAuditorAgent(Agent):
    """Declarative spec for ``configuration_auditor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "configuration_auditor"
    state_model = ConfigurationAuditorState
    toolkit_factory = ConfigurationAuditorToolkit

    nodes = {
        "collect_configs": collect_configs,
        "parse_settings": parse_settings,
        "validate_security": validate_security,
        "detect_drift": detect_drift,
        "recommend_fixes": recommend_fixes,
        "report": report,
    }

    edges = [
        edge("recommend_fixes", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_configs" via _check_error()
        # TODO: cond(...) from "parse_settings" via _check_error()
        # TODO: cond(...) from "validate_security" via _check_error()
        # TODO: cond(...) from "detect_drift" via _check_error()
    ]

    entry = "collect_configs"
