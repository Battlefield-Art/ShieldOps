# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ai_runtime_guardian agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ai_runtime_guardian.models import AIRuntimeGuardianState
from shieldops.agents.ai_runtime_guardian.nodes import (
    analyze_model_behavior,
    detect_prompt_attacks,
    enforce_guardrails,
    guard_tool_execution,
    monitor_ai_runtime,
    report,
)
from shieldops.agents.ai_runtime_guardian.tools import AIRuntimeGuardianToolkit
from shieldops.agents.runtime import END, Agent, edge


class AiRuntimeGuardianAgent(Agent):
    """Declarative spec for ``ai_runtime_guardian`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ai_runtime_guardian"
    state_model = AIRuntimeGuardianState
    toolkit_factory = AIRuntimeGuardianToolkit

    nodes = {
        "monitor_ai_runtime": monitor_ai_runtime,
        "detect_prompt_attacks": detect_prompt_attacks,
        "analyze_model_behavior": analyze_model_behavior,
        "guard_tool_execution": guard_tool_execution,
        "enforce_guardrails": enforce_guardrails,
        "report": report,
    }

    edges = [
        edge("monitor_ai_runtime", "detect_prompt_attacks"),
        edge("detect_prompt_attacks", "analyze_model_behavior"),
        edge("analyze_model_behavior", "guard_tool_execution"),
        edge("guard_tool_execution", "enforce_guardrails"),
        edge("enforce_guardrails", "report"),
        edge("report", END),
    ]

    entry = "monitor_ai_runtime"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AIRuntimeGuardianRunner`` keep working via this alias; new code
# should use ``AiRuntimeGuardianAgent`` directly through AgentRuntime.
AIRuntimeGuardianRunner = AiRuntimeGuardianAgent
