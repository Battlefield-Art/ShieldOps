# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the attack_campaign agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.attack_campaign.models import AttackCampaignState
from shieldops.agents.attack_campaign.nodes import (
    assess_defenses,
    collect_results,
    execute_simulation,
    generate_report,
    plan_campaign,
    select_ttps,
)
from shieldops.agents.attack_campaign.tools import AttackCampaignToolkit
from shieldops.agents.runtime import END, Agent, edge


class AttackCampaignAgent(Agent):
    """Declarative spec for ``attack_campaign`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "attack_campaign"
    state_model = AttackCampaignState
    toolkit_factory = AttackCampaignToolkit

    nodes = {
        "plan_campaign": plan_campaign,
        "select_ttps": select_ttps,
        "execute_simulation": execute_simulation,
        "collect_results": collect_results,
        "assess_defenses": assess_defenses,
        "generate_report": generate_report,
    }

    edges = [
        edge("plan_campaign", "select_ttps"),
        edge("select_ttps", "execute_simulation"),
        edge("execute_simulation", "collect_results"),
        edge("collect_results", "assess_defenses"),
        edge("assess_defenses", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "plan_campaign"
