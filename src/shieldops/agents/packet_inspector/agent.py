# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the packet_inspector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.packet_inspector.models import PacketInspectorState
from shieldops.agents.packet_inspector.nodes import (
    analyze_payloads,
    capture_packets,
    detect_threats,
    generate_report,
    validate_tls,
)
from shieldops.agents.packet_inspector.tools import PacketInspectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class PacketInspectorAgent(Agent):
    """Declarative spec for ``packet_inspector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "packet_inspector"
    state_model = PacketInspectorState
    toolkit_factory = PacketInspectorToolkit

    nodes = {
        "capture_packets": capture_packets,
        "analyze_payloads": analyze_payloads,
        "validate_tls": validate_tls,
        "detect_threats": detect_threats,
        "generate_report": generate_report,
    }

    edges = [
        edge("capture_packets", "analyze_payloads"),
        edge("validate_tls", "detect_threats"),
        edge("detect_threats", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "analyze_payloads"
        #   routed by _has_tls_traffic() — wrap with cond(...)
    ]

    entry = "capture_packets"
