# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the capacity_planner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.capacity_planner.models import CapacityPlannerState
from shieldops.agents.capacity_planner.nodes import (
    collect_metrics,
    forecast_demand,
    generate_report,
    identify_bottlenecks,
    plan_scaling,
    recommend,
)
from shieldops.agents.capacity_planner.tools import CapacityPlannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CapacityPlannerAgent(Agent):
    """Declarative spec for ``capacity_planner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "capacity_planner"
    state_model = CapacityPlannerState
    toolkit_factory = CapacityPlannerToolkit

    nodes = {
        "collect_metrics": collect_metrics,
        "forecast_demand": forecast_demand,
        "identify_bottlenecks": identify_bottlenecks,
        "plan_scaling": plan_scaling,
        "recommend": recommend,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_metrics", "forecast_demand"),
        edge("forecast_demand", "identify_bottlenecks"),
        edge("plan_scaling", "recommend"),
        edge("recommend", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "identify_bottlenecks"
        #   routed by _has_bottlenecks() — wrap with cond(...)
    ]

    entry = "collect_metrics"
