# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the performance_baseline_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.performance_baseline_engine.models import PerformanceBaselineEngineState
from shieldops.agents.performance_baseline_engine.nodes import (
    alert_deviations,
    analyze_trends,
    collect_metrics,
    detect_regressions,
    establish_baselines,
    report,
)
from shieldops.agents.performance_baseline_engine.tools import PerformanceBaselineEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class PerformanceBaselineEngineAgent(Agent):
    """Declarative spec for ``performance_baseline_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "performance_baseline_engine"
    state_model = PerformanceBaselineEngineState
    toolkit_factory = PerformanceBaselineEngineToolkit

    nodes = {
        "collect_metrics": collect_metrics,
        "establish_baselines": establish_baselines,
        "detect_regressions": detect_regressions,
        "analyze_trends": analyze_trends,
        "alert_deviations": alert_deviations,
        "report": report,
    }

    edges = [
        edge("alert_deviations", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_metrics" via _check_error()
        # TODO: cond(...) from "establish_baselines" via _check_error()
        # TODO: cond(...) from "detect_regressions" via _check_error()
        # TODO: cond(...) from "analyze_trends" via _check_error()
    ]

    entry = "collect_metrics"
