# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_network_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_network_analyzer.models import CloudNetworkAnalyzerState
from shieldops.agents.cloud_network_analyzer.nodes import (
    analyze_routes,
    check_segmentation,
    detect_exposure,
    discover_topology,
    generate_report,
    recommend,
)
from shieldops.agents.cloud_network_analyzer.tools import CloudNetworkAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudNetworkAnalyzerAgent(Agent):
    """Declarative spec for ``cloud_network_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_network_analyzer"
    state_model = CloudNetworkAnalyzerState
    toolkit_factory = CloudNetworkAnalyzerToolkit

    nodes = {
        "discover_topology": discover_topology,
        "analyze_routes": analyze_routes,
        "check_segmentation": check_segmentation,
        "detect_exposure": detect_exposure,
        "recommend": recommend,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_topology", "analyze_routes"),
        edge("analyze_routes", "check_segmentation"),
        edge("check_segmentation", "detect_exposure"),
        edge("recommend", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_exposure"
        #   routed by _should_recommend() — wrap with cond(...)
    ]

    entry = "discover_topology"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudNetworkAnalyzerRunner`` keep working via this alias; new code
# should use ``CloudNetworkAnalyzerAgent`` directly through AgentRuntime.
CloudNetworkAnalyzerRunner = CloudNetworkAnalyzerAgent
