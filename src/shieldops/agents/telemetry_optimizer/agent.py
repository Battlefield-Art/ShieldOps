# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the telemetry_optimizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import Agent, edge
from shieldops.agents.telemetry_optimizer.models import TelemetryOptimizerState
from shieldops.agents.telemetry_optimizer.nodes import (
    analyze_pipeline,
    identify_waste,
    propose_optimizations,
    run_experiments,
)
from shieldops.agents.telemetry_optimizer.tools import TelemetryOptimizerToolkit


class TelemetryOptimizerAgent(Agent):
    """Declarative spec for ``telemetry_optimizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "telemetry_optimizer"
    state_model = TelemetryOptimizerState
    toolkit_factory = TelemetryOptimizerToolkit

    nodes = {
        "analyze_pipeline": analyze_pipeline,
        "identify_waste": identify_waste,
        "propose_optimizations": propose_optimizations,
        "run_experiments": run_experiments,
    }

    edges = [
        edge("analyze_pipeline", "identify_waste"),
        edge("identify_waste", "propose_optimizations"),
        edge("propose_optimizations", "run_experiments"),
        # TODO: cond(...) from "run_experiments" via has_more_experiments()
    ]

    entry = "analyze_pipeline"
