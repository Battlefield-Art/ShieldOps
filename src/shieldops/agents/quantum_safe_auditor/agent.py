# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the quantum_safe_auditor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.quantum_safe_auditor.models import QuantumSafeAuditorState
from shieldops.agents.quantum_safe_auditor.nodes import (
    assess_quantum_risk,
    generate_report,
    identify_vulnerable,
    inventory_crypto,
    plan_migration,
    track_progress,
)
from shieldops.agents.quantum_safe_auditor.tools import QuantumSafeAuditorToolkit
from shieldops.agents.runtime import END, Agent, edge


class QuantumSafeAuditorAgent(Agent):
    """Declarative spec for ``quantum_safe_auditor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "quantum_safe_auditor"
    state_model = QuantumSafeAuditorState
    toolkit_factory = QuantumSafeAuditorToolkit

    nodes = {
        "inventory_crypto": inventory_crypto,
        "assess_quantum_risk": assess_quantum_risk,
        "identify_vulnerable": identify_vulnerable,
        "plan_migration": plan_migration,
        "track_progress": track_progress,
        "generate_report": generate_report,
    }

    edges = [
        edge("assess_quantum_risk", "identify_vulnerable"),
        edge("plan_migration", "track_progress"),
        edge("track_progress", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "inventory_crypto"
        #   routed by _should_assess() — wrap with cond(...)
        # TODO: conditional edge from "identify_vulnerable"
        #   routed by _should_plan() — wrap with cond(...)
    ]

    entry = "inventory_crypto"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``QuantumSafeAuditorRunner`` keep working via this alias; new code
# should use ``QuantumSafeAuditorAgent`` directly through AgentRuntime.
QuantumSafeAuditorRunner = QuantumSafeAuditorAgent
