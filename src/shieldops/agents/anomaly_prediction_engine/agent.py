# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the anomaly_prediction_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.anomaly_prediction_engine.models import AnomalyPredictionEngineState
from shieldops.agents.anomaly_prediction_engine.nodes import (
    generate_predictions,
    ingest_metrics,
    publish_alerts,
    report,
    train_models,
    validate_accuracy,
)
from shieldops.agents.anomaly_prediction_engine.tools import AnomalyPredictionEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class AnomalyPredictionEngineAgent(Agent):
    """Declarative spec for ``anomaly_prediction_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "anomaly_prediction_engine"
    state_model = AnomalyPredictionEngineState
    toolkit_factory = AnomalyPredictionEngineToolkit

    nodes = {
        "ingest_metrics": ingest_metrics,
        "train_models": train_models,
        "generate_predictions": generate_predictions,
        "validate_accuracy": validate_accuracy,
        "publish_alerts": publish_alerts,
        "report": report,
    }

    edges = [
        edge("publish_alerts", "report"),
        edge("report", END),
        # TODO: cond(...) from "ingest_metrics" via _check_error()
        # TODO: cond(...) from "train_models" via _check_error()
        # TODO: cond(...) from "generate_predictions" via _check_error()
        # TODO: cond(...) from "validate_accuracy" via _check_error()
    ]

    entry = "ingest_metrics"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AnomalyPredictionEngineRunner`` keep working via this alias; new code
# should use ``AnomalyPredictionEngineAgent`` directly through AgentRuntime.
AnomalyPredictionEngineRunner = AnomalyPredictionEngineAgent
