# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_cost_tracker agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_cost_tracker.models import IncidentCostTrackerState
from shieldops.agents.incident_cost_tracker.nodes import (
    assess_regulatory,
    calculate_direct,
    estimate_indirect,
    forecast_total,
    generate_report,
    identify_incident,
)
from shieldops.agents.incident_cost_tracker.tools import IncidentCostTrackerToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentCostTrackerAgent(Agent):
    """Declarative spec for ``incident_cost_tracker`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_cost_tracker"
    state_model = IncidentCostTrackerState
    toolkit_factory = IncidentCostTrackerToolkit

    nodes = {
        "identify_incident": identify_incident,
        "calculate_direct": calculate_direct,
        "estimate_indirect": estimate_indirect,
        "assess_regulatory": assess_regulatory,
        "forecast_total": forecast_total,
        "generate_report": generate_report,
    }

    edges = [
        edge("identify_incident", "calculate_direct"),
        edge("calculate_direct", "estimate_indirect"),
        edge("estimate_indirect", "assess_regulatory"),
        edge("forecast_total", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_regulatory"
        #   routed by _should_forecast() — wrap with cond(...)
    ]

    entry = "identify_incident"
