# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the breakout_defender agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.breakout_defender.models import BreakoutDefenderState
from shieldops.agents.breakout_defender.nodes import (
    analyze_lateral_movement,
    assess_breakout_risk,
    detect_initial_access,
    execute_containment,
    report,
    verify_containment,
)
from shieldops.agents.breakout_defender.tools import BreakoutDefenderToolkit
from shieldops.agents.runtime import END, Agent, edge


class BreakoutDefenderAgent(Agent):
    """Declarative spec for ``breakout_defender`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "breakout_defender"
    state_model = BreakoutDefenderState
    toolkit_factory = BreakoutDefenderToolkit

    nodes = {
        "detect_initial_access": detect_initial_access,
        "analyze_lateral_movement": analyze_lateral_movement,
        "assess_breakout_risk": assess_breakout_risk,
        "execute_containment": execute_containment,
        "verify_containment": verify_containment,
        "report": report,
    }

    edges = [
        edge("analyze_lateral_movement", "assess_breakout_risk"),
        edge("execute_containment", "verify_containment"),
        edge("verify_containment", "report"),
        edge("report", END),
        # TODO: conditional edge from "detect_initial_access"
        #   routed by should_analyze_lateral() — wrap with cond(...)
        # TODO: conditional edge from "assess_breakout_risk"
        #   routed by should_contain() — wrap with cond(...)
    ]

    entry = "detect_initial_access"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``BreakoutDefenderRunner`` keep working via this alias; new code
# should use ``BreakoutDefenderAgent`` directly through AgentRuntime.
BreakoutDefenderRunner = BreakoutDefenderAgent
