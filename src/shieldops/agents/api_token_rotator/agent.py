# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the api_token_rotator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.api_token_rotator.models import APITokenRotatorState
from shieldops.agents.api_token_rotator.nodes import (
    assess_risk,
    audit_age,
    discover_tokens,
    generate_report,
    rotate_tokens,
)
from shieldops.agents.api_token_rotator.tools import APITokenRotatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class ApiTokenRotatorAgent(Agent):
    """Declarative spec for ``api_token_rotator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "api_token_rotator"
    state_model = APITokenRotatorState
    toolkit_factory = APITokenRotatorToolkit

    nodes = {
        "discover_tokens": discover_tokens,
        "audit_age": audit_age,
        "assess_risk": assess_risk,
        "rotate": rotate_tokens,
        "report": generate_report,
    }

    edges = [
        edge("discover_tokens", "audit_age"),
        edge("audit_age", "assess_risk"),
        edge("assess_risk", "rotate"),
        edge("rotate", "report"),
        edge("report", END),
    ]

    entry = "discover_tokens"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``APITokenRotatorRunner`` keep working via this alias; new code
# should use ``ApiTokenRotatorAgent`` directly through AgentRuntime.
APITokenRotatorRunner = ApiTokenRotatorAgent
