# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the adaptive_access_controller agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.adaptive_access_controller.models import AdaptiveAccessControllerState
from shieldops.agents.adaptive_access_controller.nodes import (
    adjust_permissions,
    assess_context,
    audit_decisions,
    enforce_access,
    evaluate_risk,
    generate_report,
)
from shieldops.agents.adaptive_access_controller.tools import AdaptiveAccessControllerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AdaptiveAccessControllerAgent(Agent):
    """Declarative spec for ``adaptive_access_controller`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "adaptive_access_controller"
    state_model = AdaptiveAccessControllerState
    toolkit_factory = AdaptiveAccessControllerToolkit

    nodes = {
        "assess_context": assess_context,
        "evaluate_risk": evaluate_risk,
        "adjust_permissions": adjust_permissions,
        "enforce_access": enforce_access,
        "audit_decisions": audit_decisions,
        "generate_report": generate_report,
    }

    edges = [
        edge("assess_context", "evaluate_risk"),
        edge("evaluate_risk", "adjust_permissions"),
        edge("audit_decisions", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "adjust_permissions" via _should_enforce()
        # TODO: cond(...) from "enforce_access" via _should_audit()
    ]

    entry = "assess_context"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AdaptiveAccessControllerRunner`` keep working via this alias; new code
# should use ``AdaptiveAccessControllerAgent`` directly through AgentRuntime.
AdaptiveAccessControllerRunner = AdaptiveAccessControllerAgent
