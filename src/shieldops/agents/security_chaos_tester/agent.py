# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_chaos_tester agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_chaos_tester.models import SecurityChaosState
from shieldops.agents.security_chaos_tester.nodes import (
    assess_resilience,
    define_experiment,
    document_findings,
    generate_report,
    inject_fault,
    observe_behavior,
)
from shieldops.agents.security_chaos_tester.tools import SecurityChaosToolkit


class SecurityChaosTesterAgent(Agent):
    """Declarative spec for ``security_chaos_tester`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_chaos_tester"
    state_model = SecurityChaosState
    toolkit_factory = SecurityChaosToolkit

    nodes = {
        "define_experiment": define_experiment,
        "inject_fault": inject_fault,
        "observe_behavior": observe_behavior,
        "assess_resilience": assess_resilience,
        "document_findings": document_findings,
        "generate_report": generate_report,
    }

    edges = [
        edge("define_experiment", "inject_fault"),
        edge("inject_fault", "observe_behavior"),
        edge("observe_behavior", "assess_resilience"),
        edge("document_findings", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_resilience"
        #   routed by _should_document() — wrap with cond(...)
    ]

    entry = "define_experiment"
