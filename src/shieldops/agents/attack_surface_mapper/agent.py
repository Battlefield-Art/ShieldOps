# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the attack_surface_mapper agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.attack_surface_mapper.models import AttackSurfaceMapperState
from shieldops.agents.attack_surface_mapper.nodes import (
    assess_risk,
    classify_exposure,
    discover_assets,
    generate_report,
    map_attack_paths,
    recommend_remediation,
)
from shieldops.agents.attack_surface_mapper.tools import AttackSurfaceMapperToolkit
from shieldops.agents.runtime import END, Agent, edge


class AttackSurfaceMapperAgent(Agent):
    """Declarative spec for ``attack_surface_mapper`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "attack_surface_mapper"
    state_model = AttackSurfaceMapperState
    toolkit_factory = AttackSurfaceMapperToolkit

    nodes = {
        "discover_assets": discover_assets,
        "classify_exposure": classify_exposure,
        "assess_risk": assess_risk,
        "map_attack_paths": map_attack_paths,
        "recommend_remediation": recommend_remediation,
        "generate_report": generate_report,
    }

    edges = [
        edge("classify_exposure", "assess_risk"),
        edge("map_attack_paths", "recommend_remediation"),
        edge("recommend_remediation", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "discover_assets"
        #   routed by _should_classify() — wrap with cond(...)
        # TODO: conditional edge from "assess_risk"
        #   routed by _should_map_paths() — wrap with cond(...)
    ]

    entry = "discover_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AttackSurfaceMapperRunner`` keep working via this alias; new code
# should use ``AttackSurfaceMapperAgent`` directly through AgentRuntime.
AttackSurfaceMapperRunner = AttackSurfaceMapperAgent
