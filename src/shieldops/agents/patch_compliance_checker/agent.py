# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the patch_compliance_checker agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.patch_compliance_checker.models import PatchComplianceCheckerState
from shieldops.agents.patch_compliance_checker.nodes import (
    assess_risk,
    check_sla,
    generate_report,
    inventory_systems,
    scan_patches,
    schedule_rollout,
)
from shieldops.agents.patch_compliance_checker.tools import PatchComplianceCheckerToolkit
from shieldops.agents.runtime import END, Agent, edge


class PatchComplianceCheckerAgent(Agent):
    """Declarative spec for ``patch_compliance_checker`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "patch_compliance_checker"
    state_model = PatchComplianceCheckerState
    toolkit_factory = PatchComplianceCheckerToolkit

    nodes = {
        "inventory_systems": inventory_systems,
        "scan_patches": scan_patches,
        "assess_risk": assess_risk,
        "check_sla": check_sla,
        "schedule_rollout": schedule_rollout,
        "report": generate_report,
        # TODO: resolve node fn for "error_end"
    }

    edges = [
        edge("scan_patches", "assess_risk"),
        edge("assess_risk", "check_sla"),
        edge("check_sla", "schedule_rollout"),
        edge("schedule_rollout", "report"),
        edge("report", END),
        edge("error_end", END),
        # TODO: conditional edge from "inventory_systems"
        #   routed by _check_error() — wrap with cond(...)
    ]

    entry = "inventory_systems"
