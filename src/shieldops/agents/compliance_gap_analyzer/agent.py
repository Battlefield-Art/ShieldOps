# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the compliance_gap_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.compliance_gap_analyzer.models import ComplianceGapAnalyzerState
from shieldops.agents.compliance_gap_analyzer.nodes import (
    build_report,
    generate_plan,
    identify_gaps,
    map_requirements,
    prioritize_risks,
    scan_posture,
)
from shieldops.agents.compliance_gap_analyzer.tools import ComplianceGapAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class ComplianceGapAnalyzerAgent(Agent):
    """Declarative spec for ``compliance_gap_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "compliance_gap_analyzer"
    state_model = ComplianceGapAnalyzerState
    toolkit_factory = ComplianceGapAnalyzerToolkit

    nodes = {
        "scan_posture": scan_posture,
        "map_requirements": map_requirements,
        "identify_gaps": identify_gaps,
        "prioritize_risks": prioritize_risks,
        "generate_plan": generate_plan,
        "build_report": build_report,
    }

    edges = [
        edge("scan_posture", "map_requirements"),
        edge("map_requirements", "identify_gaps"),
        edge("identify_gaps", "prioritize_risks"),
        edge("prioritize_risks", "generate_plan"),
        edge("generate_plan", "build_report"),
        edge("build_report", END),
    ]

    entry = "scan_posture"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ComplianceGapAnalyzerRunner`` keep working via this alias; new code
# should use ``ComplianceGapAnalyzerAgent`` directly through AgentRuntime.
ComplianceGapAnalyzerRunner = ComplianceGapAnalyzerAgent
