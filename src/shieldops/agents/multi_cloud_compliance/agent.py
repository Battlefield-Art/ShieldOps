# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the multi_cloud_compliance agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.multi_cloud_compliance.models import MultiCloudComplianceState
from shieldops.agents.multi_cloud_compliance.nodes import (
    collect_configs,
    evaluate_benchmarks,
    generate_remediation,
    generate_report,
    identify_gaps,
)
from shieldops.agents.multi_cloud_compliance.tools import MultiCloudComplianceToolkit
from shieldops.agents.runtime import END, Agent, edge


class MultiCloudComplianceAgent(Agent):
    """Declarative spec for ``multi_cloud_compliance`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "multi_cloud_compliance"
    state_model = MultiCloudComplianceState
    toolkit_factory = MultiCloudComplianceToolkit

    nodes = {
        "collect_configs": collect_configs,
        "evaluate_benchmarks": evaluate_benchmarks,
        "identify_gaps": identify_gaps,
        "generate_remediation": generate_remediation,
        "generate_report": generate_report,
    }

    edges = [
        edge("evaluate_benchmarks", "identify_gaps"),
        edge("identify_gaps", "generate_remediation"),
        edge("generate_remediation", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "collect_configs"
        #   routed by _has_error() — wrap with cond(...)
    ]

    entry = "collect_configs"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``MultiCloudComplianceRunner`` keep working via this alias; new code
# should use ``MultiCloudComplianceAgent`` directly through AgentRuntime.
MultiCloudComplianceRunner = MultiCloudComplianceAgent
