# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cost_anomaly agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cost_anomaly.models import CostAnomalyState
from shieldops.agents.cost_anomaly.nodes import (
    analyze_llm_costs,
    classify_waste,
    collect_billing,
    detect_anomalies,
    generate_report,
    recommend,
)
from shieldops.agents.cost_anomaly.tools import CostAnomalyToolkit
from shieldops.agents.runtime import END, Agent, edge


class CostAnomalyAgent(Agent):
    """Declarative spec for ``cost_anomaly`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cost_anomaly"
    state_model = CostAnomalyState
    toolkit_factory = CostAnomalyToolkit

    nodes = {
        "collect_billing": collect_billing,
        "detect_anomalies": detect_anomalies,
        "classify_waste": classify_waste,
        "analyze_llm_costs": analyze_llm_costs,
        "recommend": recommend,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_billing", "detect_anomalies"),
        edge("detect_anomalies", "classify_waste"),
        edge("classify_waste", "analyze_llm_costs"),
        edge("analyze_llm_costs", "recommend"),
        edge("recommend", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "collect_billing"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CostAnomalyRunner`` keep working via this alias; new code
# should use ``CostAnomalyAgent`` directly through AgentRuntime.
CostAnomalyRunner = CostAnomalyAgent
