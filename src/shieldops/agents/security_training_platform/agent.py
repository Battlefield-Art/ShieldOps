# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_training_platform agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_training_platform.models import SecurityTrainingPlatformState
from shieldops.agents.security_training_platform.nodes import (
    assess_baseline,
    create_campaign,
    deploy_simulation,
    generate_report,
    score_risk,
    track_results,
)
from shieldops.agents.security_training_platform.tools import SecurityTrainingPlatformToolkit


class SecurityTrainingPlatformAgent(Agent):
    """Declarative spec for ``security_training_platform`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_training_platform"
    state_model = SecurityTrainingPlatformState
    toolkit_factory = SecurityTrainingPlatformToolkit

    nodes = {
        "assess_baseline": assess_baseline,
        "create_campaign": create_campaign,
        "deploy_simulation": deploy_simulation,
        "track_results": track_results,
        "score_risk": score_risk,
        "generate_report": generate_report,
    }

    edges = [
        edge("create_campaign", "deploy_simulation"),
        edge("deploy_simulation", "track_results"),
        edge("score_risk", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_baseline"
        #   routed by _should_create_campaign() — wrap with cond(...)
        # TODO: conditional edge from "track_results"
        #   routed by _should_score() — wrap with cond(...)
    ]

    entry = "assess_baseline"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityTrainingPlatformRunner`` keep working via this alias; new code
# should use ``SecurityTrainingPlatformAgent`` directly through AgentRuntime.
SecurityTrainingPlatformRunner = SecurityTrainingPlatformAgent
