# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_encryption_monitor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_encryption_monitor.models import DataEncryptionMonitorState
from shieldops.agents.data_encryption_monitor.nodes import (
    traced_assess,
    traced_certs,
    traced_gaps,
    traced_keys,
    traced_report,
    traced_scan,
)
from shieldops.agents.data_encryption_monitor.tools import DataEncryptionMonitorToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataEncryptionMonitorAgent(Agent):
    """Declarative spec for ``data_encryption_monitor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_encryption_monitor"
    state_model = DataEncryptionMonitorState
    toolkit_factory = DataEncryptionMonitorToolkit

    nodes = {
        "scan_assets": traced_scan,
        "assess_encryption": traced_assess,
        "check_key_rotation": traced_keys,
        "check_certificates": traced_certs,
        "identify_gaps": traced_gaps,
        "report": traced_report,
    }

    edges = [
        edge("scan_assets", "assess_encryption"),
        edge("assess_encryption", "check_key_rotation"),
        edge("check_key_rotation", "check_certificates"),
        edge("check_certificates", "identify_gaps"),
        edge("identify_gaps", "report"),
        edge("report", END),
    ]

    entry = "scan_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DataEncryptionMonitorRunner`` keep working via this alias; new code
# should use ``DataEncryptionMonitorAgent`` directly through AgentRuntime.
DataEncryptionMonitorRunner = DataEncryptionMonitorAgent
