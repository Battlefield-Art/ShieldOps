# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_risk_ranker agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_risk_ranker.models import CloudRiskRankerState
from shieldops.agents.cloud_risk_ranker.nodes import (
    collect_cloud_findings,
    correlate_attacker_tactics,
    generate_attack_paths,
    generate_report,
    prioritize_remediation,
    rank_by_exploitability,
)
from shieldops.agents.cloud_risk_ranker.tools import CloudRiskRankerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudRiskRankerAgent(Agent):
    """Declarative spec for ``cloud_risk_ranker`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_risk_ranker"
    state_model = CloudRiskRankerState
    toolkit_factory = CloudRiskRankerToolkit

    nodes = {
        "collect_cloud_findings": collect_cloud_findings,
        "correlate_attacker_tactics": correlate_attacker_tactics,
        "rank_by_exploitability": rank_by_exploitability,
        "generate_attack_paths": generate_attack_paths,
        "prioritize_remediation": prioritize_remediation,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_cloud_findings", "correlate_attacker_tactics"),
        edge("correlate_attacker_tactics", "rank_by_exploitability"),
        edge("rank_by_exploitability", "generate_attack_paths"),
        edge("prioritize_remediation", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "generate_attack_paths"
        #   routed by _has_critical_paths() — wrap with cond(...)
    ]

    entry = "collect_cloud_findings"
