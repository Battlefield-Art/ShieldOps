# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the sca_dependency_checker agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.sca_dependency_checker.models import SCADependencyCheckerState
from shieldops.agents.sca_dependency_checker.nodes import (
    check_licenses,
    discover_manifests,
    generate_report,
    match_cves,
    parse_dependencies,
    prioritize_findings,
)
from shieldops.agents.sca_dependency_checker.tools import SCADependencyCheckerToolkit


class ScaDependencyCheckerAgent(Agent):
    """Declarative spec for ``sca_dependency_checker`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "sca_dependency_checker"
    state_model = SCADependencyCheckerState
    toolkit_factory = SCADependencyCheckerToolkit

    nodes = {
        "discover_manifests": discover_manifests,
        "parse_dependencies": parse_dependencies,
        "match_cves": match_cves,
        "check_licenses": check_licenses,
        "prioritize": prioritize_findings,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_manifests", "parse_dependencies"),
        edge("parse_dependencies", "match_cves"),
        edge("match_cves", "check_licenses"),
        edge("check_licenses", "prioritize"),
        edge("prioritize", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "discover_manifests"
