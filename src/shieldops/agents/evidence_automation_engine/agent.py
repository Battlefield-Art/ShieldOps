# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the evidence_automation_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.evidence_automation_engine.models import EvidenceAutomationEngineState
from shieldops.agents.evidence_automation_engine.nodes import (
    collect_evidence,
    identify_requirements,
    package_evidence,
    report,
    submit_attestation,
    validate_artifacts,
)
from shieldops.agents.evidence_automation_engine.tools import EvidenceAutomationEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class EvidenceAutomationEngineAgent(Agent):
    """Declarative spec for ``evidence_automation_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "evidence_automation_engine"
    state_model = EvidenceAutomationEngineState
    toolkit_factory = EvidenceAutomationEngineToolkit

    nodes = {
        "identify_requirements": identify_requirements,
        "collect_evidence": collect_evidence,
        "validate_artifacts": validate_artifacts,
        "package_evidence": package_evidence,
        "submit_attestation": submit_attestation,
        "report": report,
    }

    edges = [
        edge("identify_requirements", "collect_evidence"),
        edge("collect_evidence", "validate_artifacts"),
        edge("validate_artifacts", "package_evidence"),
        edge("package_evidence", "submit_attestation"),
        edge("submit_attestation", "report"),
        edge("report", END),
    ]

    entry = "identify_requirements"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``EvidenceAutomationEngineRunner`` keep working via this alias; new code
# should use ``EvidenceAutomationEngineAgent`` directly through AgentRuntime.
EvidenceAutomationEngineRunner = EvidenceAutomationEngineAgent
