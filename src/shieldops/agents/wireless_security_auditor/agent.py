# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the wireless_security_auditor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.wireless_security_auditor.models import WirelessSecurityAuditorState
from shieldops.agents.wireless_security_auditor.nodes import (
    assess_risk,
    check_encryption,
    detect_rogues,
    discover_networks,
    generate_report,
    scan_access_points,
)
from shieldops.agents.wireless_security_auditor.tools import WirelessSecurityToolkit


class WirelessSecurityAuditorAgent(Agent):
    """Declarative spec for ``wireless_security_auditor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "wireless_security_auditor"
    state_model = WirelessSecurityAuditorState
    toolkit_factory = WirelessSecurityToolkit

    nodes = {
        "discover_networks": discover_networks,
        "scan_access_points": scan_access_points,
        "check_encryption": check_encryption,
        "detect_rogues": detect_rogues,
        "assess_risk": assess_risk,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_networks", "scan_access_points"),
        edge("scan_access_points", "check_encryption"),
        edge("check_encryption", "detect_rogues"),
        edge("assess_risk", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_rogues"
        #   routed by _should_assess() — wrap with cond(...)
    ]

    entry = "discover_networks"
