# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the firmware_security_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.firmware_security_scanner.models import FirmwareSecurityScannerState
from shieldops.agents.firmware_security_scanner.nodes import (
    analyze_components,
    assess_risk,
    check_crypto,
    extract_firmware,
    generate_report,
    scan_vulnerabilities,
)
from shieldops.agents.firmware_security_scanner.tools import FirmwareSecurityScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class FirmwareSecurityScannerAgent(Agent):
    """Declarative spec for ``firmware_security_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "firmware_security_scanner"
    state_model = FirmwareSecurityScannerState
    toolkit_factory = FirmwareSecurityScannerToolkit

    nodes = {
        "extract_firmware": extract_firmware,
        "analyze_components": analyze_components,
        "scan_vulnerabilities": scan_vulnerabilities,
        "check_crypto": check_crypto,
        "assess_risk": assess_risk,
        "generate_report": generate_report,
    }

    edges = [
        edge("analyze_components", "scan_vulnerabilities"),
        edge("check_crypto", "assess_risk"),
        edge("assess_risk", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "extract_firmware"
        #   routed by _should_analyze() — wrap with cond(...)
        # TODO: conditional edge from "scan_vulnerabilities"
        #   routed by _should_check_crypto() — wrap with cond(...)
    ]

    entry = "extract_firmware"
