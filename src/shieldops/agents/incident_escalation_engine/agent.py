# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_escalation_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_escalation_engine.models import IncidentEscalationEngineState
from shieldops.agents.incident_escalation_engine.nodes import (
    assess_severity,
    determine_escalation,
    evaluate_impact,
    notify_responders,
    report,
    track_response,
)
from shieldops.agents.incident_escalation_engine.tools import IncidentEscalationEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentEscalationEngineAgent(Agent):
    """Declarative spec for ``incident_escalation_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_escalation_engine"
    state_model = IncidentEscalationEngineState
    toolkit_factory = IncidentEscalationEngineToolkit

    nodes = {
        "assess_severity": assess_severity,
        "evaluate_impact": evaluate_impact,
        "determine_escalation": determine_escalation,
        "notify_responders": notify_responders,
        "track_response": track_response,
        "report": report,
    }

    edges = [
        edge("assess_severity", "evaluate_impact"),
        edge("evaluate_impact", "determine_escalation"),
        edge("determine_escalation", "notify_responders"),
        edge("notify_responders", "track_response"),
        edge("track_response", "report"),
        edge("report", END),
    ]

    entry = "assess_severity"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``IncidentEscalationEngineRunner`` keep working via this alias; new code
# should use ``IncidentEscalationEngineAgent`` directly through AgentRuntime.
IncidentEscalationEngineRunner = IncidentEscalationEngineAgent
