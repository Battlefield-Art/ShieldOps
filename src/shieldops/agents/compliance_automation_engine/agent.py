# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the compliance_automation_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.compliance_automation_engine.models import ComplianceAutomationEngineState
from shieldops.agents.compliance_automation_engine.nodes import (
    assess_gaps,
    collect_evidence,
    discover_controls,
    generate_attestation,
    report,
    test_controls,
)
from shieldops.agents.compliance_automation_engine.tools import ComplianceAutomationEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class ComplianceAutomationEngineAgent(Agent):
    """Declarative spec for ``compliance_automation_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "compliance_automation_engine"
    state_model = ComplianceAutomationEngineState
    toolkit_factory = ComplianceAutomationEngineToolkit

    nodes = {
        "discover_controls": discover_controls,
        "test_controls": test_controls,
        "collect_evidence": collect_evidence,
        "assess_gaps": assess_gaps,
        "generate_attestation": generate_attestation,
        "report": report,
    }

    edges = [
        edge("generate_attestation", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_controls" via _check_error()
        # TODO: cond(...) from "test_controls" via _check_error()
        # TODO: cond(...) from "collect_evidence" via _check_error()
        # TODO: cond(...) from "assess_gaps" via _check_error()
    ]

    entry = "discover_controls"
