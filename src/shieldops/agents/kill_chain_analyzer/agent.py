# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the kill_chain_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.kill_chain_analyzer.models import KillChainAnalyzerState
from shieldops.agents.kill_chain_analyzer.nodes import (
    correlate_stages,
    identify_gaps,
    ingest_alerts,
    map_kill_chain,
    recommend,
    report,
)
from shieldops.agents.kill_chain_analyzer.tools import KillChainAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class KillChainAnalyzerAgent(Agent):
    """Declarative spec for ``kill_chain_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "kill_chain_analyzer"
    state_model = KillChainAnalyzerState
    toolkit_factory = KillChainAnalyzerToolkit

    nodes = {
        "ingest_alerts": ingest_alerts,
        "map_kill_chain": map_kill_chain,
        "identify_gaps": identify_gaps,
        "correlate_stages": correlate_stages,
        "recommend": recommend,
        "report": report,
    }

    edges = [
        edge("recommend", "report"),
        edge("report", END),
        # TODO: cond(...) from "ingest_alerts" via _check_error()
        # TODO: cond(...) from "map_kill_chain" via _check_error()
        # TODO: cond(...) from "identify_gaps" via _check_error()
        # TODO: cond(...) from "correlate_stages" via _check_error()
    ]

    entry = "ingest_alerts"
