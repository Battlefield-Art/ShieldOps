# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_copilot_agent agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_copilot_agent.models import SecurityCopilotAgentState
from shieldops.agents.security_copilot_agent.nodes import (
    analyze,
    execute_action,
    gather_context,
    generate_report,
    receive_query,
    recommend,
)
from shieldops.agents.security_copilot_agent.tools import SecurityCopilotAgentToolkit


class SecurityCopilotAgentAgent(Agent):
    """Declarative spec for ``security_copilot_agent`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_copilot_agent"
    state_model = SecurityCopilotAgentState
    toolkit_factory = SecurityCopilotAgentToolkit

    nodes = {
        "receive_query": receive_query,
        "gather_context": gather_context,
        "analyze": analyze,
        "recommend": recommend,
        "execute_action": execute_action,
        "generate_report": generate_report,
    }

    edges = [
        edge("receive_query", "gather_context"),
        edge("gather_context", "analyze"),
        edge("analyze", "recommend"),
        edge("execute_action", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "recommend"
        #   routed by _should_execute() — wrap with cond(...)
    ]

    entry = "receive_query"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityCopilotAgentRunner`` keep working via this alias; new code
# should use ``SecurityCopilotAgentAgent`` directly through AgentRuntime.
SecurityCopilotAgentRunner = SecurityCopilotAgentAgent
