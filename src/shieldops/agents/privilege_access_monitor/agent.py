# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the privilege_access_monitor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.privilege_access_monitor.models import PrivilegeAccessMonitorState
from shieldops.agents.privilege_access_monitor.nodes import (
    assess_risk,
    audit_sessions,
    detect_abuse,
    discover_accounts,
    enforce_jit,
    generate_report,
)
from shieldops.agents.privilege_access_monitor.tools import PrivilegeAccessMonitorToolkit
from shieldops.agents.runtime import END, Agent, edge


class PrivilegeAccessMonitorAgent(Agent):
    """Declarative spec for ``privilege_access_monitor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "privilege_access_monitor"
    state_model = PrivilegeAccessMonitorState
    toolkit_factory = PrivilegeAccessMonitorToolkit

    nodes = {
        "discover_accounts": discover_accounts,
        "audit_sessions": audit_sessions,
        "detect_abuse": detect_abuse,
        "assess_risk": assess_risk,
        "enforce_jit": enforce_jit,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_accounts", "audit_sessions"),
        edge("audit_sessions", "detect_abuse"),
        edge("detect_abuse", "assess_risk"),
        edge("enforce_jit", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_risk"
        #   routed by _should_enforce() — wrap with cond(...)
    ]

    entry = "discover_accounts"
