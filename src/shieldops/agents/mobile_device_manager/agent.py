# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the mobile_device_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.mobile_device_manager.models import MobileDeviceManagerState
from shieldops.agents.mobile_device_manager.nodes import (
    assess_compliance,
    check_apps,
    check_enrollment,
    discover_devices,
    enforce_policies,
    generate_report,
)
from shieldops.agents.mobile_device_manager.tools import MobileDeviceManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class MobileDeviceManagerAgent(Agent):
    """Declarative spec for ``mobile_device_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "mobile_device_manager"
    state_model = MobileDeviceManagerState
    toolkit_factory = MobileDeviceManagerToolkit

    nodes = {
        "discover_devices": discover_devices,
        "check_enrollment": check_enrollment,
        "assess_compliance": assess_compliance,
        "check_apps": check_apps,
        "enforce_policies": enforce_policies,
        "report": generate_report,
        # TODO: resolve node fn for "error_end"
    }

    edges = [
        edge("check_enrollment", "assess_compliance"),
        edge("assess_compliance", "check_apps"),
        edge("check_apps", "enforce_policies"),
        edge("enforce_policies", "report"),
        edge("report", END),
        edge("error_end", END),
        # TODO: conditional edge from "discover_devices"
        #   routed by _check_error() — wrap with cond(...)
    ]

    entry = "discover_devices"
