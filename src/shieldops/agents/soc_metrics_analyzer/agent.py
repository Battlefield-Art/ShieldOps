# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the soc_metrics_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.soc_metrics_analyzer.models import SOCMetricsAnalyzerState
from shieldops.agents.soc_metrics_analyzer.nodes import (
    analyze_performance,
    benchmark_industry,
    collect_metrics,
    detect_bottlenecks,
    generate_report,
    recommend_improvements,
)
from shieldops.agents.soc_metrics_analyzer.tools import SOCMetricsAnalyzerToolkit


class SocMetricsAnalyzerAgent(Agent):
    """Declarative spec for ``soc_metrics_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "soc_metrics_analyzer"
    state_model = SOCMetricsAnalyzerState
    toolkit_factory = SOCMetricsAnalyzerToolkit

    nodes = {
        "collect_metrics": collect_metrics,
        "analyze_performance": analyze_performance,
        "detect_bottlenecks": detect_bottlenecks,
        "benchmark_industry": benchmark_industry,
        "recommend_improvements": recommend_improvements,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_metrics", "analyze_performance"),
        edge("analyze_performance", "detect_bottlenecks"),
        edge("benchmark_industry", "recommend_improvements"),
        edge("recommend_improvements", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_bottlenecks"
        #   routed by _has_bottlenecks() — wrap with cond(...)
    ]

    entry = "collect_metrics"
