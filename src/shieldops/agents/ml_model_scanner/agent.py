# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ml_model_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ml_model_scanner.models import MLModelScannerState
from shieldops.agents.ml_model_scanner.nodes import (
    assess_risk,
    check_provenance,
    detect_backdoors,
    discover_models,
    generate_report,
    scan_artifacts,
)
from shieldops.agents.ml_model_scanner.tools import MLModelScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class MlModelScannerAgent(Agent):
    """Declarative spec for ``ml_model_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ml_model_scanner"
    state_model = MLModelScannerState
    toolkit_factory = MLModelScannerToolkit

    nodes = {
        "discover_models": discover_models,
        "scan_artifacts": scan_artifacts,
        "check_provenance": check_provenance,
        "detect_backdoors": detect_backdoors,
        "assess_risk": assess_risk,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_models", "scan_artifacts"),
        edge("scan_artifacts", "check_provenance"),
        edge("detect_backdoors", "assess_risk"),
        edge("assess_risk", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "check_provenance"
        #   routed by _should_detect_backdoors() — wrap with cond(...)
    ]

    entry = "discover_models"
