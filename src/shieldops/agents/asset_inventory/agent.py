# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the asset_inventory agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.asset_inventory.models import AssetInventoryState
from shieldops.agents.asset_inventory.nodes import (
    assess_risk,
    assign_owners,
    classify,
    discover,
    generate_report,
    reconcile,
)
from shieldops.agents.asset_inventory.tools import AssetInventoryToolkit
from shieldops.agents.runtime import END, Agent, edge


class AssetInventoryAgent(Agent):
    """Declarative spec for ``asset_inventory`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "asset_inventory"
    state_model = AssetInventoryState
    toolkit_factory = AssetInventoryToolkit

    nodes = {
        "discover": discover,
        "classify": classify,
        "assign_owners": assign_owners,
        "assess_risk": assess_risk,
        "reconcile": reconcile,
        "report": generate_report,
    }

    edges = [
        edge("discover", "classify"),
        edge("classify", "assign_owners"),
        edge("assign_owners", "assess_risk"),
        edge("assess_risk", "reconcile"),
        edge("reconcile", "report"),
        edge("report", END),
    ]

    entry = "discover"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AssetInventoryRunner`` keep working via this alias; new code
# should use ``AssetInventoryAgent`` directly through AgentRuntime.
AssetInventoryRunner = AssetInventoryAgent
