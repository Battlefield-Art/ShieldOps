# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the sla_violation_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.sla_violation_detector.models import SLAViolationDetectorState
from shieldops.agents.sla_violation_detector.nodes import (
    calculate_impact,
    collect_metrics,
    detect_violations,
    evaluate_thresholds,
    notify_owners,
    report,
)
from shieldops.agents.sla_violation_detector.tools import SLAViolationDetectorToolkit


class SlaViolationDetectorAgent(Agent):
    """Declarative spec for ``sla_violation_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "sla_violation_detector"
    state_model = SLAViolationDetectorState
    toolkit_factory = SLAViolationDetectorToolkit

    nodes = {
        "collect_metrics": collect_metrics,
        "evaluate_thresholds": evaluate_thresholds,
        "detect_violations": detect_violations,
        "calculate_impact": calculate_impact,
        "notify_owners": notify_owners,
        "report": report,
    }

    edges = [
        edge("collect_metrics", "evaluate_thresholds"),
        edge("evaluate_thresholds", "detect_violations"),
        edge("detect_violations", "calculate_impact"),
        edge("calculate_impact", "notify_owners"),
        edge("notify_owners", "report"),
        edge("report", END),
    ]

    entry = "collect_metrics"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SLAViolationDetectorRunner`` keep working via this alias; new code
# should use ``SlaViolationDetectorAgent`` directly through AgentRuntime.
SLAViolationDetectorRunner = SlaViolationDetectorAgent
