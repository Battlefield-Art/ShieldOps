# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_storage_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_storage_scanner.models import CloudStorageScannerState
from shieldops.agents.cloud_storage_scanner.nodes import (
    assess_encryption,
    detect_sensitive_data,
    discover_buckets,
    generate_report,
    remediate_issues,
    scan_permissions,
)
from shieldops.agents.cloud_storage_scanner.tools import CloudStorageScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudStorageScannerAgent(Agent):
    """Declarative spec for ``cloud_storage_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_storage_scanner"
    state_model = CloudStorageScannerState
    toolkit_factory = CloudStorageScannerToolkit

    nodes = {
        "discover_buckets": discover_buckets,
        "scan_permissions": scan_permissions,
        "detect_sensitive_data": detect_sensitive_data,
        "assess_encryption": assess_encryption,
        "remediate_issues": remediate_issues,
        "report": generate_report,
    }

    edges = [
        edge("discover_buckets", "scan_permissions"),
        edge("scan_permissions", "detect_sensitive_data"),
        edge("detect_sensitive_data", "assess_encryption"),
        edge("assess_encryption", "remediate_issues"),
        edge("remediate_issues", "report"),
        edge("report", END),
    ]

    entry = "discover_buckets"
