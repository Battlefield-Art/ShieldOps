# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ai_triage_accelerator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ai_triage_accelerator.models import AITriageAcceleratorState
from shieldops.agents.ai_triage_accelerator.nodes import (
    batch_ingest,
    confidence_score,
    enrich_context,
    parallel_classify,
    report,
    route_decisions,
)
from shieldops.agents.ai_triage_accelerator.tools import AITriageAcceleratorToolkit
from shieldops.agents.runtime import END, Agent, edge


class AiTriageAcceleratorAgent(Agent):
    """Declarative spec for ``ai_triage_accelerator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ai_triage_accelerator"
    state_model = AITriageAcceleratorState
    toolkit_factory = AITriageAcceleratorToolkit

    nodes = {
        "batch_ingest": batch_ingest,
        "parallel_classify": parallel_classify,
        "enrich_context": enrich_context,
        "confidence_score": confidence_score,
        "route_decisions": route_decisions,
        "report": report,
    }

    edges = [
        edge("batch_ingest", "parallel_classify"),
        edge("parallel_classify", "enrich_context"),
        edge("enrich_context", "confidence_score"),
        edge("confidence_score", "route_decisions"),
        edge("route_decisions", "report"),
        edge("report", END),
    ]

    entry = "batch_ingest"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AITriageAcceleratorRunner`` keep working via this alias; new code
# should use ``AiTriageAcceleratorAgent`` directly through AgentRuntime.
AITriageAcceleratorRunner = AiTriageAcceleratorAgent
