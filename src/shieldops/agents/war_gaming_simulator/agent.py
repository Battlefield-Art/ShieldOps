# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the war_gaming_simulator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.war_gaming_simulator.models import WarGamingSimulatorState
from shieldops.agents.war_gaming_simulator.nodes import (
    assign_teams,
    design_scenario,
    execute_rounds,
    observe,
    report,
    score,
)
from shieldops.agents.war_gaming_simulator.tools import WarGamingSimulatorToolkit


class WarGamingSimulatorAgent(Agent):
    """Declarative spec for ``war_gaming_simulator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "war_gaming_simulator"
    state_model = WarGamingSimulatorState
    toolkit_factory = WarGamingSimulatorToolkit

    nodes = {
        "design_scenario": design_scenario,
        "assign_teams": assign_teams,
        "execute_rounds": execute_rounds,
        "observe": observe,
        "score": score,
        "report": report,
    }

    edges = [
        edge("score", "report"),
        edge("report", END),
        # TODO: cond(...) from "design_scenario" via _check_error()
        # TODO: cond(...) from "assign_teams" via _check_error()
        # TODO: cond(...) from "execute_rounds" via _check_error()
        # TODO: cond(...) from "observe" via _check_error()
    ]

    entry = "design_scenario"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``WarGamingSimulatorRunner`` keep working via this alias; new code
# should use ``WarGamingSimulatorAgent`` directly through AgentRuntime.
WarGamingSimulatorRunner = WarGamingSimulatorAgent
