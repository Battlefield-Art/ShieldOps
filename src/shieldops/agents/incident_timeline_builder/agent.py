# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_timeline_builder agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_timeline_builder.models import IncidentTimelineBuilderState
from shieldops.agents.incident_timeline_builder.nodes import (
    build_timeline,
    collect_events,
    correlate_sources,
    generate_narrative,
    generate_report,
    identify_root_cause,
)
from shieldops.agents.incident_timeline_builder.tools import IncidentTimelineBuilderToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentTimelineBuilderAgent(Agent):
    """Declarative spec for ``incident_timeline_builder`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_timeline_builder"
    state_model = IncidentTimelineBuilderState
    toolkit_factory = IncidentTimelineBuilderToolkit

    nodes = {
        "collect_events": collect_events,
        "correlate_sources": correlate_sources,
        "build_timeline": build_timeline,
        "identify_root_cause": identify_root_cause,
        "generate_narrative": generate_narrative,
        "report": generate_report,
    }

    edges = [
        edge("collect_events", "correlate_sources"),
        edge("correlate_sources", "build_timeline"),
        edge("build_timeline", "identify_root_cause"),
        edge("identify_root_cause", "generate_narrative"),
        edge("generate_narrative", "report"),
        edge("report", END),
    ]

    entry = "collect_events"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``IncidentTimelineBuilderRunner`` keep working via this alias; new code
# should use ``IncidentTimelineBuilderAgent`` directly through AgentRuntime.
IncidentTimelineBuilderRunner = IncidentTimelineBuilderAgent
