# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the runtime_protection_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.runtime_protection_engine.models import RuntimeProtectionEngineState
from shieldops.agents.runtime_protection_engine.nodes import (
    analyze_behavior,
    collect_telemetry,
    detect_anomalies,
    enforce_policies,
    generate_alerts,
    generate_report,
)
from shieldops.agents.runtime_protection_engine.tools import RuntimeProtectionEngineToolkit


class RuntimeProtectionEngineAgent(Agent):
    """Declarative spec for ``runtime_protection_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "runtime_protection_engine"
    state_model = RuntimeProtectionEngineState
    toolkit_factory = RuntimeProtectionEngineToolkit

    nodes = {
        "collect_telemetry": collect_telemetry,
        "analyze_behavior": analyze_behavior,
        "detect_anomalies": detect_anomalies,
        "enforce_policies": enforce_policies,
        "generate_alerts": generate_alerts,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_telemetry", "analyze_behavior"),
        edge("analyze_behavior", "detect_anomalies"),
        edge("generate_alerts", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "detect_anomalies" via should_enforce_policies()
        # TODO: cond(...) from "enforce_policies" via should_generate_alerts()
    ]

    entry = "collect_telemetry"
