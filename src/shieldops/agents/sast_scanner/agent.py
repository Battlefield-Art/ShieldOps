# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the sast_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.sast_scanner.models import SASTScannerState
from shieldops.agents.sast_scanner.nodes import (
    analyze_dataflow,
    discover_files,
    generate_report,
    parse_ast,
    prioritize,
    scan_patterns,
)
from shieldops.agents.sast_scanner.tools import SASTScannerToolkit


class SastScannerAgent(Agent):
    """Declarative spec for ``sast_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "sast_scanner"
    state_model = SASTScannerState
    toolkit_factory = SASTScannerToolkit

    nodes = {
        "discover_files": discover_files,
        "parse_ast": parse_ast,
        "scan_patterns": scan_patterns,
        "analyze_dataflow": analyze_dataflow,
        "prioritize": prioritize,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_files", "parse_ast"),
        edge("parse_ast", "scan_patterns"),
        edge("scan_patterns", "analyze_dataflow"),
        edge("analyze_dataflow", "prioritize"),
        edge("prioritize", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "discover_files"
