# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the threat_modeling agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.threat_modeling.models import ThreatModelingState
from shieldops.agents.threat_modeling.nodes import (
    analyze_threats,
    assess_risk,
    discover_architecture,
    recommend_mitigations,
)
from shieldops.agents.threat_modeling.tools import ThreatModelingToolkit


class ThreatModelingAgent(Agent):
    """Declarative spec for ``threat_modeling`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "threat_modeling"
    state_model = ThreatModelingState
    toolkit_factory = ThreatModelingToolkit

    nodes = {
        "discover_architecture": discover_architecture,
        "analyze_threats": analyze_threats,
        "assess_risk": assess_risk,
        "recommend_mitigations": recommend_mitigations,
    }

    edges = [
        edge("discover_architecture", "analyze_threats"),
        edge("analyze_threats", "assess_risk"),
        edge("assess_risk", "recommend_mitigations"),
        edge("recommend_mitigations", END),
    ]

    entry = "discover_architecture"
