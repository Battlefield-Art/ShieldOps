# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_resilience agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_resilience.models import DataResilienceState
from shieldops.agents.data_resilience.nodes import (
    assess_protection,
    detect_anomalies,
    enforce_immutability,
    generate_report,
    inventory_data_assets,
    validate_recovery,
)
from shieldops.agents.data_resilience.tools import DataResilienceToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataResilienceAgent(Agent):
    """Declarative spec for ``data_resilience`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_resilience"
    state_model = DataResilienceState
    toolkit_factory = DataResilienceToolkit

    nodes = {
        "inventory_data_assets": inventory_data_assets,
        "assess_protection": assess_protection,
        "detect_anomalies": detect_anomalies,
        "enforce_immutability": enforce_immutability,
        "validate_recovery": validate_recovery,
        "generate_report": generate_report,
    }

    edges = [
        edge("inventory_data_assets", "assess_protection"),
        edge("assess_protection", "detect_anomalies"),
        edge("enforce_immutability", "validate_recovery"),
        edge("validate_recovery", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_anomalies"
        #   routed by _has_anomalies() — wrap with cond(...)
    ]

    entry = "inventory_data_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DataResilienceRunner`` keep working via this alias; new code
# should use ``DataResilienceAgent`` directly through AgentRuntime.
DataResilienceRunner = DataResilienceAgent
