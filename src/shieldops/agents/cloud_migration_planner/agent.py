# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_migration_planner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_migration_planner.models import CloudMigrationPlannerState
from shieldops.agents.cloud_migration_planner.nodes import (
    assess_readiness,
    discover_workloads,
    execute_migration,
    generate_report,
    plan_migration,
    validate_dependencies,
)
from shieldops.agents.cloud_migration_planner.tools import CloudMigrationPlannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudMigrationPlannerAgent(Agent):
    """Declarative spec for ``cloud_migration_planner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_migration_planner"
    state_model = CloudMigrationPlannerState
    toolkit_factory = CloudMigrationPlannerToolkit

    nodes = {
        "discover_workloads": discover_workloads,
        "assess_readiness": assess_readiness,
        "plan_migration": plan_migration,
        "validate_dependencies": validate_dependencies,
        "execute_migration": execute_migration,
        "report": generate_report,
    }

    edges = [
        edge("discover_workloads", "assess_readiness"),
        edge("assess_readiness", "plan_migration"),
        edge("plan_migration", "validate_dependencies"),
        edge("validate_dependencies", "execute_migration"),
        edge("execute_migration", "report"),
        edge("report", END),
    ]

    entry = "discover_workloads"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudMigrationPlannerRunner`` keep working via this alias; new code
# should use ``CloudMigrationPlannerAgent`` directly through AgentRuntime.
CloudMigrationPlannerRunner = CloudMigrationPlannerAgent
