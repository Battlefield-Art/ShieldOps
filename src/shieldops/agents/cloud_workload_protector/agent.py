# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_workload_protector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_workload_protector.models import CloudWorkloadProtectorState
from shieldops.agents.cloud_workload_protector.nodes import (
    analyze_drift,
    assess_vulnerabilities,
    contain_threats,
    detect_anomalies,
    generate_report,
    scan_workloads,
)
from shieldops.agents.cloud_workload_protector.tools import CloudWorkloadProtectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudWorkloadProtectorAgent(Agent):
    """Declarative spec for ``cloud_workload_protector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_workload_protector"
    state_model = CloudWorkloadProtectorState
    toolkit_factory = CloudWorkloadProtectorToolkit

    nodes = {
        "scan_workloads": scan_workloads,
        "detect_anomalies": detect_anomalies,
        "analyze_drift": analyze_drift,
        "assess_vulnerabilities": assess_vulnerabilities,
        "contain_threats": contain_threats,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_workloads", "detect_anomalies"),
        edge("detect_anomalies", "analyze_drift"),
        edge("analyze_drift", "assess_vulnerabilities"),
        edge("contain_threats", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_vulnerabilities"
        #   routed by _should_contain() — wrap with cond(...)
    ]

    entry = "scan_workloads"
