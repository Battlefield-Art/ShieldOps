# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_simulator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_simulator.models import IncidentSimulatorState
from shieldops.agents.incident_simulator.nodes import (
    debrief,
    design_scenario,
    inject_events,
    observe_response,
    report,
    score_performance,
)
from shieldops.agents.incident_simulator.tools import IncidentSimulatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentSimulatorAgent(Agent):
    """Declarative spec for ``incident_simulator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_simulator"
    state_model = IncidentSimulatorState
    toolkit_factory = IncidentSimulatorToolkit

    nodes = {
        "design_scenario": design_scenario,
        "inject_events": inject_events,
        "observe_response": observe_response,
        "score_performance": score_performance,
        "debrief": debrief,
        "report": report,
    }

    edges = [
        edge("inject_events", "observe_response"),
        edge("observe_response", "score_performance"),
        edge("score_performance", "debrief"),
        edge("debrief", "report"),
        edge("report", END),
        # TODO: cond(...) from "design_scenario" via _route_after_design()
    ]

    entry = "design_scenario"
