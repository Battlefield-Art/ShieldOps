# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the dark_web_intelligence agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.dark_web_intelligence.models import DarkWebIntelligenceState
from shieldops.agents.dark_web_intelligence.nodes import (
    analyze_threats,
    assess_credibility,
    collect_mentions,
    generate_report,
    monitor_forums,
    send_alerts,
)
from shieldops.agents.dark_web_intelligence.tools import DarkWebIntelligenceToolkit
from shieldops.agents.runtime import END, Agent, edge


class DarkWebIntelligenceAgent(Agent):
    """Declarative spec for ``dark_web_intelligence`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "dark_web_intelligence"
    state_model = DarkWebIntelligenceState
    toolkit_factory = DarkWebIntelligenceToolkit

    nodes = {
        "monitor_forums": monitor_forums,
        "collect_mentions": collect_mentions,
        "analyze_threats": analyze_threats,
        "assess_credibility": assess_credibility,
        "send_alerts": send_alerts,
        "generate_report": generate_report,
    }

    edges = [
        edge("monitor_forums", "collect_mentions"),
        edge("collect_mentions", "analyze_threats"),
        edge("analyze_threats", "assess_credibility"),
        edge("send_alerts", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_credibility"
        #   routed by _should_alert() — wrap with cond(...)
    ]

    entry = "monitor_forums"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DarkWebIntelligenceRunner`` keep working via this alias; new code
# should use ``DarkWebIntelligenceAgent`` directly through AgentRuntime.
DarkWebIntelligenceRunner = DarkWebIntelligenceAgent
