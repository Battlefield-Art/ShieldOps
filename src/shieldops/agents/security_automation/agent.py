# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_automation agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_automation.models import SecurityAutomationState
from shieldops.agents.security_automation.nodes import (
    execute_response,
    select_playbook,
    triage_alerts,
    validate_and_learn,
)
from shieldops.agents.security_automation.tools import SecurityAutomationToolkit


class SecurityAutomationAgent(Agent):
    """Declarative spec for ``security_automation`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_automation"
    state_model = SecurityAutomationState
    toolkit_factory = SecurityAutomationToolkit

    nodes = {
        "triage_alerts": triage_alerts,
        "select_playbook": select_playbook,
        "execute_response": execute_response,
        "validate_and_learn": validate_and_learn,
    }

    edges = [
        edge("select_playbook", "execute_response"),
        edge("execute_response", "validate_and_learn"),
        edge("validate_and_learn", END),
        # TODO: cond(...) from "triage_alerts" via should_continue_after_triage()
    ]

    entry = "triage_alerts"
