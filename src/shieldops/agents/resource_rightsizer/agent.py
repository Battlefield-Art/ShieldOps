# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the resource_rightsizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.resource_rightsizer.models import ResourceRightsizerState
from shieldops.agents.resource_rightsizer.nodes import (
    analyze_patterns,
    collect_utilization,
    identify_overprovisioned,
    recommend_sizes,
    report,
    validate_impact,
)
from shieldops.agents.resource_rightsizer.tools import ResourceRightsizerToolkit
from shieldops.agents.runtime import END, Agent, edge


class ResourceRightsizerAgent(Agent):
    """Declarative spec for ``resource_rightsizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "resource_rightsizer"
    state_model = ResourceRightsizerState
    toolkit_factory = ResourceRightsizerToolkit

    nodes = {
        "collect_utilization": collect_utilization,
        "analyze_patterns": analyze_patterns,
        "identify_overprovisioned": identify_overprovisioned,
        "recommend_sizes": recommend_sizes,
        "validate_impact": validate_impact,
        "report": report,
    }

    edges = [
        edge("validate_impact", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_utilization" via _check_error()
        # TODO: cond(...) from "analyze_patterns" via _check_error()
        # TODO: cond(...) from "identify_overprovisioned" via _check_error()
        # TODO: cond(...) from "recommend_sizes" via _check_error()
    ]

    entry = "collect_utilization"
