# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_data_mesh agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_data_mesh.models import SecurityDataMeshState
from shieldops.agents.security_data_mesh.nodes import (
    assess_quality,
    discover_domains,
    federate_queries,
    generate_insights,
    generate_report,
    map_data_products,
)
from shieldops.agents.security_data_mesh.tools import SecurityDataMeshToolkit


class SecurityDataMeshAgent(Agent):
    """Declarative spec for ``security_data_mesh`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_data_mesh"
    state_model = SecurityDataMeshState
    toolkit_factory = SecurityDataMeshToolkit

    nodes = {
        "discover_domains": discover_domains,
        "map_data_products": map_data_products,
        "assess_quality": assess_quality,
        "federate_queries": federate_queries,
        "generate_insights": generate_insights,
        "report": generate_report,
    }

    edges = [
        edge("discover_domains", "map_data_products"),
        edge("map_data_products", "assess_quality"),
        edge("assess_quality", "federate_queries"),
        edge("federate_queries", "generate_insights"),
        edge("generate_insights", "report"),
        edge("report", END),
    ]

    entry = "discover_domains"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityDataMeshRunner`` keep working via this alias; new code
# should use ``SecurityDataMeshAgent`` directly through AgentRuntime.
SecurityDataMeshRunner = SecurityDataMeshAgent
