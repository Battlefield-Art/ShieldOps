# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_resource_tagger agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_resource_tagger.models import CloudResourceTaggerState
from shieldops.agents.cloud_resource_tagger.nodes import (
    analyze_metadata,
    apply_tags,
    generate_report,
    generate_tags,
    scan_resources,
    validate_compliance,
)
from shieldops.agents.cloud_resource_tagger.tools import CloudResourceTaggerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudResourceTaggerAgent(Agent):
    """Declarative spec for ``cloud_resource_tagger`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_resource_tagger"
    state_model = CloudResourceTaggerState
    toolkit_factory = CloudResourceTaggerToolkit

    nodes = {
        "scan_resources": scan_resources,
        "analyze_metadata": analyze_metadata,
        "generate_tags": generate_tags,
        "validate_compliance": validate_compliance,
        "apply_tags": apply_tags,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_resources", "analyze_metadata"),
        edge("analyze_metadata", "generate_tags"),
        edge("generate_tags", "validate_compliance"),
        edge("apply_tags", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "validate_compliance"
        #   routed by _should_apply() — wrap with cond(...)
    ]

    entry = "scan_resources"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudResourceTaggerRunner`` keep working via this alias; new code
# should use ``CloudResourceTaggerAgent`` directly through AgentRuntime.
CloudResourceTaggerRunner = CloudResourceTaggerAgent
