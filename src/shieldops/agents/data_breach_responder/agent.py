# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_breach_responder agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_breach_responder.models import DataBreachResponderState
from shieldops.agents.data_breach_responder.nodes import (
    assess_scope,
    contain,
    detect_breach,
    notify_authorities,
    notify_subjects,
    report,
)
from shieldops.agents.data_breach_responder.tools import DataBreachResponderToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataBreachResponderAgent(Agent):
    """Declarative spec for ``data_breach_responder`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_breach_responder"
    state_model = DataBreachResponderState
    toolkit_factory = DataBreachResponderToolkit

    nodes = {
        "detect_breach": detect_breach,
        "assess_scope": assess_scope,
        "contain": contain,
        "notify_authorities": notify_authorities,
        "notify_subjects": notify_subjects,
        "report": report,
    }

    edges = [
        edge("notify_subjects", "report"),
        edge("report", END),
        # TODO: cond(...) from "detect_breach" via _check_error()
        # TODO: cond(...) from "assess_scope" via _check_error()
        # TODO: cond(...) from "contain" via _check_error()
        # TODO: cond(...) from "notify_authorities" via _check_error()
    ]

    entry = "detect_breach"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DataBreachResponderRunner`` keep working via this alias; new code
# should use ``DataBreachResponderAgent`` directly through AgentRuntime.
DataBreachResponderRunner = DataBreachResponderAgent
