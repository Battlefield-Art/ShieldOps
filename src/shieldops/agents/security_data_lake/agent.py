# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_data_lake agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_data_lake.models import SecurityDataLakeState
from shieldops.agents.security_data_lake.nodes import (
    analyze_data,
    execute_queries,
    identify_sources,
    merge_results,
    parse_query,
    report,
)
from shieldops.agents.security_data_lake.tools import SecurityDataLakeToolkit


class SecurityDataLakeAgent(Agent):
    """Declarative spec for ``security_data_lake`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_data_lake"
    state_model = SecurityDataLakeState
    toolkit_factory = SecurityDataLakeToolkit

    nodes = {
        "parse_query": parse_query,
        "identify_sources": identify_sources,
        "execute_queries": execute_queries,
        "merge_results": merge_results,
        "analyze_data": analyze_data,
        "report": report,
    }

    edges = [
        edge("parse_query", "identify_sources"),
        edge("identify_sources", "execute_queries"),
        edge("execute_queries", "merge_results"),
        edge("merge_results", "analyze_data"),
        edge("analyze_data", "report"),
        edge("report", END),
    ]

    entry = "parse_query"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityDataLakeRunner`` keep working via this alias; new code
# should use ``SecurityDataLakeAgent`` directly through AgentRuntime.
SecurityDataLakeRunner = SecurityDataLakeAgent
