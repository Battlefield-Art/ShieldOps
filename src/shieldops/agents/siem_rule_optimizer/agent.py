# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the siem_rule_optimizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.siem_rule_optimizer.models import SIEMRuleOptimizerState
from shieldops.agents.siem_rule_optimizer.nodes import (
    analyze_performance,
    collect_rules,
    detect_overlap,
    generate_report,
    tune_thresholds,
    validate_rules,
)
from shieldops.agents.siem_rule_optimizer.tools import SIEMRuleOptimizerToolkit


class SiemRuleOptimizerAgent(Agent):
    """Declarative spec for ``siem_rule_optimizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "siem_rule_optimizer"
    state_model = SIEMRuleOptimizerState
    toolkit_factory = SIEMRuleOptimizerToolkit

    nodes = {
        "collect_rules": collect_rules,
        "analyze_performance": analyze_performance,
        "detect_overlap": detect_overlap,
        "tune_thresholds": tune_thresholds,
        "validate_rules": validate_rules,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_rules", "analyze_performance"),
        edge("analyze_performance", "detect_overlap"),
        edge("tune_thresholds", "validate_rules"),
        edge("validate_rules", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_overlap"
        #   routed by _should_tune() — wrap with cond(...)
    ]

    entry = "collect_rules"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SIEMRuleOptimizerRunner`` keep working via this alias; new code
# should use ``SiemRuleOptimizerAgent`` directly through AgentRuntime.
SIEMRuleOptimizerRunner = SiemRuleOptimizerAgent
