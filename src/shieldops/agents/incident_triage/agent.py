# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_triage agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_triage.models import IncidentTriageState
from shieldops.agents.incident_triage.nodes import (
    classify,
    deduplicate,
    enrich,
    generate_report,
    ingest,
    route,
)
from shieldops.agents.incident_triage.tools import IncidentTriageToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentTriageAgent(Agent):
    """Declarative spec for ``incident_triage`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_triage"
    state_model = IncidentTriageState
    toolkit_factory = IncidentTriageToolkit

    nodes = {
        "ingest": ingest,
        "classify": classify,
        "enrich": enrich,
        "deduplicate": deduplicate,
        "route": route,
        "generate_report": generate_report,
    }

    edges = [
        edge("ingest", "classify"),
        edge("classify", "enrich"),
        edge("enrich", "deduplicate"),
        edge("deduplicate", "route"),
        edge("route", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "ingest"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``IncidentTriageRunner`` keep working via this alias; new code
# should use ``IncidentTriageAgent`` directly through AgentRuntime.
IncidentTriageRunner = IncidentTriageAgent
