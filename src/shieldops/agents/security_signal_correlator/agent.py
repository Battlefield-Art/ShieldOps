# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_signal_correlator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_signal_correlator.models import SecuritySignalCorrelatorState
from shieldops.agents.security_signal_correlator.nodes import (
    collect_signals,
    correlate,
    generate_incidents,
    generate_report,
    normalize,
    score_confidence,
)
from shieldops.agents.security_signal_correlator.tools import SecuritySignalCorrelatorToolkit


class SecuritySignalCorrelatorAgent(Agent):
    """Declarative spec for ``security_signal_correlator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_signal_correlator"
    state_model = SecuritySignalCorrelatorState
    toolkit_factory = SecuritySignalCorrelatorToolkit

    nodes = {
        "collect_signals": collect_signals,
        "normalize": normalize,
        "correlate": correlate,
        "score_confidence": score_confidence,
        "generate_incidents": generate_incidents,
        "report": generate_report,
    }

    edges = [
        edge("collect_signals", "normalize"),
        edge("normalize", "correlate"),
        edge("correlate", "score_confidence"),
        edge("score_confidence", "generate_incidents"),
        edge("generate_incidents", "report"),
        edge("report", END),
    ]

    entry = "collect_signals"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecuritySignalCorrelatorRunner`` keep working via this alias; new code
# should use ``SecuritySignalCorrelatorAgent`` directly through AgentRuntime.
SecuritySignalCorrelatorRunner = SecuritySignalCorrelatorAgent
