# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the adaptive_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.adaptive_security.models import AdaptiveSecurityState
from shieldops.agents.adaptive_security.nodes import (
    apply_accepted,
    compute_baseline,
    detect_and_propose,
    evaluate_proposals,
)
from shieldops.agents.adaptive_security.tools import AdaptiveSecurityToolkit
from shieldops.agents.runtime import END, Agent, edge


class AdaptiveSecurityAgent(Agent):
    """Declarative spec for ``adaptive_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "adaptive_security"
    state_model = AdaptiveSecurityState
    toolkit_factory = AdaptiveSecurityToolkit

    nodes = {
        "baseline": compute_baseline,
        "detect_and_propose": detect_and_propose,
        "evaluate": evaluate_proposals,
        "apply": apply_accepted,
    }

    edges = [
        edge("baseline", "detect_and_propose"),
        edge("detect_and_propose", "evaluate"),
        edge("apply", END),
        # TODO: conditional edge from "evaluate"
        #   routed by _should_apply() — wrap with cond(...)
    ]

    entry = "baseline"
