# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the war_room_coordinator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.war_room_coordinator.models import WarRoomCoordinatorState
from shieldops.agents.war_room_coordinator.nodes import (
    assign_roles,
    coordinate_comms,
    maintain_timeline,
    open_war_room,
    report,
    track_actions,
)
from shieldops.agents.war_room_coordinator.tools import WarRoomCoordinatorToolkit


class WarRoomCoordinatorAgent(Agent):
    """Declarative spec for ``war_room_coordinator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "war_room_coordinator"
    state_model = WarRoomCoordinatorState
    toolkit_factory = WarRoomCoordinatorToolkit

    nodes = {
        "open_war_room": open_war_room,
        "assign_roles": assign_roles,
        "track_actions": track_actions,
        "maintain_timeline": maintain_timeline,
        "coordinate_comms": coordinate_comms,
        "report": report,
    }

    edges = [
        edge("coordinate_comms", "report"),
        edge("report", END),
        # TODO: cond(...) from "open_war_room" via _check_error()
        # TODO: cond(...) from "assign_roles" via _check_error()
        # TODO: cond(...) from "track_actions" via _check_error()
        # TODO: cond(...) from "maintain_timeline" via _check_error()
    ]

    entry = "open_war_room"
