# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_change_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_change_manager.models import SecurityChangeManagerState
from shieldops.agents.security_change_manager.nodes import (
    approve_or_reject,
    assess_risk,
    check_dependencies,
    generate_report,
    monitor_rollout,
    receive_change,
)
from shieldops.agents.security_change_manager.tools import SecurityChangeManagerToolkit


class SecurityChangeManagerAgent(Agent):
    """Declarative spec for ``security_change_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_change_manager"
    state_model = SecurityChangeManagerState
    toolkit_factory = SecurityChangeManagerToolkit

    nodes = {
        "receive_change": receive_change,
        "assess_risk": assess_risk,
        "check_dependencies": check_dependencies,
        "approve_or_reject": approve_or_reject,
        "monitor_rollout": monitor_rollout,
        "report": generate_report,
    }

    edges = [
        edge("receive_change", "assess_risk"),
        edge("assess_risk", "check_dependencies"),
        edge("check_dependencies", "approve_or_reject"),
        edge("approve_or_reject", "monitor_rollout"),
        edge("monitor_rollout", "report"),
        edge("report", END),
    ]

    entry = "receive_change"
