# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the permission_creep_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.permission_creep_analyzer.models import PermissionCreepAnalyzerState
from shieldops.agents.permission_creep_analyzer.nodes import (
    assess_risk,
    baseline_role,
    collect_permissions,
    detect_creep,
    recommend,
    report,
)
from shieldops.agents.permission_creep_analyzer.tools import PermissionCreepAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class PermissionCreepAnalyzerAgent(Agent):
    """Declarative spec for ``permission_creep_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "permission_creep_analyzer"
    state_model = PermissionCreepAnalyzerState
    toolkit_factory = PermissionCreepAnalyzerToolkit

    nodes = {
        "collect_permissions": collect_permissions,
        "baseline_role": baseline_role,
        "detect_creep": detect_creep,
        "assess_risk": assess_risk,
        "recommend": recommend,
        "report": report,
    }

    edges = [
        edge("recommend", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_permissions" via _check_error()
        # TODO: cond(...) from "baseline_role" via _check_error()
        # TODO: cond(...) from "detect_creep" via _check_error()
        # TODO: cond(...) from "assess_risk" via _check_error()
    ]

    entry = "collect_permissions"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``PermissionCreepAnalyzerRunner`` keep working via this alias; new code
# should use ``PermissionCreepAnalyzerAgent`` directly through AgentRuntime.
PermissionCreepAnalyzerRunner = PermissionCreepAnalyzerAgent
