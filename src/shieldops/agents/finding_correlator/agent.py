# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the finding_correlator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.finding_correlator.models import FindingCorrelatorState
from shieldops.agents.finding_correlator.nodes import (
    collect_findings,
    correlate_related,
    deduplicate,
    generate_report,
    normalize_findings,
    prioritize,
)
from shieldops.agents.finding_correlator.tools import FindingCorrelatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class FindingCorrelatorAgent(Agent):
    """Declarative spec for ``finding_correlator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "finding_correlator"
    state_model = FindingCorrelatorState
    toolkit_factory = FindingCorrelatorToolkit

    nodes = {
        "collect_findings": collect_findings,
        "normalize_findings": normalize_findings,
        "deduplicate": deduplicate,
        "correlate_related": correlate_related,
        "prioritize": prioritize,
        "generate_report": generate_report,
    }

    edges = [
        edge("normalize_findings", "deduplicate"),
        edge("deduplicate", "correlate_related"),
        edge("correlate_related", "prioritize"),
        edge("prioritize", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "collect_findings"
        #   routed by _has_findings() — wrap with cond(...)
    ]

    entry = "collect_findings"
