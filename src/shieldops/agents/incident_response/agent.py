# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_response agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_response.models import IncidentResponseState
from shieldops.agents.incident_response.nodes import (
    assess_incident,
    build_timeline,
    execute_containment,
    finalize_response,
    notify_stakeholders,
    plan_containment,
    plan_eradication,
    plan_recovery,
    validate_response,
)
from shieldops.agents.incident_response.tools import IncidentResponseToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentResponseAgent(Agent):
    """Declarative spec for ``incident_response`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_response"
    state_model = IncidentResponseState
    toolkit_factory = IncidentResponseToolkit

    nodes = {
        "assess_incident": assess_incident,
        "plan_containment": plan_containment,
        "execute_containment": execute_containment,
        "plan_eradication": plan_eradication,
        "plan_recovery": plan_recovery,
        "validate_response": validate_response,
        "notify_stakeholders": notify_stakeholders,
        "build_timeline": build_timeline,
        "finalize_response": finalize_response,
    }

    edges = [
        edge("plan_containment", "execute_containment"),
        edge("execute_containment", "plan_eradication"),
        edge("plan_eradication", "plan_recovery"),
        edge("validate_response", "notify_stakeholders"),
        edge("notify_stakeholders", "build_timeline"),
        edge("build_timeline", "finalize_response"),
        edge("finalize_response", END),
        # TODO: conditional edge from "assess_incident"
        #   routed by should_contain() — wrap with cond(...)
        # TODO: conditional edge from "plan_recovery"
        #   routed by should_validate() — wrap with cond(...)
    ]

    entry = "assess_incident"
