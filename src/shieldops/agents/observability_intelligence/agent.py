# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the observability_intelligence agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.observability_intelligence.models import ObservabilityIntelligenceState
from shieldops.agents.observability_intelligence.nodes import (
    analyze_insights,
    collect_signals,
    correlate_data,
    finalize_analysis,
    generate_recommendations,
)
from shieldops.agents.observability_intelligence.tools import ObservabilityIntelligenceToolkit
from shieldops.agents.runtime import END, Agent, edge


class ObservabilityIntelligenceAgent(Agent):
    """Declarative spec for ``observability_intelligence`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "observability_intelligence"
    state_model = ObservabilityIntelligenceState
    toolkit_factory = ObservabilityIntelligenceToolkit

    nodes = {
        "collect_signals": collect_signals,
        "correlate_data": correlate_data,
        "analyze_insights": analyze_insights,
        "generate_recommendations": generate_recommendations,
        "finalize_analysis": finalize_analysis,
    }

    edges = [
        edge("correlate_data", "analyze_insights"),
        edge("analyze_insights", "generate_recommendations"),
        edge("generate_recommendations", "finalize_analysis"),
        edge("finalize_analysis", END),
        # TODO: conditional edge from "collect_signals"
        #   routed by should_continue() — wrap with cond(...)
    ]

    entry = "collect_signals"
