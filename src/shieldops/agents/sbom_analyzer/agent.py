# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the sbom_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.sbom_analyzer.models import SbomAnalyzerState
from shieldops.agents.sbom_analyzer.nodes import (
    assess_risk,
    check_licenses,
    match_cves,
    parse_sbom,
    prioritize,
    report,
)
from shieldops.agents.sbom_analyzer.tools import SbomAnalyzerToolkit


class SbomAnalyzerAgent(Agent):
    """Declarative spec for ``sbom_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "sbom_analyzer"
    state_model = SbomAnalyzerState
    toolkit_factory = SbomAnalyzerToolkit

    nodes = {
        "parse_sbom": parse_sbom,
        "match_cves": match_cves,
        "check_licenses": check_licenses,
        "assess_risk": assess_risk,
        "prioritize": prioritize,
        "report": report,
    }

    edges = [
        edge("prioritize", "report"),
        edge("report", END),
        # TODO: cond(...) from "parse_sbom" via _check_error()
        # TODO: cond(...) from "match_cves" via _check_error()
        # TODO: cond(...) from "check_licenses" via _check_error()
        # TODO: cond(...) from "assess_risk" via _check_error()
    ]

    entry = "parse_sbom"
