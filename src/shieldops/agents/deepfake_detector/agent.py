# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the deepfake_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.deepfake_detector.models import DeepfakeDetectorState
from shieldops.agents.deepfake_detector.nodes import (
    analyze_artifacts,
    check_provenance,
    classify_authenticity,
    generate_evidence,
    generate_report,
    ingest_media,
)
from shieldops.agents.deepfake_detector.tools import DeepfakeDetectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class DeepfakeDetectorAgent(Agent):
    """Declarative spec for ``deepfake_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "deepfake_detector"
    state_model = DeepfakeDetectorState
    toolkit_factory = DeepfakeDetectorToolkit

    nodes = {
        "ingest_media": ingest_media,
        "analyze_artifacts": analyze_artifacts,
        "check_provenance": check_provenance,
        "classify_authenticity": classify_authenticity,
        "generate_evidence": generate_evidence,
        "generate_report": generate_report,
    }

    edges = [
        edge("analyze_artifacts", "check_provenance"),
        edge("check_provenance", "classify_authenticity"),
        edge("generate_evidence", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "ingest_media"
        #   routed by _has_error() — wrap with cond(...)
        # TODO: conditional edge from "classify_authenticity"
        #   routed by _has_synthetic() — wrap with cond(...)
    ]

    entry = "ingest_media"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DeepfakeDetectorRunner`` keep working via this alias; new code
# should use ``DeepfakeDetectorAgent`` directly through AgentRuntime.
DeepfakeDetectorRunner = DeepfakeDetectorAgent
