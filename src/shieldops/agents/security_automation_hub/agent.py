# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_automation_hub agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_automation_hub.models import SecurityAutomationHubState
from shieldops.agents.security_automation_hub.nodes import (
    execute_automations,
    generate_report,
    ingest_triggers,
    learn_outcomes,
    match_playbooks,
    validate_results,
)
from shieldops.agents.security_automation_hub.tools import SecurityAutomationHubToolkit


class SecurityAutomationHubAgent(Agent):
    """Declarative spec for ``security_automation_hub`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_automation_hub"
    state_model = SecurityAutomationHubState
    toolkit_factory = SecurityAutomationHubToolkit

    nodes = {
        "ingest_triggers": ingest_triggers,
        "match_playbooks": match_playbooks,
        "execute_automations": execute_automations,
        "validate_results": validate_results,
        "learn_outcomes": learn_outcomes,
        "generate_report": generate_report,
    }

    edges = [
        edge("learn_outcomes", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "ingest_triggers" via _check_error()
        # TODO: cond(...) from "match_playbooks" via _check_error()
        # TODO: cond(...) from "execute_automations" via _check_error()
        # TODO: cond(...) from "validate_results" via _check_error()
    ]

    entry = "ingest_triggers"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityAutomationHubRunner`` keep working via this alias; new code
# should use ``SecurityAutomationHubAgent`` directly through AgentRuntime.
SecurityAutomationHubRunner = SecurityAutomationHubAgent
