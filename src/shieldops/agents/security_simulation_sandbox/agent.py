# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_simulation_sandbox agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_simulation_sandbox.models import SecuritySimulationSandboxState
from shieldops.agents.security_simulation_sandbox.nodes import (
    analyze,
    collect_results,
    configure_scenario,
    execute_test,
    generate_report,
    provision_sandbox,
)
from shieldops.agents.security_simulation_sandbox.tools import SecuritySimulationSandboxToolkit


class SecuritySimulationSandboxAgent(Agent):
    """Declarative spec for ``security_simulation_sandbox`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_simulation_sandbox"
    state_model = SecuritySimulationSandboxState
    toolkit_factory = SecuritySimulationSandboxToolkit

    nodes = {
        "provision_sandbox": provision_sandbox,
        "configure_scenario": configure_scenario,
        "execute_test": execute_test,
        "collect_results": collect_results,
        "analyze": analyze,
        "generate_report": generate_report,
    }

    edges = [
        edge("provision_sandbox", "configure_scenario"),
        edge("configure_scenario", "execute_test"),
        edge("execute_test", "collect_results"),
        edge("analyze", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "collect_results"
        #   routed by _should_analyze() — wrap with cond(...)
    ]

    entry = "provision_sandbox"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecuritySimulationSandboxRunner`` keep working via this alias; new code
# should use ``SecuritySimulationSandboxAgent`` directly through AgentRuntime.
SecuritySimulationSandboxRunner = SecuritySimulationSandboxAgent
