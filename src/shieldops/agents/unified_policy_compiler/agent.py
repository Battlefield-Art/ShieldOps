# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the unified_policy_compiler agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.unified_policy_compiler.models import UnifiedPolicyCompilerState
from shieldops.agents.unified_policy_compiler.nodes import (
    compile_ruleset,
    generate_report,
    ingest_policies,
    parse_requirements,
    resolve_conflicts,
    validate_coverage,
)
from shieldops.agents.unified_policy_compiler.tools import UnifiedPolicyCompilerToolkit


class UnifiedPolicyCompilerAgent(Agent):
    """Declarative spec for ``unified_policy_compiler`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "unified_policy_compiler"
    state_model = UnifiedPolicyCompilerState
    toolkit_factory = UnifiedPolicyCompilerToolkit

    nodes = {
        "ingest_policies": ingest_policies,
        "parse_requirements": parse_requirements,
        "resolve_conflicts": resolve_conflicts,
        "compile_ruleset": compile_ruleset,
        "validate_coverage": validate_coverage,
        "generate_report": generate_report,
    }

    edges = [
        edge("ingest_policies", "parse_requirements"),
        edge("resolve_conflicts", "compile_ruleset"),
        edge("validate_coverage", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "parse_requirements" via _should_resolve()
        # TODO: cond(...) from "compile_ruleset" via _should_validate()
    ]

    entry = "ingest_policies"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``UnifiedPolicyCompilerRunner`` keep working via this alias; new code
# should use ``UnifiedPolicyCompilerAgent`` directly through AgentRuntime.
UnifiedPolicyCompilerRunner = UnifiedPolicyCompilerAgent
