# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ioc_lifecycle agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ioc_lifecycle.models import IOCLifecycleState
from shieldops.agents.ioc_lifecycle.nodes import (
    age_check,
    classify,
    collect,
    enrich,
    report,
    validate,
)
from shieldops.agents.ioc_lifecycle.tools import IOCLifecycleToolkit
from shieldops.agents.runtime import END, Agent, edge


class IocLifecycleAgent(Agent):
    """Declarative spec for ``ioc_lifecycle`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ioc_lifecycle"
    state_model = IOCLifecycleState
    toolkit_factory = IOCLifecycleToolkit

    nodes = {
        "collect": collect,
        "validate": validate,
        "enrich": enrich,
        "classify": classify,
        "age_check": age_check,
        "report": report,
    }

    edges = [
        edge("report", END),
        # TODO: cond(...) from "collect" via _should_continue()
        # TODO: cond(...) from "validate" via _should_continue()
        # TODO: cond(...) from "enrich" via _should_continue()
        # TODO: cond(...) from "classify" via _should_continue()
        # TODO: cond(...) from "age_check" via _should_continue()
    ]

    entry = "collect"
