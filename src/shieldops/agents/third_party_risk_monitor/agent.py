# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the third_party_risk_monitor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.third_party_risk_monitor.models import ThirdPartyRiskMonitorState
from shieldops.agents.third_party_risk_monitor.nodes import (
    assess_posture,
    evaluate_risk,
    generate_alerts,
    generate_report,
    inventory_vendors,
    monitor_changes,
)
from shieldops.agents.third_party_risk_monitor.tools import ThirdPartyRiskMonitorToolkit


class ThirdPartyRiskMonitorAgent(Agent):
    """Declarative spec for ``third_party_risk_monitor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "third_party_risk_monitor"
    state_model = ThirdPartyRiskMonitorState
    toolkit_factory = ThirdPartyRiskMonitorToolkit

    nodes = {
        "inventory_vendors": inventory_vendors,
        "assess_posture": assess_posture,
        "monitor_changes": monitor_changes,
        "evaluate_risk": evaluate_risk,
        "generate_alerts": generate_alerts,
        "generate_report": generate_report,
    }

    edges = [
        edge("inventory_vendors", "assess_posture"),
        edge("assess_posture", "monitor_changes"),
        edge("monitor_changes", "evaluate_risk"),
        edge("generate_alerts", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "evaluate_risk"
        #   routed by _should_alert() — wrap with cond(...)
    ]

    entry = "inventory_vendors"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ThirdPartyRiskMonitorRunner`` keep working via this alias; new code
# should use ``ThirdPartyRiskMonitorAgent`` directly through AgentRuntime.
ThirdPartyRiskMonitorRunner = ThirdPartyRiskMonitorAgent
