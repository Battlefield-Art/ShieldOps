# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the purple_team_orchestrator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.purple_team_orchestrator.models import PurpleTeamOrchestratorState
from shieldops.agents.purple_team_orchestrator.nodes import (
    assess_responses,
    execute_attacks,
    monitor_detections,
    plan_exercise,
    report,
    score_exercise,
)
from shieldops.agents.purple_team_orchestrator.tools import PurpleTeamOrchestratorToolkit
from shieldops.agents.runtime import END, Agent, edge


class PurpleTeamOrchestratorAgent(Agent):
    """Declarative spec for ``purple_team_orchestrator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "purple_team_orchestrator"
    state_model = PurpleTeamOrchestratorState
    toolkit_factory = PurpleTeamOrchestratorToolkit

    nodes = {
        "plan_exercise": plan_exercise,
        "execute_attacks": execute_attacks,
        "monitor_detections": monitor_detections,
        "assess_responses": assess_responses,
        "score_exercise": score_exercise,
        "report": report,
    }

    edges = [
        edge("plan_exercise", "execute_attacks"),
        edge("execute_attacks", "monitor_detections"),
        edge("monitor_detections", "assess_responses"),
        edge("assess_responses", "score_exercise"),
        edge("score_exercise", "report"),
        edge("report", END),
    ]

    entry = "plan_exercise"
