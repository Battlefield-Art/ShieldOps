# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_entitlement_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_entitlement_manager.models import CloudEntitlementManagerState
from shieldops.agents.cloud_entitlement_manager.nodes import (
    analyze_permissions,
    assess_risk,
    detect_excess,
    discover_identities,
    generate_report,
    recommend_least_privilege,
)
from shieldops.agents.cloud_entitlement_manager.tools import CloudEntitlementManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudEntitlementManagerAgent(Agent):
    """Declarative spec for ``cloud_entitlement_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_entitlement_manager"
    state_model = CloudEntitlementManagerState
    toolkit_factory = CloudEntitlementManagerToolkit

    nodes = {
        "discover_identities": discover_identities,
        "analyze_permissions": analyze_permissions,
        "detect_excess": detect_excess,
        "assess_risk": assess_risk,
        "recommend_least_privilege": recommend_least_privilege,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_identities", "analyze_permissions"),
        edge("analyze_permissions", "detect_excess"),
        edge("detect_excess", "assess_risk"),
        edge("recommend_least_privilege", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_risk"
        #   routed by _should_recommend() — wrap with cond(...)
    ]

    entry = "discover_identities"
