# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the multi_cloud_orchestrator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.multi_cloud_orchestrator.models import MultiCloudOrchestratorState
from shieldops.agents.multi_cloud_orchestrator.nodes import (
    compare_pricing,
    discover_resources,
    execute_migration,
    normalize_inventory,
    optimize_placement,
    report,
)
from shieldops.agents.multi_cloud_orchestrator.tools import MultiCloudOrchestratorToolkit
from shieldops.agents.runtime import END, Agent, edge


class MultiCloudOrchestratorAgent(Agent):
    """Declarative spec for ``multi_cloud_orchestrator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "multi_cloud_orchestrator"
    state_model = MultiCloudOrchestratorState
    toolkit_factory = MultiCloudOrchestratorToolkit

    nodes = {
        "discover_resources": discover_resources,
        "normalize_inventory": normalize_inventory,
        "compare_pricing": compare_pricing,
        "optimize_placement": optimize_placement,
        "execute_migration": execute_migration,
        "report": report,
    }

    edges = [
        edge("execute_migration", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_resources" via _check_error()
        # TODO: cond(...) from "normalize_inventory" via _check_error()
        # TODO: cond(...) from "compare_pricing" via _check_error()
        # TODO: cond(...) from "optimize_placement" via _check_error()
    ]

    entry = "discover_resources"
