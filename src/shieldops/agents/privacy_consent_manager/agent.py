# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the privacy_consent_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.privacy_consent_manager.models import PrivacyConsentManagerState
from shieldops.agents.privacy_consent_manager.nodes import (
    audit_compliance,
    check_expiry,
    discover_consents,
    enforce_preferences,
    report,
    validate_records,
)
from shieldops.agents.privacy_consent_manager.tools import PrivacyConsentManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class PrivacyConsentManagerAgent(Agent):
    """Declarative spec for ``privacy_consent_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "privacy_consent_manager"
    state_model = PrivacyConsentManagerState
    toolkit_factory = PrivacyConsentManagerToolkit

    nodes = {
        "discover_consents": discover_consents,
        "validate_records": validate_records,
        "check_expiry": check_expiry,
        "enforce_preferences": enforce_preferences,
        "audit_compliance": audit_compliance,
        "report": report,
    }

    edges = [
        edge("discover_consents", "validate_records"),
        edge("validate_records", "check_expiry"),
        edge("check_expiry", "enforce_preferences"),
        edge("enforce_preferences", "audit_compliance"),
        edge("audit_compliance", "report"),
        edge("report", END),
    ]

    entry = "discover_consents"
