# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_governance_enforcer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_governance_enforcer.models import CloudGovernanceEnforcerState
from shieldops.agents.cloud_governance_enforcer.nodes import (
    check_tags,
    detect_violations,
    evaluate_policies,
    generate_report,
    remediate,
    scan_resources,
)
from shieldops.agents.cloud_governance_enforcer.tools import CloudGovernanceEnforcerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudGovernanceEnforcerAgent(Agent):
    """Declarative spec for ``cloud_governance_enforcer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_governance_enforcer"
    state_model = CloudGovernanceEnforcerState
    toolkit_factory = CloudGovernanceEnforcerToolkit

    nodes = {
        "scan_resources": scan_resources,
        "check_tags": check_tags,
        "evaluate_policies": evaluate_policies,
        "detect_violations": detect_violations,
        "remediate": remediate,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_resources", "check_tags"),
        edge("check_tags", "evaluate_policies"),
        edge("evaluate_policies", "detect_violations"),
        edge("remediate", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_violations"
        #   routed by _should_remediate() — wrap with cond(...)
    ]

    entry = "scan_resources"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudGovernanceEnforcerRunner`` keep working via this alias; new code
# should use ``CloudGovernanceEnforcerAgent`` directly through AgentRuntime.
CloudGovernanceEnforcerRunner = CloudGovernanceEnforcerAgent
