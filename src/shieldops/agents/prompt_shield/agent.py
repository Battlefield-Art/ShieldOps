# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the prompt_shield agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.prompt_shield.models import PromptShieldState
from shieldops.agents.prompt_shield.nodes import (
    analyze_jailbreaks,
    classify_threats,
    detect_injections,
    enforce_policies,
    generate_report,
    ingest_prompts,
)
from shieldops.agents.prompt_shield.tools import PromptShieldToolkit
from shieldops.agents.runtime import END, Agent, edge


class PromptShieldAgent(Agent):
    """Declarative spec for ``prompt_shield`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "prompt_shield"
    state_model = PromptShieldState
    toolkit_factory = PromptShieldToolkit

    nodes = {
        "ingest_prompts": ingest_prompts,
        "classify_threats": classify_threats,
        "detect_injections": detect_injections,
        "analyze_jailbreaks": analyze_jailbreaks,
        "enforce_policies": enforce_policies,
        "generate_report": generate_report,
    }

    edges = [
        edge("ingest_prompts", "classify_threats"),
        edge("classify_threats", "detect_injections"),
        edge("detect_injections", "analyze_jailbreaks"),
        edge("enforce_policies", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "analyze_jailbreaks"
        #   routed by should_enforce() — wrap with cond(...)
    ]

    entry = "ingest_prompts"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``PromptShieldRunner`` keep working via this alias; new code
# should use ``PromptShieldAgent`` directly through AgentRuntime.
PromptShieldRunner = PromptShieldAgent
