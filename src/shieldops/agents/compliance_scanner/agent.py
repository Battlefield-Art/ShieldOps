# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the compliance_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.compliance_scanner.models import ComplianceScannerState
from shieldops.agents.compliance_scanner.nodes import (
    evaluate_findings,
    generate_evidence,
    generate_report,
    scan_controls,
    select_frameworks,
    track_remediation,
)
from shieldops.agents.compliance_scanner.tools import ComplianceScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class ComplianceScannerAgent(Agent):
    """Declarative spec for ``compliance_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "compliance_scanner"
    state_model = ComplianceScannerState
    toolkit_factory = ComplianceScannerToolkit

    nodes = {
        "select_frameworks": select_frameworks,
        "scan_controls": scan_controls,
        "evaluate_findings": evaluate_findings,
        "track_remediation": track_remediation,
        "generate_evidence": generate_evidence,
        "report": generate_report,
    }

    edges = [
        edge("select_frameworks", "scan_controls"),
        edge("scan_controls", "evaluate_findings"),
        edge("evaluate_findings", "track_remediation"),
        edge("track_remediation", "generate_evidence"),
        edge("generate_evidence", "report"),
        edge("report", END),
    ]

    entry = "select_frameworks"
