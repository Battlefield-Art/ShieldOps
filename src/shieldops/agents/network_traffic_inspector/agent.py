# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the network_traffic_inspector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.network_traffic_inspector.models import NetworkTrafficInspectorState
from shieldops.agents.network_traffic_inspector.nodes import (
    analyze_protocols,
    capture_traffic,
    classify_threats,
    detect_anomalies,
    generate_alerts,
    generate_report,
)
from shieldops.agents.network_traffic_inspector.tools import NetworkTrafficInspectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class NetworkTrafficInspectorAgent(Agent):
    """Declarative spec for ``network_traffic_inspector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "network_traffic_inspector"
    state_model = NetworkTrafficInspectorState
    toolkit_factory = NetworkTrafficInspectorToolkit

    nodes = {
        "capture_traffic": capture_traffic,
        "analyze_protocols": analyze_protocols,
        "detect_anomalies": detect_anomalies,
        "classify_threats": classify_threats,
        "generate_alerts": generate_alerts,
        "generate_report": generate_report,
    }

    edges = [
        edge("analyze_protocols", "detect_anomalies"),
        edge("detect_anomalies", "classify_threats"),
        edge("generate_alerts", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "capture_traffic"
        #   routed by _should_analyze() — wrap with cond(...)
        # TODO: conditional edge from "classify_threats"
        #   routed by _should_alert() — wrap with cond(...)
    ]

    entry = "capture_traffic"
