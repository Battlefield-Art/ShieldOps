# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the rate_limit_enforcer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.rate_limit_enforcer.models import RateLimitEnforcerState
from shieldops.agents.rate_limit_enforcer.nodes import (
    apply_limits,
    classify_patterns,
    detect_anomalies,
    monitor_traffic,
    notify_stakeholders,
    report,
)
from shieldops.agents.rate_limit_enforcer.tools import RateLimitEnforcerToolkit
from shieldops.agents.runtime import END, Agent, edge


class RateLimitEnforcerAgent(Agent):
    """Declarative spec for ``rate_limit_enforcer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "rate_limit_enforcer"
    state_model = RateLimitEnforcerState
    toolkit_factory = RateLimitEnforcerToolkit

    nodes = {
        "monitor_traffic": monitor_traffic,
        "detect_anomalies": detect_anomalies,
        "classify_patterns": classify_patterns,
        "apply_limits": apply_limits,
        "notify_stakeholders": notify_stakeholders,
        "report": report,
    }

    edges = [
        edge("notify_stakeholders", "report"),
        edge("report", END),
        # TODO: cond(...) from "monitor_traffic" via _check_error()
        # TODO: cond(...) from "detect_anomalies" via _check_error()
        # TODO: cond(...) from "classify_patterns" via _check_error()
        # TODO: cond(...) from "apply_limits" via _check_error()
    ]

    entry = "monitor_traffic"
