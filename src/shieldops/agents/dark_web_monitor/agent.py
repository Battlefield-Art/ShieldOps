# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the dark_web_monitor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.dark_web_monitor.models import DarkWebMonitorState
from shieldops.agents.dark_web_monitor.nodes import (
    alert,
    assess_risk,
    crawl_sources,
    extract_mentions,
    match_assets,
    report,
)
from shieldops.agents.dark_web_monitor.tools import DarkWebMonitorToolkit
from shieldops.agents.runtime import END, Agent, edge


class DarkWebMonitorAgent(Agent):
    """Declarative spec for ``dark_web_monitor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "dark_web_monitor"
    state_model = DarkWebMonitorState
    toolkit_factory = DarkWebMonitorToolkit

    nodes = {
        "crawl_sources": crawl_sources,
        "extract_mentions": extract_mentions,
        "match_assets": match_assets,
        "assess_risk": assess_risk,
        "alert": alert,
        "report": report,
    }

    edges = [
        edge("alert", "report"),
        edge("report", END),
        # TODO: cond(...) from "crawl_sources" via _check_error()
        # TODO: cond(...) from "extract_mentions" via _check_error()
        # TODO: cond(...) from "match_assets" via _check_error()
        # TODO: cond(...) from "assess_risk" via _check_error()
    ]

    entry = "crawl_sources"
