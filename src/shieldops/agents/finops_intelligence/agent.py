# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the finops_intelligence agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.finops_intelligence.models import FinOpsIntelligenceState
from shieldops.agents.finops_intelligence.nodes import (
    analyze_costs,
    finalize_analysis,
    identify_optimizations,
    plan_implementation,
    prioritize_savings,
)
from shieldops.agents.finops_intelligence.tools import FinOpsIntelligenceToolkit
from shieldops.agents.runtime import END, Agent, edge


class FinopsIntelligenceAgent(Agent):
    """Declarative spec for ``finops_intelligence`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "finops_intelligence"
    state_model = FinOpsIntelligenceState
    toolkit_factory = FinOpsIntelligenceToolkit

    nodes = {
        "analyze_costs": analyze_costs,
        "identify_optimizations": identify_optimizations,
        "prioritize_savings": prioritize_savings,
        "plan_implementation": plan_implementation,
        "finalize_analysis": finalize_analysis,
    }

    edges = [
        edge("identify_optimizations", "prioritize_savings"),
        edge("plan_implementation", "finalize_analysis"),
        edge("finalize_analysis", END),
        # TODO: conditional edge from "analyze_costs"
        #   routed by should_optimize() — wrap with cond(...)
        # TODO: conditional edge from "prioritize_savings"
        #   routed by should_plan() — wrap with cond(...)
    ]

    entry = "analyze_costs"
