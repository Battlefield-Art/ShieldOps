# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the serverless_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.serverless_security.models import ServerlessSecurityState
from shieldops.agents.serverless_security.nodes import (
    analyze_permissions,
    assess_risk,
    detect_threats,
    discover_functions,
    scan_dependencies,
)
from shieldops.agents.serverless_security.tools import ServerlessSecurityToolkit


class ServerlessSecurityAgent(Agent):
    """Declarative spec for ``serverless_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "serverless_security"
    state_model = ServerlessSecurityState
    toolkit_factory = ServerlessSecurityToolkit

    nodes = {
        "discover_functions": discover_functions,
        "analyze_permissions": analyze_permissions,
        "scan_dependencies": scan_dependencies,
        "detect_threats": detect_threats,
        "assess_risk": assess_risk,
    }

    edges = [
        edge("analyze_permissions", "scan_dependencies"),
        edge("scan_dependencies", "detect_threats"),
        edge("detect_threats", "assess_risk"),
        edge("assess_risk", END),
        # TODO: conditional edge from "discover_functions"
        #   routed by _has_error() — wrap with cond(...)
    ]

    entry = "discover_functions"
