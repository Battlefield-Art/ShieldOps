# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the kubernetes_policy_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.kubernetes_policy_engine.models import KubernetesPolicyEngineState
from shieldops.agents.kubernetes_policy_engine.nodes import (
    check_standards,
    detect_violations,
    enforce_policies,
    evaluate_policies,
    generate_report,
    scan_resources,
)
from shieldops.agents.kubernetes_policy_engine.tools import KubernetesPolicyEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class KubernetesPolicyEngineAgent(Agent):
    """Declarative spec for ``kubernetes_policy_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "kubernetes_policy_engine"
    state_model = KubernetesPolicyEngineState
    toolkit_factory = KubernetesPolicyEngineToolkit

    nodes = {
        "scan_resources": scan_resources,
        "evaluate_policies": evaluate_policies,
        "check_standards": check_standards,
        "detect_violations": detect_violations,
        "enforce_policies": enforce_policies,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_resources", "evaluate_policies"),
        edge("evaluate_policies", "check_standards"),
        edge("check_standards", "detect_violations"),
        edge("enforce_policies", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_violations"
        #   routed by _should_enforce() — wrap with cond(...)
    ]

    entry = "scan_resources"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``KubernetesPolicyEngineRunner`` keep working via this alias; new code
# should use ``KubernetesPolicyEngineAgent`` directly through AgentRuntime.
KubernetesPolicyEngineRunner = KubernetesPolicyEngineAgent
