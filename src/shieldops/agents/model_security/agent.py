# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the model_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.model_security.models import ModelSecurityState
from shieldops.agents.model_security.nodes import (
    assess_integrity,
    detect_backdoors,
    evaluate_risks,
    scan_models,
    verify_provenance,
)
from shieldops.agents.model_security.tools import ModelSecurityToolkit
from shieldops.agents.runtime import END, Agent, edge


class ModelSecurityAgent(Agent):
    """Declarative spec for ``model_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "model_security"
    state_model = ModelSecurityState
    toolkit_factory = ModelSecurityToolkit

    nodes = {
        "scan_models": scan_models,
        "verify_provenance": verify_provenance,
        "detect_backdoors": detect_backdoors,
        "assess_integrity": assess_integrity,
        "evaluate_risks": evaluate_risks,
    }

    edges = [
        edge("scan_models", "verify_provenance"),
        edge("verify_provenance", "detect_backdoors"),
        edge("detect_backdoors", "assess_integrity"),
        edge("assess_integrity", "evaluate_risks"),
        edge("evaluate_risks", END),
    ]

    entry = "scan_models"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ModelSecurityRunner`` keep working via this alias; new code
# should use ``ModelSecurityAgent`` directly through AgentRuntime.
ModelSecurityRunner = ModelSecurityAgent
