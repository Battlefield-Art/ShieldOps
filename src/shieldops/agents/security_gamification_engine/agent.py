# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_gamification_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_gamification_engine.models import SecurityGamificationEngineState
from shieldops.agents.security_gamification_engine.nodes import (
    award_badges,
    define_challenges,
    generate_report,
    score_performance,
    track_participation,
    update_leaderboard,
)
from shieldops.agents.security_gamification_engine.tools import SecurityGamificationEngineToolkit


class SecurityGamificationEngineAgent(Agent):
    """Declarative spec for ``security_gamification_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_gamification_engine"
    state_model = SecurityGamificationEngineState
    toolkit_factory = SecurityGamificationEngineToolkit

    nodes = {
        "define_challenges": define_challenges,
        "track_participation": track_participation,
        "score_performance": score_performance,
        "update_leaderboard": update_leaderboard,
        "award_badges": award_badges,
        "generate_report": generate_report,
    }

    edges = [
        edge("define_challenges", "track_participation"),
        edge("track_participation", "score_performance"),
        edge("score_performance", "update_leaderboard"),
        edge("award_badges", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "update_leaderboard"
        #   routed by _should_award() — wrap with cond(...)
    ]

    entry = "define_challenges"
