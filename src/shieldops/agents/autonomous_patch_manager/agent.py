# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the autonomous_patch_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.autonomous_patch_manager.models import AutonomousPatchManagerState
from shieldops.agents.autonomous_patch_manager.nodes import (
    assess_patches,
    execute_patches,
    generate_report,
    scan_inventory,
    schedule_deployment,
    validate_results,
)
from shieldops.agents.autonomous_patch_manager.tools import AutonomousPatchManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AutonomousPatchManagerAgent(Agent):
    """Declarative spec for ``autonomous_patch_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "autonomous_patch_manager"
    state_model = AutonomousPatchManagerState
    toolkit_factory = AutonomousPatchManagerToolkit

    nodes = {
        "scan_inventory": scan_inventory,
        "assess_patches": assess_patches,
        "schedule_deployment": schedule_deployment,
        "execute_patches": execute_patches,
        "validate_results": validate_results,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_inventory", "assess_patches"),
        edge("schedule_deployment", "execute_patches"),
        edge("validate_results", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "assess_patches" via _should_schedule()
        # TODO: cond(...) from "execute_patches" via _should_validate()
    ]

    entry = "scan_inventory"
