# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the api_abuse_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.api_abuse_detector.models import ApiAbuseDetectorState
from shieldops.agents.api_abuse_detector.nodes import (
    analyze_patterns,
    apply_mitigation,
    classify_threat,
    collect_traffic,
    detect_abuse,
    generate_report,
)
from shieldops.agents.api_abuse_detector.tools import ApiAbuseDetectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class ApiAbuseDetectorAgent(Agent):
    """Declarative spec for ``api_abuse_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "api_abuse_detector"
    state_model = ApiAbuseDetectorState
    toolkit_factory = ApiAbuseDetectorToolkit

    nodes = {
        "collect_traffic": collect_traffic,
        "analyze_patterns": analyze_patterns,
        "detect_abuse": detect_abuse,
        "classify_threat": classify_threat,
        "apply_mitigation": apply_mitigation,
        "generate_report": generate_report,
    }

    edges = [
        edge("analyze_patterns", "detect_abuse"),
        edge("detect_abuse", "classify_threat"),
        edge("apply_mitigation", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "collect_traffic"
        #   routed by _should_analyze() — wrap with cond(...)
        # TODO: conditional edge from "classify_threat"
        #   routed by _should_mitigate() — wrap with cond(...)
    ]

    entry = "collect_traffic"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ApiAbuseDetectorRunner`` keep working via this alias; new code
# should use ``ApiAbuseDetectorAgent`` directly through AgentRuntime.
ApiAbuseDetectorRunner = ApiAbuseDetectorAgent
