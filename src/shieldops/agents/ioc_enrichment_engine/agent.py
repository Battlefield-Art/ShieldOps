# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ioc_enrichment_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ioc_enrichment_engine.models import IOCEnrichmentEngineState
from shieldops.agents.ioc_enrichment_engine.nodes import (
    assess_risk,
    collect_iocs,
    correlate_context,
    query_sources,
    report,
    tag_indicators,
)
from shieldops.agents.ioc_enrichment_engine.tools import IOCEnrichmentEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class IocEnrichmentEngineAgent(Agent):
    """Declarative spec for ``ioc_enrichment_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ioc_enrichment_engine"
    state_model = IOCEnrichmentEngineState
    toolkit_factory = IOCEnrichmentEngineToolkit

    nodes = {
        "collect_iocs": collect_iocs,
        "query_sources": query_sources,
        "correlate_context": correlate_context,
        "assess_risk": assess_risk,
        "tag_indicators": tag_indicators,
        "report": report,
    }

    edges = [
        edge("tag_indicators", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_iocs" via _check_error()
        # TODO: cond(...) from "query_sources" via _check_error()
        # TODO: cond(...) from "correlate_context" via _check_error()
        # TODO: cond(...) from "assess_risk" via _check_error()
    ]

    entry = "collect_iocs"
