# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_awareness_trainer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_awareness_trainer.models import SecurityAwarenessTrainerState
from shieldops.agents.security_awareness_trainer.nodes import (
    assess_baseline,
    deliver_training,
    design_campaign,
    generate_content,
    measure_effectiveness,
    report,
)
from shieldops.agents.security_awareness_trainer.tools import SecurityAwarenessTrainerToolkit


class SecurityAwarenessTrainerAgent(Agent):
    """Declarative spec for ``security_awareness_trainer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_awareness_trainer"
    state_model = SecurityAwarenessTrainerState
    toolkit_factory = SecurityAwarenessTrainerToolkit

    nodes = {
        "assess_baseline": assess_baseline,
        "design_campaign": design_campaign,
        "generate_content": generate_content,
        "deliver_training": deliver_training,
        "measure_effectiveness": measure_effectiveness,
        "report": report,
    }

    edges = [
        edge("measure_effectiveness", "report"),
        edge("report", END),
        # TODO: cond(...) from "assess_baseline" via _check_error()
        # TODO: cond(...) from "design_campaign" via _check_error()
        # TODO: cond(...) from "generate_content" via _check_error()
        # TODO: cond(...) from "deliver_training" via _check_error()
    ]

    entry = "assess_baseline"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityAwarenessTrainerRunner`` keep working via this alias; new code
# should use ``SecurityAwarenessTrainerAgent`` directly through AgentRuntime.
SecurityAwarenessTrainerRunner = SecurityAwarenessTrainerAgent
