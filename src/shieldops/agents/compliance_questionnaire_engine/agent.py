# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the compliance_questionnaire_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.compliance_questionnaire_engine.models import (
    ComplianceQuestionnaireEngineState,
)
from shieldops.agents.compliance_questionnaire_engine.nodes import (
    finalize_response,
    generate_answers,
    generate_report,
    map_controls,
    receive_questionnaire,
    review_gaps,
)
from shieldops.agents.compliance_questionnaire_engine.tools import (
    ComplianceQuestionnaireEngineToolkit,
)
from shieldops.agents.runtime import END, Agent, edge


class ComplianceQuestionnaireEngineAgent(Agent):
    """Declarative spec for ``compliance_questionnaire_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "compliance_questionnaire_engine"
    state_model = ComplianceQuestionnaireEngineState
    toolkit_factory = ComplianceQuestionnaireEngineToolkit

    nodes = {
        "receive_questionnaire": receive_questionnaire,
        "map_controls": map_controls,
        "generate_answers": generate_answers,
        "review_gaps": review_gaps,
        "finalize_response": finalize_response,
        "generate_report": generate_report,
    }

    edges = [
        edge("receive_questionnaire", "map_controls"),
        edge("map_controls", "generate_answers"),
        edge("generate_answers", "review_gaps"),
        edge("finalize_response", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "review_gaps"
        #   routed by _should_finalize() — wrap with cond(...)
    ]

    entry = "receive_questionnaire"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ComplianceQuestionnaireEngineRunner`` keep working via this alias; new code
# should use ``ComplianceQuestionnaireEngineAgent`` directly through AgentRuntime.
ComplianceQuestionnaireEngineRunner = ComplianceQuestionnaireEngineAgent
