# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the sla_breach_predictor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.sla_breach_predictor.models import SlaBreachPredictorState
from shieldops.agents.sla_breach_predictor.nodes import (
    alert,
    collect_tickets,
    compute_velocity,
    predict_breach,
    rank_risk,
    report,
)
from shieldops.agents.sla_breach_predictor.tools import SlaBreachPredictorToolkit


class SlaBreachPredictorAgent(Agent):
    """Declarative spec for ``sla_breach_predictor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "sla_breach_predictor"
    state_model = SlaBreachPredictorState
    toolkit_factory = SlaBreachPredictorToolkit

    nodes = {
        "collect_tickets": collect_tickets,
        "compute_velocity": compute_velocity,
        "predict_breach": predict_breach,
        "rank_risk": rank_risk,
        "alert": alert,
        "report": report,
    }

    edges = [
        edge("alert", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_tickets" via _check_error()
        # TODO: cond(...) from "compute_velocity" via _check_error()
        # TODO: cond(...) from "predict_breach" via _check_error()
        # TODO: cond(...) from "rank_risk" via _check_error()
    ]

    entry = "collect_tickets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SlaBreachPredictorRunner`` keep working via this alias; new code
# should use ``SlaBreachPredictorAgent`` directly through AgentRuntime.
SlaBreachPredictorRunner = SlaBreachPredictorAgent
