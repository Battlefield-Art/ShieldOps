# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the toxic_combination_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.toxic_combination_detector.models import ToxicCombinationDetectorState
from shieldops.agents.toxic_combination_detector.nodes import (
    analyze_combinations,
    assess_blast_radius,
    collect_permissions,
    detect_toxic,
    generate_report,
    recommend,
)
from shieldops.agents.toxic_combination_detector.tools import ToxicCombinationDetectorToolkit


class ToxicCombinationDetectorAgent(Agent):
    """Declarative spec for ``toxic_combination_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "toxic_combination_detector"
    state_model = ToxicCombinationDetectorState
    toolkit_factory = ToxicCombinationDetectorToolkit

    nodes = {
        "collect_permissions": collect_permissions,
        "analyze_combinations": analyze_combinations,
        "detect_toxic": detect_toxic,
        "assess_blast_radius": assess_blast_radius,
        "recommend": recommend,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_permissions", "analyze_combinations"),
        edge("analyze_combinations", "detect_toxic"),
        edge("assess_blast_radius", "recommend"),
        edge("recommend", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_toxic"
        #   routed by _should_assess() — wrap with cond(...)
    ]

    entry = "collect_permissions"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ToxicCombinationDetectorRunner`` keep working via this alias; new code
# should use ``ToxicCombinationDetectorAgent`` directly through AgentRuntime.
ToxicCombinationDetectorRunner = ToxicCombinationDetectorAgent
