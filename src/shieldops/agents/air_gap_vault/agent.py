# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the air_gap_vault agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.air_gap_vault.models import AirGapVaultState
from shieldops.agents.air_gap_vault.nodes import (
    check_integrity,
    detect_tampering,
    enforce_retention,
    generate_report,
    inventory_vault_assets,
    verify_isolation,
)
from shieldops.agents.air_gap_vault.tools import AirGapVaultToolkit
from shieldops.agents.runtime import END, Agent, edge


class AirGapVaultAgent(Agent):
    """Declarative spec for ``air_gap_vault`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "air_gap_vault"
    state_model = AirGapVaultState
    toolkit_factory = AirGapVaultToolkit

    nodes = {
        "inventory_vault_assets": inventory_vault_assets,
        "verify_isolation": verify_isolation,
        "check_integrity": check_integrity,
        "detect_tampering": detect_tampering,
        "enforce_retention": enforce_retention,
        "generate_report": generate_report,
    }

    edges = [
        edge("inventory_vault_assets", "verify_isolation"),
        edge("check_integrity", "detect_tampering"),
        edge("detect_tampering", "enforce_retention"),
        edge("enforce_retention", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "verify_isolation"
        #   routed by route_after_isolation() — wrap with cond(...)
    ]

    entry = "inventory_vault_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AirGapVaultRunner`` keep working via this alias; new code
# should use ``AirGapVaultAgent`` directly through AgentRuntime.
AirGapVaultRunner = AirGapVaultAgent
