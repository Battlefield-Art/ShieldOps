# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_catalog_protector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_catalog_protector.models import DataCatalogProtectorState
from shieldops.agents.data_catalog_protector.nodes import (
    classify_sensitivity,
    detect_violations,
    enforce_policies,
    generate_report,
    map_access,
    scan_catalogs,
)
from shieldops.agents.data_catalog_protector.tools import DataCatalogProtectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataCatalogProtectorAgent(Agent):
    """Declarative spec for ``data_catalog_protector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_catalog_protector"
    state_model = DataCatalogProtectorState
    toolkit_factory = DataCatalogProtectorToolkit

    nodes = {
        "scan_catalogs": scan_catalogs,
        "classify_sensitivity": classify_sensitivity,
        "map_access": map_access,
        "detect_violations": detect_violations,
        "enforce_policies": enforce_policies,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_catalogs", "classify_sensitivity"),
        edge("classify_sensitivity", "map_access"),
        edge("map_access", "detect_violations"),
        edge("enforce_policies", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_violations"
        #   routed by _should_enforce() — wrap with cond(...)
    ]

    entry = "scan_catalogs"
