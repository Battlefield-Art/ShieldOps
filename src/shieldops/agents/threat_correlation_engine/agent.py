# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the threat_correlation_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.threat_correlation_engine.models import ThreatCorrelationEngineState
from shieldops.agents.threat_correlation_engine.nodes import (
    collect_events,
    correlate_signals,
    generate_alerts,
    normalize_data,
    report,
    score_threats,
)
from shieldops.agents.threat_correlation_engine.tools import ThreatCorrelationEngineToolkit


class ThreatCorrelationEngineAgent(Agent):
    """Declarative spec for ``threat_correlation_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "threat_correlation_engine"
    state_model = ThreatCorrelationEngineState
    toolkit_factory = ThreatCorrelationEngineToolkit

    nodes = {
        "collect_events": collect_events,
        "normalize_data": normalize_data,
        "correlate_signals": correlate_signals,
        "score_threats": score_threats,
        "generate_alerts": generate_alerts,
        "report": report,
    }

    edges = [
        edge("generate_alerts", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_events" via _check_error()
        # TODO: cond(...) from "normalize_data" via _check_error()
        # TODO: cond(...) from "correlate_signals" via _check_error()
        # TODO: cond(...) from "score_threats" via _check_error()
    ]

    entry = "collect_events"
