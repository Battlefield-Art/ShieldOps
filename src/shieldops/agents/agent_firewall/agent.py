# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the agent_firewall agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.agent_firewall.models import AgentFirewallState
from shieldops.agents.agent_firewall.nodes import (
    build_baseline,
    detect_anomalies,
    enforce_actions,
    evaluate_policies,
    generate_alerts,
    ingest_calls,
    report,
)
from shieldops.agents.agent_firewall.tools import AgentFirewallToolkit
from shieldops.agents.runtime import END, Agent, edge


class AgentFirewallAgent(Agent):
    """Declarative spec for ``agent_firewall`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "agent_firewall"
    state_model = AgentFirewallState
    toolkit_factory = AgentFirewallToolkit

    nodes = {
        "ingest_calls": ingest_calls,
        "build_baseline": build_baseline,
        "detect_anomalies": detect_anomalies,
        "evaluate_policies": evaluate_policies,
        "enforce_actions": enforce_actions,
        "generate_alerts": generate_alerts,
        "report": report,
    }

    edges = [
        edge("ingest_calls", "build_baseline"),
        edge("build_baseline", "detect_anomalies"),
        edge("detect_anomalies", "evaluate_policies"),
        edge("enforce_actions", "generate_alerts"),
        edge("generate_alerts", "report"),
        edge("report", END),
        # TODO: conditional edge from "evaluate_policies"
        #   routed by _has_violations() — wrap with cond(...)
    ]

    entry = "ingest_calls"
