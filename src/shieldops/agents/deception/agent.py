# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the deception agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.deception.models import DeceptionState
from shieldops.agents.deception.nodes import (
    analyze_behavior,
    deploy_assets,
    extract_indicators,
    generate_report,
    monitor_interactions,
    respond_to_threat,
    update_strategy,
)
from shieldops.agents.deception.tools import DeceptionToolkit
from shieldops.agents.runtime import END, Agent, edge


class DeceptionAgent(Agent):
    """Declarative spec for ``deception`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "deception"
    state_model = DeceptionState
    toolkit_factory = DeceptionToolkit

    nodes = {
        "deploy_assets": deploy_assets,
        "monitor_interactions": monitor_interactions,
        "analyze_behavior": analyze_behavior,
        "extract_indicators": extract_indicators,
        "respond_to_threat": respond_to_threat,
        "update_strategy": update_strategy,
        "generate_report": generate_report,
    }

    edges = [
        edge("deploy_assets", "monitor_interactions"),
        edge("analyze_behavior", "extract_indicators"),
        edge("respond_to_threat", "update_strategy"),
        edge("update_strategy", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "monitor_interactions"
        #   routed by should_analyze() — wrap with cond(...)
        # TODO: conditional edge from "extract_indicators"
        #   routed by should_respond() — wrap with cond(...)
    ]

    entry = "deploy_assets"
