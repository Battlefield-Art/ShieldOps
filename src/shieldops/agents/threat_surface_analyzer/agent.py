# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the threat_surface_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.threat_surface_analyzer.models import ThreatSurfaceAnalyzerState
from shieldops.agents.threat_surface_analyzer.nodes import (
    assess_risks,
    discover_assets,
    generate_report,
    map_exposure,
    prioritize_threats,
    recommend_mitigations,
)
from shieldops.agents.threat_surface_analyzer.tools import ThreatSurfaceAnalyzerToolkit


class ThreatSurfaceAnalyzerAgent(Agent):
    """Declarative spec for ``threat_surface_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "threat_surface_analyzer"
    state_model = ThreatSurfaceAnalyzerState
    toolkit_factory = ThreatSurfaceAnalyzerToolkit

    nodes = {
        "discover_assets": discover_assets,
        "map_exposure": map_exposure,
        "assess_risks": assess_risks,
        "prioritize_threats": prioritize_threats,
        "recommend_mitigations": recommend_mitigations,
        "generate_report": generate_report,
    }

    edges = [
        edge("map_exposure", "assess_risks"),
        edge("assess_risks", "prioritize_threats"),
        edge("recommend_mitigations", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "discover_assets" via should_map_exposure()
        # TODO: cond(...) from "prioritize_threats" via should_recommend_mitigations()
    ]

    entry = "discover_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ThreatSurfaceAnalyzerRunner`` keep working via this alias; new code
# should use ``ThreatSurfaceAnalyzerAgent`` directly through AgentRuntime.
ThreatSurfaceAnalyzerRunner = ThreatSurfaceAnalyzerAgent
