# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the otel_deployer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.otel_deployer.models import OTelDeployerState
from shieldops.agents.otel_deployer.nodes import (
    deploy_collectors,
    plan_deployments,
    validate_configs,
    verify_and_report,
)
from shieldops.agents.otel_deployer.tools import OTelDeployerToolkit
from shieldops.agents.runtime import END, Agent, edge


class OtelDeployerAgent(Agent):
    """Declarative spec for ``otel_deployer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "otel_deployer"
    state_model = OTelDeployerState
    toolkit_factory = OTelDeployerToolkit

    nodes = {
        "plan": plan_deployments,
        "validate": validate_configs,
        "deploy": deploy_collectors,
        "verify": verify_and_report,
    }

    edges = [
        edge("plan", "validate"),
        edge("validate", "deploy"),
        edge("deploy", "verify"),
        edge("verify", END),
    ]

    entry = "plan"
