# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_classification agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_classification.models import DataClassificationState
from shieldops.agents.data_classification.nodes import (
    classify_level,
    detect_sensitive,
    enforce_labels,
    map_regulations,
    report,
    scan_sources,
)
from shieldops.agents.data_classification.tools import DataClassificationToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataClassificationAgent(Agent):
    """Declarative spec for ``data_classification`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_classification"
    state_model = DataClassificationState
    toolkit_factory = DataClassificationToolkit

    nodes = {
        "scan_sources": scan_sources,
        "detect_sensitive": detect_sensitive,
        "classify_level": classify_level,
        "map_regulations": map_regulations,
        "enforce_labels": enforce_labels,
        "report": report,
    }

    edges = [
        edge("scan_sources", "detect_sensitive"),
        edge("detect_sensitive", "classify_level"),
        edge("classify_level", "map_regulations"),
        edge("enforce_labels", "report"),
        edge("report", END),
        # TODO: conditional edge from "map_regulations"
        #   routed by _has_findings() — wrap with cond(...)
    ]

    entry = "scan_sources"
