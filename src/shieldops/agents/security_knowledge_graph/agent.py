# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_knowledge_graph agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_knowledge_graph.models import SecurityKnowledgeGraphState
from shieldops.agents.security_knowledge_graph.nodes import (
    build_graph,
    detect_anomalies,
    extract_relationships,
    generate_report,
    ingest_entities,
    query_patterns,
)
from shieldops.agents.security_knowledge_graph.tools import SecurityKnowledgeGraphToolkit


class SecurityKnowledgeGraphAgent(Agent):
    """Declarative spec for ``security_knowledge_graph`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_knowledge_graph"
    state_model = SecurityKnowledgeGraphState
    toolkit_factory = SecurityKnowledgeGraphToolkit

    nodes = {
        "ingest_entities": ingest_entities,
        "extract_relationships": extract_relationships,
        "build_graph": build_graph,
        "query_patterns": query_patterns,
        "detect_anomalies": detect_anomalies,
        "generate_report": generate_report,
    }

    edges = [
        edge("ingest_entities", "extract_relationships"),
        edge("extract_relationships", "build_graph"),
        edge("detect_anomalies", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "build_graph" via _should_query()
        # TODO: cond(...) from "query_patterns" via _should_detect()
    ]

    entry = "ingest_entities"
