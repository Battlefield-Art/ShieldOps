# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_testing agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_testing.models import SecurityTestingState
from shieldops.agents.security_testing.nodes import (
    analyze_findings,
    define_scope,
    execute_scans,
    generate_report,
)
from shieldops.agents.security_testing.tools import SecurityTestingToolkit


class SecurityTestingAgent(Agent):
    """Declarative spec for ``security_testing`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_testing"
    state_model = SecurityTestingState
    toolkit_factory = SecurityTestingToolkit

    nodes = {
        "define_scope": define_scope,
        "execute_scans": execute_scans,
        "analyze_findings": analyze_findings,
        "generate_report": generate_report,
    }

    edges = [
        edge("define_scope", "execute_scans"),
        edge("execute_scans", "analyze_findings"),
        edge("analyze_findings", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "define_scope"
