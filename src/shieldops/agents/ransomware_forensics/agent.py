# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ransomware_forensics agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ransomware_forensics.models import RansomwareForensicsState
from shieldops.agents.ransomware_forensics.nodes import (
    analyze_attack_chain,
    assess_blast_radius,
    collect_artifacts,
    generate_report,
    identify_variant,
    recommend_recovery,
)
from shieldops.agents.ransomware_forensics.tools import RansomwareForensicsToolkit
from shieldops.agents.runtime import END, Agent, edge


class RansomwareForensicsAgent(Agent):
    """Declarative spec for ``ransomware_forensics`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ransomware_forensics"
    state_model = RansomwareForensicsState
    toolkit_factory = RansomwareForensicsToolkit

    nodes = {
        "collect_artifacts": collect_artifacts,
        "analyze_attack_chain": analyze_attack_chain,
        "identify_variant": identify_variant,
        "assess_blast_radius": assess_blast_radius,
        "recommend_recovery": recommend_recovery,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_artifacts", "analyze_attack_chain"),
        edge("analyze_attack_chain", "identify_variant"),
        edge("identify_variant", "assess_blast_radius"),
        edge("recommend_recovery", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_blast_radius"
        #   routed by should_continue_after_blast() — wrap with cond(...)
    ]

    entry = "collect_artifacts"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``RansomwareForensicsRunner`` keep working via this alias; new code
# should use ``RansomwareForensicsAgent`` directly through AgentRuntime.
RansomwareForensicsRunner = RansomwareForensicsAgent
