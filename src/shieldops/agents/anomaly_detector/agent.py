# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the anomaly_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.anomaly_detector.models import AnomalyDetectorState
from shieldops.agents.anomaly_detector.nodes import (
    classify_anomalies,
    collect_data,
    correlate_anomalies,
    detect_anomalies,
    generate_report,
    send_alerts,
)
from shieldops.agents.anomaly_detector.tools import AnomalyDetectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class AnomalyDetectorAgent(Agent):
    """Declarative spec for ``anomaly_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "anomaly_detector"
    state_model = AnomalyDetectorState
    toolkit_factory = AnomalyDetectorToolkit

    nodes = {
        "collect_data": collect_data,
        "detect_anomalies": detect_anomalies,
        "classify": classify_anomalies,
        "correlate": correlate_anomalies,
        "alert": send_alerts,
        "report": generate_report,
    }

    edges = [
        edge("collect_data", "detect_anomalies"),
        edge("detect_anomalies", "classify"),
        edge("classify", "correlate"),
        edge("correlate", "alert"),
        edge("alert", "report"),
        edge("report", END),
    ]

    entry = "collect_data"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AnomalyDetectorRunner`` keep working via this alias; new code
# should use ``AnomalyDetectorAgent`` directly through AgentRuntime.
AnomalyDetectorRunner = AnomalyDetectorAgent
