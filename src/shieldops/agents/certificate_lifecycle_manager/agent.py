# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the certificate_lifecycle_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.certificate_lifecycle_manager.models import CertificateLifecycleManagerState
from shieldops.agents.certificate_lifecycle_manager.nodes import (
    check_expiry,
    discover_certs,
    execute_renewal,
    generate_report,
    plan_renewal,
    validate_config,
)
from shieldops.agents.certificate_lifecycle_manager.tools import CertificateLifecycleManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CertificateLifecycleManagerAgent(Agent):
    """Declarative spec for ``certificate_lifecycle_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "certificate_lifecycle_manager"
    state_model = CertificateLifecycleManagerState
    toolkit_factory = CertificateLifecycleManagerToolkit

    nodes = {
        "discover_certs": discover_certs,
        "check_expiry": check_expiry,
        "validate_config": validate_config,
        "plan_renewal": plan_renewal,
        "execute_renewal": execute_renewal,
        "report": generate_report,
    }

    edges = [
        edge("discover_certs", "check_expiry"),
        edge("check_expiry", "validate_config"),
        edge("validate_config", "plan_renewal"),
        edge("plan_renewal", "execute_renewal"),
        edge("execute_renewal", "report"),
        edge("report", END),
    ]

    entry = "discover_certs"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CertificateLifecycleManagerRunner`` keep working via this alias; new code
# should use ``CertificateLifecycleManagerAgent`` directly through AgentRuntime.
CertificateLifecycleManagerRunner = CertificateLifecycleManagerAgent
