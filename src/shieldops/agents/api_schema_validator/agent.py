# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the api_schema_validator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.api_schema_validator.models import APISchemaValidatorState
from shieldops.agents.api_schema_validator.nodes import (
    assess_impact,
    detect_breaking,
    discover_schemas,
    generate_fixes,
    generate_report,
    validate_contracts,
)
from shieldops.agents.api_schema_validator.tools import APISchemaValidatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class ApiSchemaValidatorAgent(Agent):
    """Declarative spec for ``api_schema_validator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "api_schema_validator"
    state_model = APISchemaValidatorState
    toolkit_factory = APISchemaValidatorToolkit

    nodes = {
        "discover_schemas": discover_schemas,
        "validate_contracts": validate_contracts,
        "detect_breaking": detect_breaking,
        "assess_impact": assess_impact,
        "generate_fixes": generate_fixes,
        "generate_report": generate_report,
    }

    edges = [
        edge("validate_contracts", "detect_breaking"),
        edge("detect_breaking", "assess_impact"),
        edge("generate_fixes", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "discover_schemas"
        #   routed by _should_validate() — wrap with cond(...)
        # TODO: conditional edge from "assess_impact"
        #   routed by _should_fix() — wrap with cond(...)
    ]

    entry = "discover_schemas"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``APISchemaValidatorRunner`` keep working via this alias; new code
# should use ``ApiSchemaValidatorAgent`` directly through AgentRuntime.
APISchemaValidatorRunner = ApiSchemaValidatorAgent
