# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the iac_security_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.iac_security_scanner.models import IACScannerState
from shieldops.agents.iac_security_scanner.nodes import (
    discover_templates,
    evaluate_policies,
    generate_report,
    parse_resources,
    prioritize_findings,
    scan_misconfigs,
)
from shieldops.agents.iac_security_scanner.tools import IACSecurityScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class IacSecurityScannerAgent(Agent):
    """Declarative spec for ``iac_security_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "iac_security_scanner"
    state_model = IACScannerState
    toolkit_factory = IACSecurityScannerToolkit

    nodes = {
        "discover_templates": discover_templates,
        "parse_resources": parse_resources,
        "scan_misconfigs": scan_misconfigs,
        "evaluate_policies": evaluate_policies,
        "prioritize": prioritize_findings,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_templates", "parse_resources"),
        edge("parse_resources", "scan_misconfigs"),
        edge("scan_misconfigs", "evaluate_policies"),
        edge("evaluate_policies", "prioritize"),
        edge("prioritize", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "discover_templates"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``IACSecurityScannerRunner`` keep working via this alias; new code
# should use ``IacSecurityScannerAgent`` directly through AgentRuntime.
IACSecurityScannerRunner = IacSecurityScannerAgent
