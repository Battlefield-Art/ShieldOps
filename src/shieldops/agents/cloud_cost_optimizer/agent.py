# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_cost_optimizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_cost_optimizer.models import CloudCostOptimizerState
from shieldops.agents.cloud_cost_optimizer.nodes import (
    analyze_spending,
    collect_billing,
    generate_report,
    identify_waste,
    implement_optimizations,
    recommend_savings,
)
from shieldops.agents.cloud_cost_optimizer.tools import CloudCostOptimizerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudCostOptimizerAgent(Agent):
    """Declarative spec for ``cloud_cost_optimizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_cost_optimizer"
    state_model = CloudCostOptimizerState
    toolkit_factory = CloudCostOptimizerToolkit

    nodes = {
        "collect_billing": collect_billing,
        "analyze_spending": analyze_spending,
        "identify_waste": identify_waste,
        "recommend_savings": recommend_savings,
        "implement_optimizations": implement_optimizations,
        "report": generate_report,
    }

    edges = [
        edge("collect_billing", "analyze_spending"),
        edge("analyze_spending", "identify_waste"),
        edge("identify_waste", "recommend_savings"),
        edge("recommend_savings", "implement_optimizations"),
        edge("implement_optimizations", "report"),
        edge("report", END),
    ]

    entry = "collect_billing"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudCostOptimizerRunner`` keep working via this alias; new code
# should use ``CloudCostOptimizerAgent`` directly through AgentRuntime.
CloudCostOptimizerRunner = CloudCostOptimizerAgent
