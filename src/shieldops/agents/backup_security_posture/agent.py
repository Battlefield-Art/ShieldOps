# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the backup_security_posture agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.backup_security_posture.models import BackupSecurityPostureState
from shieldops.agents.backup_security_posture.nodes import (
    assess_security_config,
    detect_vulnerabilities,
    inventory_backup_infra,
    recommend_hardening,
    report,
    test_recovery,
)
from shieldops.agents.backup_security_posture.tools import BackupSecurityPostureToolkit
from shieldops.agents.runtime import END, Agent, edge


class BackupSecurityPostureAgent(Agent):
    """Declarative spec for ``backup_security_posture`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "backup_security_posture"
    state_model = BackupSecurityPostureState
    toolkit_factory = BackupSecurityPostureToolkit

    nodes = {
        "inventory_backup_infra": inventory_backup_infra,
        "assess_security_config": assess_security_config,
        "detect_vulnerabilities": detect_vulnerabilities,
        "test_recovery": test_recovery,
        "recommend_hardening": recommend_hardening,
        "report": report,
    }

    edges = [
        edge("inventory_backup_infra", "assess_security_config"),
        edge("assess_security_config", "detect_vulnerabilities"),
        edge("detect_vulnerabilities", "test_recovery"),
        edge("test_recovery", "recommend_hardening"),
        edge("recommend_hardening", "report"),
        edge("report", END),
    ]

    entry = "inventory_backup_infra"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``BackupSecurityPostureRunner`` keep working via this alias; new code
# should use ``BackupSecurityPostureAgent`` directly through AgentRuntime.
BackupSecurityPostureRunner = BackupSecurityPostureAgent
