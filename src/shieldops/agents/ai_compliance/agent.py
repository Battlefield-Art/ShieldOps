# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ai_compliance agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ai_compliance.models import AIComplianceState
from shieldops.agents.ai_compliance.nodes import (
    assess_requirements,
    classify_risk_levels,
    collect_inventory,
    evaluate_controls,
    generate_evidence,
    generate_report,
)
from shieldops.agents.ai_compliance.tools import AIComplianceToolkit
from shieldops.agents.runtime import END, Agent, edge


class AiComplianceAgent(Agent):
    """Declarative spec for ``ai_compliance`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ai_compliance"
    state_model = AIComplianceState
    toolkit_factory = AIComplianceToolkit

    nodes = {
        "collect_inventory": collect_inventory,
        "classify_risk_levels": classify_risk_levels,
        "assess_requirements": assess_requirements,
        "evaluate_controls": evaluate_controls,
        "generate_evidence": generate_evidence,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_inventory", "classify_risk_levels"),
        edge("classify_risk_levels", "assess_requirements"),
        edge("assess_requirements", "evaluate_controls"),
        edge("evaluate_controls", "generate_evidence"),
        edge("generate_evidence", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "collect_inventory"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AIComplianceRunner`` keep working via this alias; new code
# should use ``AiComplianceAgent`` directly through AgentRuntime.
AIComplianceRunner = AiComplianceAgent
