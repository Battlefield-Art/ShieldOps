# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the it_asset_intelligence agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.it_asset_intelligence.models import ITAssetIntelligenceState
from shieldops.agents.it_asset_intelligence.nodes import (
    assess_security_posture,
    classify_criticality,
    correlate_with_threats,
    discover_assets,
    generate_risk_report,
    report,
)
from shieldops.agents.it_asset_intelligence.tools import ITAssetIntelligenceToolkit
from shieldops.agents.runtime import END, Agent, edge


class ItAssetIntelligenceAgent(Agent):
    """Declarative spec for ``it_asset_intelligence`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "it_asset_intelligence"
    state_model = ITAssetIntelligenceState
    toolkit_factory = ITAssetIntelligenceToolkit

    nodes = {
        "discover_assets": discover_assets,
        "classify_criticality": classify_criticality,
        "assess_security_posture": assess_security_posture,
        "correlate_with_threats": correlate_with_threats,
        "generate_risk_report": generate_risk_report,
        "report": report,
    }

    edges = [
        edge("discover_assets", "classify_criticality"),
        edge("classify_criticality", "assess_security_posture"),
        edge("assess_security_posture", "correlate_with_threats"),
        edge("correlate_with_threats", "generate_risk_report"),
        edge("generate_risk_report", "report"),
        edge("report", END),
    ]

    entry = "discover_assets"
