# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the observability_pipeline_optimizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.observability_pipeline_optimizer.models import (
    ObservabilityPipelineOptimizerState,
)
from shieldops.agents.observability_pipeline_optimizer.nodes import (
    analyze_cardinality,
    audit_pipelines,
    generate_report,
    optimize_sampling,
    reduce_costs,
    validate_quality,
)
from shieldops.agents.observability_pipeline_optimizer.tools import (
    ObservabilityPipelineOptimizerToolkit,
)
from shieldops.agents.runtime import END, Agent, edge


class ObservabilityPipelineOptimizerAgent(Agent):
    """Declarative spec for ``observability_pipeline_optimizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "observability_pipeline_optimizer"
    state_model = ObservabilityPipelineOptimizerState
    toolkit_factory = ObservabilityPipelineOptimizerToolkit

    nodes = {
        "audit_pipelines": audit_pipelines,
        "analyze_cardinality": analyze_cardinality,
        "optimize_sampling": optimize_sampling,
        "reduce_costs": reduce_costs,
        "validate_quality": validate_quality,
        "report": generate_report,
    }

    edges = [
        edge("audit_pipelines", "analyze_cardinality"),
        edge("analyze_cardinality", "optimize_sampling"),
        edge("optimize_sampling", "reduce_costs"),
        edge("reduce_costs", "validate_quality"),
        edge("validate_quality", "report"),
        edge("report", END),
    ]

    entry = "audit_pipelines"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ObservabilityPipelineOptimizerRunner`` keep working via this alias; new code
# should use ``ObservabilityPipelineOptimizerAgent`` directly through AgentRuntime.
ObservabilityPipelineOptimizerRunner = ObservabilityPipelineOptimizerAgent
