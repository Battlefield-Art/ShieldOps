# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_forensics_collector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_forensics_collector.models import CloudForensicsCollectorState
from shieldops.agents.cloud_forensics_collector.nodes import (
    analyze_evidence,
    capture_snapshots,
    collect_logs,
    generate_report,
    identify_scope,
    preserve_evidence,
)
from shieldops.agents.cloud_forensics_collector.tools import CloudForensicsCollectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudForensicsCollectorAgent(Agent):
    """Declarative spec for ``cloud_forensics_collector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_forensics_collector"
    state_model = CloudForensicsCollectorState
    toolkit_factory = CloudForensicsCollectorToolkit

    nodes = {
        "identify_scope": identify_scope,
        "collect_logs": collect_logs,
        "capture_snapshots": capture_snapshots,
        "preserve_evidence": preserve_evidence,
        "analyze_evidence": analyze_evidence,
        "generate_report": generate_report,
    }

    edges = [
        edge("identify_scope", "collect_logs"),
        edge("collect_logs", "capture_snapshots"),
        edge("capture_snapshots", "preserve_evidence"),
        edge("analyze_evidence", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "preserve_evidence"
        #   routed by _should_analyze() — wrap with cond(...)
    ]

    entry = "identify_scope"
