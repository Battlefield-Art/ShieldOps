# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the compliance_auditor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.compliance_auditor.models import ComplianceAuditorState
from shieldops.agents.compliance_auditor.nodes import (
    analyze_cloudtrail,
    analyze_gaps,
    collect_evidence,
    generate_report,
    scan_infrastructure,
)
from shieldops.agents.compliance_auditor.tools import ComplianceAuditorToolkit
from shieldops.agents.runtime import END, Agent, edge


class ComplianceAuditorAgent(Agent):
    """Declarative spec for ``compliance_auditor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "compliance_auditor"
    state_model = ComplianceAuditorState
    toolkit_factory = ComplianceAuditorToolkit

    nodes = {
        "scan": scan_infrastructure,
        "analyze_cloudtrail": analyze_cloudtrail,
        "collect_evidence": collect_evidence,
        "analyze_gaps": analyze_gaps,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan", "analyze_cloudtrail"),
        edge("analyze_cloudtrail", "collect_evidence"),
        edge("collect_evidence", "analyze_gaps"),
        edge("analyze_gaps", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "scan"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ComplianceAuditorRunner`` keep working via this alias; new code
# should use ``ComplianceAuditorAgent`` directly through AgentRuntime.
ComplianceAuditorRunner = ComplianceAuditorAgent
