# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_loss_prevention agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_loss_prevention.models import DataLossPreventionState
from shieldops.agents.data_loss_prevention.nodes import (
    classify_sensitive_data,
    detect_exfiltration,
    discover_data_flows,
    enforce_policies,
    report,
    respond_to_incidents,
)
from shieldops.agents.data_loss_prevention.tools import DataLossPreventionToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataLossPreventionAgent(Agent):
    """Declarative spec for ``data_loss_prevention`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_loss_prevention"
    state_model = DataLossPreventionState
    toolkit_factory = DataLossPreventionToolkit

    nodes = {
        "discover_data_flows": discover_data_flows,
        "classify_sensitive_data": classify_sensitive_data,
        "detect_exfiltration": detect_exfiltration,
        "enforce_policies": enforce_policies,
        "respond_to_incidents": respond_to_incidents,
        "report": report,
    }

    edges = [
        edge("discover_data_flows", "classify_sensitive_data"),
        edge("classify_sensitive_data", "detect_exfiltration"),
        edge("enforce_policies", "respond_to_incidents"),
        edge("respond_to_incidents", "report"),
        edge("report", END),
        # TODO: conditional edge from "detect_exfiltration"
        #   routed by _has_exfiltration() — wrap with cond(...)
    ]

    entry = "discover_data_flows"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DataLossPreventionRunner`` keep working via this alias; new code
# should use ``DataLossPreventionAgent`` directly through AgentRuntime.
DataLossPreventionRunner = DataLossPreventionAgent
