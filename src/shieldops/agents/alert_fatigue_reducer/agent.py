# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the alert_fatigue_reducer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.alert_fatigue_reducer.models import AlertFatigueReducerState
from shieldops.agents.alert_fatigue_reducer.nodes import (
    analyze_noise,
    collect_alerts,
    detect_fatigue,
    generate_report,
    tune_rules,
    validate_changes,
)
from shieldops.agents.alert_fatigue_reducer.tools import AlertFatigueReducerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AlertFatigueReducerAgent(Agent):
    """Declarative spec for ``alert_fatigue_reducer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "alert_fatigue_reducer"
    state_model = AlertFatigueReducerState
    toolkit_factory = AlertFatigueReducerToolkit

    nodes = {
        "collect_alerts": collect_alerts,
        "analyze_noise": analyze_noise,
        "detect_fatigue": detect_fatigue,
        "tune_rules": tune_rules,
        "validate": validate_changes,
        "report": generate_report,
    }

    edges = [
        edge("collect_alerts", "analyze_noise"),
        edge("analyze_noise", "detect_fatigue"),
        edge("detect_fatigue", "tune_rules"),
        edge("tune_rules", "validate"),
        edge("validate", "report"),
        edge("report", END),
    ]

    entry = "collect_alerts"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AlertFatigueReducerRunner`` keep working via this alias; new code
# should use ``AlertFatigueReducerAgent`` directly through AgentRuntime.
AlertFatigueReducerRunner = AlertFatigueReducerAgent
