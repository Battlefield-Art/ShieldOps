# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the unified_cloud_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.unified_cloud_security.models import UnifiedCloudSecurityState
from shieldops.agents.unified_cloud_security.nodes import (
    assess_posture,
    collect_cloud_state,
    detect_threats,
    orchestrate_response,
    prioritize_risks,
    report,
)
from shieldops.agents.unified_cloud_security.tools import UnifiedCloudSecurityToolkit


class UnifiedCloudSecurityAgent(Agent):
    """Declarative spec for ``unified_cloud_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "unified_cloud_security"
    state_model = UnifiedCloudSecurityState
    toolkit_factory = UnifiedCloudSecurityToolkit

    nodes = {
        "collect_cloud_state": collect_cloud_state,
        "assess_posture": assess_posture,
        "detect_threats": detect_threats,
        "prioritize_risks": prioritize_risks,
        "orchestrate_response": orchestrate_response,
        "report": report,
    }

    edges = [
        edge("collect_cloud_state", "assess_posture"),
        edge("assess_posture", "detect_threats"),
        edge("detect_threats", "prioritize_risks"),
        edge("prioritize_risks", "orchestrate_response"),
        edge("orchestrate_response", "report"),
        edge("report", END),
    ]

    entry = "collect_cloud_state"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``UnifiedCloudSecurityRunner`` keep working via this alias; new code
# should use ``UnifiedCloudSecurityAgent`` directly through AgentRuntime.
UnifiedCloudSecurityRunner = UnifiedCloudSecurityAgent
