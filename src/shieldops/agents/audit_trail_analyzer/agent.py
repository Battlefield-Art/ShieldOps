# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the audit_trail_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.audit_trail_analyzer.models import AuditTrailAnalyzerState
from shieldops.agents.audit_trail_analyzer.nodes import (
    collect_logs,
    correlate_activities,
    detect_anomalies,
    generate_findings,
    normalize_events,
    report,
)
from shieldops.agents.audit_trail_analyzer.tools import AuditTrailAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AuditTrailAnalyzerAgent(Agent):
    """Declarative spec for ``audit_trail_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "audit_trail_analyzer"
    state_model = AuditTrailAnalyzerState
    toolkit_factory = AuditTrailAnalyzerToolkit

    nodes = {
        "collect_logs": collect_logs,
        "normalize_events": normalize_events,
        "detect_anomalies": detect_anomalies,
        "correlate_activities": correlate_activities,
        "generate_findings": generate_findings,
        "report": report,
    }

    edges = [
        edge("collect_logs", "normalize_events"),
        edge("normalize_events", "detect_anomalies"),
        edge("detect_anomalies", "correlate_activities"),
        edge("correlate_activities", "generate_findings"),
        edge("generate_findings", "report"),
        edge("report", END),
    ]

    entry = "collect_logs"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AuditTrailAnalyzerRunner`` keep working via this alias; new code
# should use ``AuditTrailAnalyzerAgent`` directly through AgentRuntime.
AuditTrailAnalyzerRunner = AuditTrailAnalyzerAgent
