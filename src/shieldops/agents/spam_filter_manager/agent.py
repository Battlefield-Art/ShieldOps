# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the spam_filter_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.spam_filter_manager.models import SpamFilterManagerState
from shieldops.agents.spam_filter_manager.nodes import (
    analyze_false_positives,
    classify_messages,
    collect_rules,
    generate_report,
    manage_quarantine,
    tune_filters,
)
from shieldops.agents.spam_filter_manager.tools import SpamFilterManagerToolkit


class SpamFilterManagerAgent(Agent):
    """Declarative spec for ``spam_filter_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "spam_filter_manager"
    state_model = SpamFilterManagerState
    toolkit_factory = SpamFilterManagerToolkit

    nodes = {
        "collect_rules": collect_rules,
        "classify_messages": classify_messages,
        "tune_filters": tune_filters,
        "analyze_false_positives": analyze_false_positives,
        "manage_quarantine": manage_quarantine,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_rules", "classify_messages"),
        edge("classify_messages", "tune_filters"),
        edge("tune_filters", "analyze_false_positives"),
        edge("analyze_false_positives", "manage_quarantine"),
        edge("manage_quarantine", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "collect_rules"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SpamFilterManagerRunner`` keep working via this alias; new code
# should use ``SpamFilterManagerAgent`` directly through AgentRuntime.
SpamFilterManagerRunner = SpamFilterManagerAgent
