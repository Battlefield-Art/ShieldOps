# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_lake_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_lake_manager.models import SecurityLakeState
from shieldops.agents.security_lake_manager.nodes import (
    discover_sources,
    generate_report,
    ingest_data,
    normalize_schema,
    optimize_storage,
    query_analytics,
)
from shieldops.agents.security_lake_manager.tools import SecurityLakeManagerToolkit


class SecurityLakeManagerAgent(Agent):
    """Declarative spec for ``security_lake_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_lake_manager"
    state_model = SecurityLakeState
    toolkit_factory = SecurityLakeManagerToolkit

    nodes = {
        "discover_sources": discover_sources,
        "ingest_data": ingest_data,
        "normalize_schema": normalize_schema,
        "optimize_storage": optimize_storage,
        "query_analytics": query_analytics,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_sources", "ingest_data"),
        edge("ingest_data", "normalize_schema"),
        edge("optimize_storage", "query_analytics"),
        edge("query_analytics", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "normalize_schema"
        #   routed by _should_optimize() — wrap with cond(...)
    ]

    entry = "discover_sources"
