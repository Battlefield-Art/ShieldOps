# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the mcp_gateway agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.mcp_gateway.models import MCPGatewayState
from shieldops.agents.mcp_gateway.nodes import (
    assess_security,
    detect_abuse,
    discover_servers,
    enforce_policies,
    generate_report,
    monitor_traffic,
)
from shieldops.agents.mcp_gateway.tools import McpGatewayToolkit
from shieldops.agents.runtime import END, Agent, edge


class McpGatewayAgent(Agent):
    """Declarative spec for ``mcp_gateway`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "mcp_gateway"
    state_model = MCPGatewayState
    toolkit_factory = McpGatewayToolkit

    nodes = {
        "discover_servers": discover_servers,
        "assess_security": assess_security,
        "enforce_policies": enforce_policies,
        "monitor_traffic": monitor_traffic,
        "detect_abuse": detect_abuse,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_servers", "assess_security"),
        edge("assess_security", "enforce_policies"),
        edge("monitor_traffic", "detect_abuse"),
        edge("detect_abuse", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "enforce_policies"
        #   routed by _should_monitor() — wrap with cond(...)
    ]

    entry = "discover_servers"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``MCPGatewayRunner`` keep working via this alias; new code
# should use ``McpGatewayAgent`` directly through AgentRuntime.
MCPGatewayRunner = McpGatewayAgent
