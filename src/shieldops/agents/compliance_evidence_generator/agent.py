# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the compliance_evidence_generator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.compliance_evidence_generator.models import ComplianceEvidenceGeneratorState
from shieldops.agents.compliance_evidence_generator.nodes import (
    collect_evidence,
    generate_report,
    identify_controls,
    identify_gaps,
    package_evidence,
    validate_evidence,
)
from shieldops.agents.compliance_evidence_generator.tools import ComplianceEvidenceGeneratorToolkit
from shieldops.agents.runtime import END, Agent, edge


class ComplianceEvidenceGeneratorAgent(Agent):
    """Declarative spec for ``compliance_evidence_generator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "compliance_evidence_generator"
    state_model = ComplianceEvidenceGeneratorState
    toolkit_factory = ComplianceEvidenceGeneratorToolkit

    nodes = {
        "identify_controls": identify_controls,
        "collect_evidence": collect_evidence,
        "validate_evidence": validate_evidence,
        "identify_gaps": identify_gaps,
        "package_evidence": package_evidence,
        "generate_report": generate_report,
    }

    edges = [
        edge("identify_controls", "collect_evidence"),
        edge("validate_evidence", "identify_gaps"),
        edge("package_evidence", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "collect_evidence" via _should_validate()
        # TODO: cond(...) from "identify_gaps" via _should_package()
    ]

    entry = "identify_controls"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ComplianceEvidenceGeneratorRunner`` keep working via this alias; new code
# should use ``ComplianceEvidenceGeneratorAgent`` directly through AgentRuntime.
ComplianceEvidenceGeneratorRunner = ComplianceEvidenceGeneratorAgent
