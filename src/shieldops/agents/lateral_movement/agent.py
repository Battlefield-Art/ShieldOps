# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the lateral_movement agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.lateral_movement.models import LateralMovementState
from shieldops.agents.lateral_movement.nodes import (
    analyze_paths,
    assess_blast_radius,
    collect_signals,
    detect_pivots,
    generate_report,
    respond,
)
from shieldops.agents.lateral_movement.tools import LateralMovementToolkit
from shieldops.agents.runtime import END, Agent, edge


class LateralMovementAgent(Agent):
    """Declarative spec for ``lateral_movement`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "lateral_movement"
    state_model = LateralMovementState
    toolkit_factory = LateralMovementToolkit

    nodes = {
        "collect_signals": collect_signals,
        "analyze_paths": analyze_paths,
        "detect_pivots": detect_pivots,
        "assess_blast_radius": assess_blast_radius,
        "respond": respond,
        "report": generate_report,
    }

    edges = [
        edge("collect_signals", "analyze_paths"),
        edge("analyze_paths", "detect_pivots"),
        edge("detect_pivots", "assess_blast_radius"),
        edge("respond", "report"),
        edge("report", END),
        # TODO: conditional edge from "assess_blast_radius"
        #   routed by _has_movement_paths() — wrap with cond(...)
    ]

    entry = "collect_signals"
