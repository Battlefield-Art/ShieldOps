# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the session_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.session_manager.models import SessionManagerState
from shieldops.agents.session_manager.nodes import (
    analyze_patterns,
    detect_hijacking,
    discover_sessions,
    enforce_timeouts,
    report,
    revoke_suspicious,
)
from shieldops.agents.session_manager.tools import SessionManagerToolkit


class SessionManagerAgent(Agent):
    """Declarative spec for ``session_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "session_manager"
    state_model = SessionManagerState
    toolkit_factory = SessionManagerToolkit

    nodes = {
        "discover_sessions": discover_sessions,
        "analyze_patterns": analyze_patterns,
        "detect_hijacking": detect_hijacking,
        "enforce_timeouts": enforce_timeouts,
        "revoke_suspicious": revoke_suspicious,
        "report": report,
    }

    edges = [
        edge("revoke_suspicious", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_sessions" via _check_error()
        # TODO: cond(...) from "analyze_patterns" via _check_error()
        # TODO: cond(...) from "detect_hijacking" via _check_error()
        # TODO: cond(...) from "enforce_timeouts" via _check_error()
    ]

    entry = "discover_sessions"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SessionManagerRunner`` keep working via this alias; new code
# should use ``SessionManagerAgent`` directly through AgentRuntime.
SessionManagerRunner = SessionManagerAgent
