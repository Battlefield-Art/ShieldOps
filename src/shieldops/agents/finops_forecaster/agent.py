# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the finops_forecaster agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.finops_forecaster.models import FinopsForecasterState
from shieldops.agents.finops_forecaster.nodes import (
    analyze_trends,
    collect_history,
    detect_anomalies,
    forecast_spend,
    generate_report,
    recommend_commitments,
)
from shieldops.agents.finops_forecaster.tools import FinopsForecasterToolkit
from shieldops.agents.runtime import END, Agent, edge


class FinopsForecasterAgent(Agent):
    """Declarative spec for ``finops_forecaster`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "finops_forecaster"
    state_model = FinopsForecasterState
    toolkit_factory = FinopsForecasterToolkit

    nodes = {
        "collect_history": collect_history,
        "analyze_trends": analyze_trends,
        "forecast_spend": forecast_spend,
        "detect_anomalies": detect_anomalies,
        "recommend_commitments": recommend_commitments,
        "report": generate_report,
    }

    edges = [
        edge("collect_history", "analyze_trends"),
        edge("analyze_trends", "forecast_spend"),
        edge("forecast_spend", "detect_anomalies"),
        edge("detect_anomalies", "recommend_commitments"),
        edge("recommend_commitments", "report"),
        edge("report", END),
    ]

    entry = "collect_history"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``FinopsForecasterRunner`` keep working via this alias; new code
# should use ``FinopsForecasterAgent`` directly through AgentRuntime.
FinopsForecasterRunner = FinopsForecasterAgent
