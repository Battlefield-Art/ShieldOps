# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the multi_cloud_posture agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.multi_cloud_posture.models import MultiCloudPostureState
from shieldops.agents.multi_cloud_posture.nodes import (
    compare_posture,
    detect_gaps,
    generate_report,
    normalize_findings,
    recommend,
    scan_clouds,
)
from shieldops.agents.multi_cloud_posture.tools import MultiCloudPostureToolkit
from shieldops.agents.runtime import END, Agent, edge


class MultiCloudPostureAgent(Agent):
    """Declarative spec for ``multi_cloud_posture`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "multi_cloud_posture"
    state_model = MultiCloudPostureState
    toolkit_factory = MultiCloudPostureToolkit

    nodes = {
        "scan_clouds": scan_clouds,
        "normalize_findings": normalize_findings,
        "compare_posture": compare_posture,
        "detect_gaps": detect_gaps,
        "recommend": recommend,
        "generate_report": generate_report,
    }

    edges = [
        edge("normalize_findings", "compare_posture"),
        edge("compare_posture", "detect_gaps"),
        edge("recommend", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "scan_clouds"
        #   routed by _should_normalize() — wrap with cond(...)
        # TODO: conditional edge from "detect_gaps"
        #   routed by _should_recommend() — wrap with cond(...)
    ]

    entry = "scan_clouds"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``MultiCloudPostureRunner`` keep working via this alias; new code
# should use ``MultiCloudPostureAgent`` directly through AgentRuntime.
MultiCloudPostureRunner = MultiCloudPostureAgent
