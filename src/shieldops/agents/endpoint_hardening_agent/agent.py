# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the endpoint_hardening_agent agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.endpoint_hardening_agent.models import EndpointHardeningAgentState
from shieldops.agents.endpoint_hardening_agent.nodes import (
    apply_hardening,
    check_baseline,
    detect_deviations,
    generate_fixes,
    generate_report,
    scan_endpoints,
)
from shieldops.agents.endpoint_hardening_agent.tools import EndpointHardeningAgentToolkit
from shieldops.agents.runtime import END, Agent, edge


class EndpointHardeningAgentAgent(Agent):
    """Declarative spec for ``endpoint_hardening_agent`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "endpoint_hardening_agent"
    state_model = EndpointHardeningAgentState
    toolkit_factory = EndpointHardeningAgentToolkit

    nodes = {
        "scan_endpoints": scan_endpoints,
        "check_baseline": check_baseline,
        "detect_deviations": detect_deviations,
        "generate_fixes": generate_fixes,
        "apply_hardening": apply_hardening,
        "report": generate_report,
    }

    edges = [
        edge("scan_endpoints", "check_baseline"),
        edge("check_baseline", "detect_deviations"),
        edge("detect_deviations", "generate_fixes"),
        edge("generate_fixes", "apply_hardening"),
        edge("apply_hardening", "report"),
        edge("report", END),
    ]

    entry = "scan_endpoints"
