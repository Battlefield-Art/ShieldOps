# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the attack_emulation_framework agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.attack_emulation_framework.models import AttackEmulationState
from shieldops.agents.attack_emulation_framework.nodes import (
    build_campaign,
    execute_techniques,
    generate_gaps,
    generate_report,
    measure_detection,
    select_adversary,
)
from shieldops.agents.attack_emulation_framework.tools import AttackEmulationFrameworkToolkit
from shieldops.agents.runtime import END, Agent, edge


class AttackEmulationFrameworkAgent(Agent):
    """Declarative spec for ``attack_emulation_framework`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "attack_emulation_framework"
    state_model = AttackEmulationState
    toolkit_factory = AttackEmulationFrameworkToolkit

    nodes = {
        "select_adversary": select_adversary,
        "build_campaign": build_campaign,
        "execute_techniques": execute_techniques,
        "measure_detection": measure_detection,
        "generate_gaps": generate_gaps,
        "generate_report": generate_report,
    }

    edges = [
        edge("select_adversary", "build_campaign"),
        edge("build_campaign", "execute_techniques"),
        edge("execute_techniques", "measure_detection"),
        edge("generate_gaps", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "measure_detection"
        #   routed by _should_analyze_gaps() — wrap with cond(...)
    ]

    entry = "select_adversary"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AttackEmulationFrameworkRunner`` keep working via this alias; new code
# should use ``AttackEmulationFrameworkAgent`` directly through AgentRuntime.
AttackEmulationFrameworkRunner = AttackEmulationFrameworkAgent
