# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the bandwidth_anomaly_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.bandwidth_anomaly_detector.models import BandwidthAnomalyDetectorState
from shieldops.agents.bandwidth_anomaly_detector.nodes import (
    build_baselines,
    classify_traffic,
    collect_samples,
    detect_anomalies,
    generate_report,
    send_alerts,
)
from shieldops.agents.bandwidth_anomaly_detector.tools import BandwidthAnomalyDetectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class BandwidthAnomalyDetectorAgent(Agent):
    """Declarative spec for ``bandwidth_anomaly_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "bandwidth_anomaly_detector"
    state_model = BandwidthAnomalyDetectorState
    toolkit_factory = BandwidthAnomalyDetectorToolkit

    nodes = {
        "collect_samples": collect_samples,
        "build_baselines": build_baselines,
        "detect_anomalies": detect_anomalies,
        "classify_traffic": classify_traffic,
        "alert": send_alerts,
        "report": generate_report,
    }

    edges = [
        edge("collect_samples", "build_baselines"),
        edge("build_baselines", "detect_anomalies"),
        edge("detect_anomalies", "classify_traffic"),
        edge("classify_traffic", "alert"),
        edge("alert", "report"),
        edge("report", END),
    ]

    entry = "collect_samples"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``BandwidthAnomalyDetectorRunner`` keep working via this alias; new code
# should use ``BandwidthAnomalyDetectorAgent`` directly through AgentRuntime.
BandwidthAnomalyDetectorRunner = BandwidthAnomalyDetectorAgent
