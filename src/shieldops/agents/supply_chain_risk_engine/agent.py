# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the supply_chain_risk_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.supply_chain_risk_engine.models import SupplyChainRiskEngineState
from shieldops.agents.supply_chain_risk_engine.nodes import (
    assess_risk,
    generate_report,
    inventory_dependencies,
    map_blast_radius,
    recommend_mitigations,
    scan_vulnerabilities,
)
from shieldops.agents.supply_chain_risk_engine.tools import SupplyChainRiskEngineToolkit


class SupplyChainRiskEngineAgent(Agent):
    """Declarative spec for ``supply_chain_risk_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "supply_chain_risk_engine"
    state_model = SupplyChainRiskEngineState
    toolkit_factory = SupplyChainRiskEngineToolkit

    nodes = {
        "inventory_dependencies": inventory_dependencies,
        "scan_vulnerabilities": scan_vulnerabilities,
        "assess_risk": assess_risk,
        "map_blast_radius": map_blast_radius,
        "recommend_mitigations": recommend_mitigations,
        "generate_report": generate_report,
    }

    edges = [
        edge("assess_risk", "map_blast_radius"),
        edge("recommend_mitigations", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "inventory_dependencies" via _check_error()
        # TODO: cond(...) from "scan_vulnerabilities" via _check_error()
        # TODO: cond(...) from "map_blast_radius" via should_recommend()
    ]

    entry = "inventory_dependencies"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SupplyChainRiskEngineRunner`` keep working via this alias; new code
# should use ``SupplyChainRiskEngineAgent`` directly through AgentRuntime.
SupplyChainRiskEngineRunner = SupplyChainRiskEngineAgent
