# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_cost_calculator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_cost_calculator.models import IncidentCostCalculatorState
from shieldops.agents.incident_cost_calculator.nodes import (
    benchmark,
    compute_direct,
    compute_indirect,
    gather_metrics,
    project_long_term,
    report,
)
from shieldops.agents.incident_cost_calculator.tools import IncidentCostCalculatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentCostCalculatorAgent(Agent):
    """Declarative spec for ``incident_cost_calculator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_cost_calculator"
    state_model = IncidentCostCalculatorState
    toolkit_factory = IncidentCostCalculatorToolkit

    nodes = {
        "gather_metrics": gather_metrics,
        "compute_direct": compute_direct,
        "compute_indirect": compute_indirect,
        "project_long_term": project_long_term,
        "benchmark": benchmark,
        "report": report,
    }

    edges = [
        edge("benchmark", "report"),
        edge("report", END),
        # TODO: cond(...) from "gather_metrics" via _check_error()
        # TODO: cond(...) from "compute_direct" via _check_error()
        # TODO: cond(...) from "compute_indirect" via _check_error()
        # TODO: cond(...) from "project_long_term" via _check_error()
    ]

    entry = "gather_metrics"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``IncidentCostCalculatorRunner`` keep working via this alias; new code
# should use ``IncidentCostCalculatorAgent`` directly through AgentRuntime.
IncidentCostCalculatorRunner = IncidentCostCalculatorAgent
