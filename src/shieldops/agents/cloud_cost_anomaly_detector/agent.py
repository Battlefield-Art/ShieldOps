# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_cost_anomaly_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_cost_anomaly_detector.models import CloudCostAnomalyDetectorState
from shieldops.agents.cloud_cost_anomaly_detector.nodes import (
    analyze_trends,
    classify_cause,
    collect_billing,
    detect_anomalies,
    generate_report,
    send_alerts,
)
from shieldops.agents.cloud_cost_anomaly_detector.tools import CloudCostAnomalyDetectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudCostAnomalyDetectorAgent(Agent):
    """Declarative spec for ``cloud_cost_anomaly_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_cost_anomaly_detector"
    state_model = CloudCostAnomalyDetectorState
    toolkit_factory = CloudCostAnomalyDetectorToolkit

    nodes = {
        "collect_billing": collect_billing,
        "analyze_trends": analyze_trends,
        "detect_anomalies": detect_anomalies,
        "classify_cause": classify_cause,
        "send_alerts": send_alerts,
        "report": generate_report,
    }

    edges = [
        edge("collect_billing", "analyze_trends"),
        edge("analyze_trends", "detect_anomalies"),
        edge("detect_anomalies", "classify_cause"),
        edge("classify_cause", "send_alerts"),
        edge("send_alerts", "report"),
        edge("report", END),
    ]

    entry = "collect_billing"
