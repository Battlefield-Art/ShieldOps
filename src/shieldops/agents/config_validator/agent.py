# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the config_validator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.config_validator.models import ConfigValidatorState
from shieldops.agents.config_validator.nodes import (
    assess_impact,
    collect_configs,
    compare_baselines,
    detect_drift,
    generate_report,
    remediate,
)
from shieldops.agents.config_validator.tools import ConfigValidatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class ConfigValidatorAgent(Agent):
    """Declarative spec for ``config_validator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "config_validator"
    state_model = ConfigValidatorState
    toolkit_factory = ConfigValidatorToolkit

    nodes = {
        "collect_configs": collect_configs,
        "compare_baselines": compare_baselines,
        "detect_drift": detect_drift,
        "assess_impact": assess_impact,
        "remediate": remediate,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_configs", "compare_baselines"),
        edge("compare_baselines", "detect_drift"),
        edge("detect_drift", "assess_impact"),
        edge("remediate", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_impact"
        #   routed by should_remediate() — wrap with cond(...)
    ]

    entry = "collect_configs"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ConfigValidatorRunner`` keep working via this alias; new code
# should use ``ConfigValidatorAgent`` directly through AgentRuntime.
ConfigValidatorRunner = ConfigValidatorAgent
