# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the risk_appetite_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.risk_appetite_engine.models import RiskAppetiteEngineState
from shieldops.agents.risk_appetite_engine.nodes import (
    compare_thresholds,
    define_appetite,
    generate_report,
    identify_breaches,
    measure_exposure,
    recommend_adjustments,
)
from shieldops.agents.risk_appetite_engine.tools import RiskAppetiteEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class RiskAppetiteEngineAgent(Agent):
    """Declarative spec for ``risk_appetite_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "risk_appetite_engine"
    state_model = RiskAppetiteEngineState
    toolkit_factory = RiskAppetiteEngineToolkit

    nodes = {
        "define_appetite": define_appetite,
        "measure_exposure": measure_exposure,
        "compare_thresholds": compare_thresholds,
        "identify_breaches": identify_breaches,
        "recommend_adjustments": recommend_adjustments,
        "generate_report": generate_report,
    }

    edges = [
        edge("define_appetite", "measure_exposure"),
        edge("measure_exposure", "compare_thresholds"),
        edge("recommend_adjustments", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "compare_thresholds" via _should_identify()
        # TODO: cond(...) from "identify_breaches" via _should_recommend()
    ]

    entry = "define_appetite"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``RiskAppetiteEngineRunner`` keep working via this alias; new code
# should use ``RiskAppetiteEngineAgent`` directly through AgentRuntime.
RiskAppetiteEngineRunner = RiskAppetiteEngineAgent
