# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the supervisor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.supervisor.models import SupervisorState
from shieldops.agents.supervisor.nodes import (
    chain_followup,
    classify_event,
    dispatch_to_agent,
    escalate,
    evaluate_result,
    finalize,
)
from shieldops.agents.supervisor.tools import SupervisorToolkit


class SupervisorAgent(Agent):
    """Declarative spec for ``supervisor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "supervisor"
    state_model = SupervisorState
    toolkit_factory = SupervisorToolkit

    nodes = {
        "classify_event": classify_event,
        "dispatch_to_agent": dispatch_to_agent,
        "evaluate_result": evaluate_result,
        "chain_followup": chain_followup,
        "escalate": escalate,
        "finalize": finalize,
    }

    edges = [
        edge("classify_event", "dispatch_to_agent"),
        edge("dispatch_to_agent", "evaluate_result"),
        edge("escalate", "finalize"),
        edge("finalize", END),
        # TODO: conditional edge from "evaluate_result"
        #   routed by should_chain_or_escalate() — wrap with cond(...)
        # TODO: conditional edge from "chain_followup"
        #   routed by after_chain() — wrap with cond(...)
    ]

    entry = "classify_event"
