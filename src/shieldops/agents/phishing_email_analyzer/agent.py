# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the phishing_email_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.phishing_email_analyzer.models import PhishingEmailAnalyzerState
from shieldops.agents.phishing_email_analyzer.nodes import (
    analyze_content,
    analyze_sender,
    analyze_urls,
    generate_report,
    ingest_email,
    score_risk,
)
from shieldops.agents.phishing_email_analyzer.tools import PhishingEmailAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class PhishingEmailAnalyzerAgent(Agent):
    """Declarative spec for ``phishing_email_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "phishing_email_analyzer"
    state_model = PhishingEmailAnalyzerState
    toolkit_factory = PhishingEmailAnalyzerToolkit

    nodes = {
        "ingest_email": ingest_email,
        "analyze_sender": analyze_sender,
        "analyze_urls": analyze_urls,
        "analyze_content": analyze_content,
        "score_risk": score_risk,
        "generate_report": generate_report,
    }

    edges = [
        edge("ingest_email", "analyze_sender"),
        edge("analyze_sender", "analyze_urls"),
        edge("analyze_urls", "analyze_content"),
        edge("analyze_content", "score_risk"),
        edge("score_risk", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "ingest_email"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``PhishingEmailAnalyzerRunner`` keep working via this alias; new code
# should use ``PhishingEmailAnalyzerAgent`` directly through AgentRuntime.
PhishingEmailAnalyzerRunner = PhishingEmailAnalyzerAgent
