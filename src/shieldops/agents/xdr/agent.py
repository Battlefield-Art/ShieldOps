# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the xdr agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.xdr.models import XDRState
from shieldops.agents.xdr.nodes import (
    build_attack_story,
    correlate_threats,
    execute_response,
    finalize_investigation,
    ingest_telemetry,
)
from shieldops.agents.xdr.tools import XDRToolkit


class XdrAgent(Agent):
    """Declarative spec for ``xdr`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "xdr"
    state_model = XDRState
    toolkit_factory = XDRToolkit

    nodes = {
        "ingest_telemetry": ingest_telemetry,
        "correlate_threats": correlate_threats,
        "build_attack_story": build_attack_story,
        "execute_response": execute_response,
        "finalize_investigation": finalize_investigation,
    }

    edges = [
        edge("correlate_threats", "build_attack_story"),
        edge("build_attack_story", "execute_response"),
        edge("execute_response", "finalize_investigation"),
        edge("finalize_investigation", END),
        # TODO: conditional edge from "ingest_telemetry"
        #   routed by should_continue() — wrap with cond(...)
    ]

    entry = "ingest_telemetry"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``XDRRunner`` keep working via this alias; new code
# should use ``XdrAgent`` directly through AgentRuntime.
XDRRunner = XdrAgent
