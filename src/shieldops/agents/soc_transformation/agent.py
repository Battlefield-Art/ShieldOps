# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the soc_transformation agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.soc_transformation.models import SOCTransformationState
from shieldops.agents.soc_transformation.nodes import (
    assess_current_soc,
    design_target_architecture,
    execute_migration_steps,
    plan_migration,
    report,
    validate_transformation,
)
from shieldops.agents.soc_transformation.tools import SOCTransformationToolkit


class SocTransformationAgent(Agent):
    """Declarative spec for ``soc_transformation`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "soc_transformation"
    state_model = SOCTransformationState
    toolkit_factory = SOCTransformationToolkit

    nodes = {
        "assess_current_soc": assess_current_soc,
        "design_target_architecture": design_target_architecture,
        "plan_migration": plan_migration,
        "execute_migration_steps": execute_migration_steps,
        "validate_transformation": validate_transformation,
        "report": report,
    }

    edges = [
        edge("assess_current_soc", "design_target_architecture"),
        edge("design_target_architecture", "plan_migration"),
        edge("validate_transformation", "report"),
        edge("report", END),
        # TODO: conditional edge from "plan_migration"
        #   routed by has_migration_steps() — wrap with cond(...)
        # TODO: conditional edge from "execute_migration_steps"
        #   routed by should_validate() — wrap with cond(...)
    ]

    entry = "assess_current_soc"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SOCTransformationRunner`` keep working via this alias; new code
# should use ``SocTransformationAgent`` directly through AgentRuntime.
SOCTransformationRunner = SocTransformationAgent
