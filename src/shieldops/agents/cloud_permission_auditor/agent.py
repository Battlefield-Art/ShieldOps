# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_permission_auditor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_permission_auditor.models import CloudPermissionAuditorState
from shieldops.agents.cloud_permission_auditor.nodes import (
    analyze_scope,
    collect_permissions,
    detect_violations,
    generate_fixes,
    generate_report,
    map_cross_account,
)
from shieldops.agents.cloud_permission_auditor.tools import CloudPermissionAuditorToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudPermissionAuditorAgent(Agent):
    """Declarative spec for ``cloud_permission_auditor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_permission_auditor"
    state_model = CloudPermissionAuditorState
    toolkit_factory = CloudPermissionAuditorToolkit

    nodes = {
        "collect_permissions": collect_permissions,
        "analyze_scope": analyze_scope,
        "detect_violations": detect_violations,
        "map_cross_account": map_cross_account,
        "generate_fixes": generate_fixes,
        "report": generate_report,
    }

    edges = [
        edge("collect_permissions", "analyze_scope"),
        edge("analyze_scope", "detect_violations"),
        edge("detect_violations", "map_cross_account"),
        edge("map_cross_account", "generate_fixes"),
        edge("generate_fixes", "report"),
        edge("report", END),
    ]

    entry = "collect_permissions"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudPermissionAuditorRunner`` keep working via this alias; new code
# should use ``CloudPermissionAuditorAgent`` directly through AgentRuntime.
CloudPermissionAuditorRunner = CloudPermissionAuditorAgent
