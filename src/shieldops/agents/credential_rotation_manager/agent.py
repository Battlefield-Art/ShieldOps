# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the credential_rotation_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.credential_rotation_manager.models import CredentialRotationManagerState
from shieldops.agents.credential_rotation_manager.nodes import (
    check_age,
    discover_credentials,
    execute_rotation,
    report,
    schedule_rotation,
    validate,
)
from shieldops.agents.credential_rotation_manager.tools import CredentialRotationManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CredentialRotationManagerAgent(Agent):
    """Declarative spec for ``credential_rotation_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "credential_rotation_manager"
    state_model = CredentialRotationManagerState
    toolkit_factory = CredentialRotationManagerToolkit

    nodes = {
        "discover_credentials": discover_credentials,
        "check_age": check_age,
        "schedule_rotation": schedule_rotation,
        "execute_rotation": execute_rotation,
        "validate": validate,
        "report": report,
    }

    edges = [
        edge("validate", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_credentials" via _check_error()
        # TODO: cond(...) from "check_age" via _check_error()
        # TODO: cond(...) from "schedule_rotation" via _check_error()
        # TODO: cond(...) from "execute_rotation" via _check_error()
    ]

    entry = "discover_credentials"
