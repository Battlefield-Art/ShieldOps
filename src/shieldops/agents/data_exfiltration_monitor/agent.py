# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the data_exfiltration_monitor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.data_exfiltration_monitor.models import DataExfiltrationMonitorState
from shieldops.agents.data_exfiltration_monitor.nodes import (
    analyze_flows,
    block_transfers,
    classify_sensitivity,
    detect_exfiltration,
    generate_report,
    monitor_channels,
)
from shieldops.agents.data_exfiltration_monitor.tools import DataExfiltrationMonitorToolkit
from shieldops.agents.runtime import END, Agent, edge


class DataExfiltrationMonitorAgent(Agent):
    """Declarative spec for ``data_exfiltration_monitor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "data_exfiltration_monitor"
    state_model = DataExfiltrationMonitorState
    toolkit_factory = DataExfiltrationMonitorToolkit

    nodes = {
        "monitor_channels": monitor_channels,
        "analyze_flows": analyze_flows,
        "detect_exfiltration": detect_exfiltration,
        "classify_sensitivity": classify_sensitivity,
        "block_transfers": block_transfers,
        "generate_report": generate_report,
    }

    edges = [
        edge("analyze_flows", "detect_exfiltration"),
        edge("detect_exfiltration", "classify_sensitivity"),
        edge("block_transfers", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "monitor_channels"
        #   routed by _should_analyze() — wrap with cond(...)
        # TODO: conditional edge from "classify_sensitivity"
        #   routed by _should_block() — wrap with cond(...)
    ]

    entry = "monitor_channels"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``DataExfiltrationMonitorRunner`` keep working via this alias; new code
# should use ``DataExfiltrationMonitorAgent`` directly through AgentRuntime.
DataExfiltrationMonitorRunner = DataExfiltrationMonitorAgent
