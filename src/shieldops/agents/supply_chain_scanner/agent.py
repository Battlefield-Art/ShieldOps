# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the supply_chain_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.supply_chain_scanner.models import SupplyChainScannerState
from shieldops.agents.supply_chain_scanner.nodes import (
    generate_report,
    inventory_ai_assets,
    scan_model_registries,
    scan_prompt_templates,
    scan_rag_sources,
    scan_tool_definitions,
)
from shieldops.agents.supply_chain_scanner.tools import SupplyChainScannerToolkit


class SupplyChainScannerAgent(Agent):
    """Declarative spec for ``supply_chain_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "supply_chain_scanner"
    state_model = SupplyChainScannerState
    toolkit_factory = SupplyChainScannerToolkit

    nodes = {
        "inventory_ai_assets": inventory_ai_assets,
        "scan_model_registries": scan_model_registries,
        "scan_rag_sources": scan_rag_sources,
        "scan_prompt_templates": scan_prompt_templates,
        "scan_tool_definitions": scan_tool_definitions,
        "generate_report": generate_report,
    }

    edges = [
        edge("inventory_ai_assets", "scan_model_registries"),
        edge("scan_model_registries", "scan_rag_sources"),
        edge("scan_rag_sources", "scan_prompt_templates"),
        edge("scan_prompt_templates", "scan_tool_definitions"),
        edge("scan_tool_definitions", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "inventory_ai_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SupplyChainScannerRunner`` keep working via this alias; new code
# should use ``SupplyChainScannerAgent`` directly through AgentRuntime.
SupplyChainScannerRunner = SupplyChainScannerAgent
