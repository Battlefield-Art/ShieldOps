# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the predictive_scaler agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.predictive_scaler.models import PredictiveScalerState
from shieldops.agents.predictive_scaler.nodes import (
    analyze_patterns,
    collect_metrics,
    execute_scaling,
    generate_report,
    plan_scaling,
    predict_demand,
)
from shieldops.agents.predictive_scaler.tools import PredictiveScalerToolkit
from shieldops.agents.runtime import END, Agent, edge


class PredictiveScalerAgent(Agent):
    """Declarative spec for ``predictive_scaler`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "predictive_scaler"
    state_model = PredictiveScalerState
    toolkit_factory = PredictiveScalerToolkit

    nodes = {
        "collect_metrics": collect_metrics,
        "analyze_patterns": analyze_patterns,
        "predict_demand": predict_demand,
        "plan_scaling": plan_scaling,
        "execute_scaling": execute_scaling,
        "report": generate_report,
    }

    edges = [
        edge("collect_metrics", "analyze_patterns"),
        edge("analyze_patterns", "predict_demand"),
        edge("predict_demand", "plan_scaling"),
        edge("plan_scaling", "execute_scaling"),
        edge("execute_scaling", "report"),
        edge("report", END),
    ]

    entry = "collect_metrics"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``PredictiveScalerRunner`` keep working via this alias; new code
# should use ``PredictiveScalerAgent`` directly through AgentRuntime.
PredictiveScalerRunner = PredictiveScalerAgent
