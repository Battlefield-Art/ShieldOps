# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the agentic_mdr agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.agentic_mdr.models import AgenticMDRState
from shieldops.agents.agentic_mdr.nodes import (
    auto_triage,
    decide_response,
    execute_response,
    ingest_alerts,
    investigate,
    report,
    validate_and_learn,
)
from shieldops.agents.agentic_mdr.tools import AgenticMDRToolkit
from shieldops.agents.runtime import END, Agent, edge


class AgenticMdrAgent(Agent):
    """Declarative spec for ``agentic_mdr`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "agentic_mdr"
    state_model = AgenticMDRState
    toolkit_factory = AgenticMDRToolkit

    nodes = {
        "ingest_alerts": ingest_alerts,
        "auto_triage": auto_triage,
        "investigate": investigate,
        "decide_response": decide_response,
        "execute_response": execute_response,
        "validate_and_learn": validate_and_learn,
        "report": report,
    }

    edges = [
        edge("ingest_alerts", "auto_triage"),
        edge("investigate", "decide_response"),
        edge("execute_response", "validate_and_learn"),
        edge("validate_and_learn", "report"),
        edge("report", END),
        # TODO: conditional edge from "auto_triage"
        #   routed by _has_actionable_alerts() — wrap with cond(...)
        # TODO: conditional edge from "decide_response"
        #   routed by _needs_execution() — wrap with cond(...)
    ]

    entry = "ingest_alerts"
