# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_training_tracker agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_training_tracker.models import SecurityTrainingTrackerState
from shieldops.agents.security_training_tracker.nodes import (
    assess_requirements,
    assign_remediation,
    generate_report,
    identify_gaps,
    measure_effectiveness,
    track_completion,
)
from shieldops.agents.security_training_tracker.tools import SecurityTrainingTrackerToolkit


class SecurityTrainingTrackerAgent(Agent):
    """Declarative spec for ``security_training_tracker`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_training_tracker"
    state_model = SecurityTrainingTrackerState
    toolkit_factory = SecurityTrainingTrackerToolkit

    nodes = {
        "assess_requirements": assess_requirements,
        "track_completion": track_completion,
        "measure_effectiveness": measure_effectiveness,
        "identify_gaps": identify_gaps,
        "assign_remediation": assign_remediation,
        "generate_report": generate_report,
    }

    edges = [
        edge("assess_requirements", "track_completion"),
        edge("track_completion", "measure_effectiveness"),
        edge("measure_effectiveness", "identify_gaps"),
        edge("assign_remediation", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "identify_gaps"
        #   routed by _should_remediate() — wrap with cond(...)
    ]

    entry = "assess_requirements"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityTrainingTrackerRunner`` keep working via this alias; new code
# should use ``SecurityTrainingTrackerAgent`` directly through AgentRuntime.
SecurityTrainingTrackerRunner = SecurityTrainingTrackerAgent
