# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the privacy_engineering agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.privacy_engineering.models import PrivacyEngineeringState
from shieldops.agents.privacy_engineering.nodes import (
    assess_anonymization,
    audit_pets,
    check_compliance,
    report,
    scan_pipelines,
    validate_differential_privacy,
)
from shieldops.agents.privacy_engineering.tools import PrivacyEngineeringToolkit
from shieldops.agents.runtime import END, Agent, edge


class PrivacyEngineeringAgent(Agent):
    """Declarative spec for ``privacy_engineering`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "privacy_engineering"
    state_model = PrivacyEngineeringState
    toolkit_factory = PrivacyEngineeringToolkit

    nodes = {
        "scan_pipelines": scan_pipelines,
        "assess_anonymization": assess_anonymization,
        "validate_differential_privacy": validate_differential_privacy,
        "audit_pets": audit_pets,
        "check_compliance": check_compliance,
        "report": report,
    }

    edges = [
        edge("validate_differential_privacy", "audit_pets"),
        edge("audit_pets", "check_compliance"),
        edge("check_compliance", "report"),
        edge("report", END),
        # TODO: conditional edge from "scan_pipelines"
        #   routed by _has_error() — wrap with cond(...)
        # TODO: conditional edge from "assess_anonymization"
        #   routed by _has_findings() — wrap with cond(...)
    ]

    entry = "scan_pipelines"
