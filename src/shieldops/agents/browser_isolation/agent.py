# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the browser_isolation agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.browser_isolation.models import BrowserIsolationState
from shieldops.agents.browser_isolation.nodes import (
    collect_sessions,
    detect_breakouts,
    evaluate_policies,
    generate_report,
    sandbox_content,
)
from shieldops.agents.browser_isolation.tools import BrowserIsolationToolkit
from shieldops.agents.runtime import END, Agent, edge


class BrowserIsolationAgent(Agent):
    """Declarative spec for ``browser_isolation`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "browser_isolation"
    state_model = BrowserIsolationState
    toolkit_factory = BrowserIsolationToolkit

    nodes = {
        "collect_sessions": collect_sessions,
        "detect_breakouts": detect_breakouts,
        "evaluate_policies": evaluate_policies,
        "sandbox_content": sandbox_content,
        "report": generate_report,
        # TODO: resolve node fn for "error_end"
    }

    edges = [
        edge("detect_breakouts", "evaluate_policies"),
        edge("evaluate_policies", "sandbox_content"),
        edge("sandbox_content", "report"),
        edge("report", END),
        edge("error_end", END),
        # TODO: conditional edge from "collect_sessions"
        #   routed by _check_error() — wrap with cond(...)
    ]

    entry = "collect_sessions"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``BrowserIsolationRunner`` keep working via this alias; new code
# should use ``BrowserIsolationAgent`` directly through AgentRuntime.
BrowserIsolationRunner = BrowserIsolationAgent
