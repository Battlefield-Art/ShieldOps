# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the browser_threat_protector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.browser_threat_protector.models import BrowserThreatProtectorState
from shieldops.agents.browser_threat_protector.nodes import (
    analyze_request,
    check_reputation,
    enforce_policy,
    generate_report,
    isolate_session,
    scan_content,
)
from shieldops.agents.browser_threat_protector.tools import BrowserThreatProtectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class BrowserThreatProtectorAgent(Agent):
    """Declarative spec for ``browser_threat_protector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "browser_threat_protector"
    state_model = BrowserThreatProtectorState
    toolkit_factory = BrowserThreatProtectorToolkit

    nodes = {
        "analyze_request": analyze_request,
        "check_reputation": check_reputation,
        "isolate_session": isolate_session,
        "scan_content": scan_content,
        "enforce_policy": enforce_policy,
        "generate_report": generate_report,
    }

    edges = [
        edge("analyze_request", "check_reputation"),
        edge("scan_content", "enforce_policy"),
        edge("enforce_policy", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "check_reputation"
        #   routed by _should_isolate() — wrap with cond(...)
        # TODO: conditional edge from "isolate_session"
        #   routed by _should_scan() — wrap with cond(...)
    ]

    entry = "analyze_request"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``BrowserThreatProtectorRunner`` keep working via this alias; new code
# should use ``BrowserThreatProtectorAgent`` directly through AgentRuntime.
BrowserThreatProtectorRunner = BrowserThreatProtectorAgent
