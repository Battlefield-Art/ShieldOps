# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the policy_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.policy_engine.models import PolicyEngineState
from shieldops.agents.policy_engine.nodes import (
    collect_requirements,
    detect_drift,
    generate_policies,
    generate_report,
    reconcile,
    validate_coverage,
)
from shieldops.agents.policy_engine.tools import PolicyEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class PolicyEngineAgent(Agent):
    """Declarative spec for ``policy_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "policy_engine"
    state_model = PolicyEngineState
    toolkit_factory = PolicyEngineToolkit

    nodes = {
        "collect_requirements": collect_requirements,
        "generate_policies": generate_policies,
        "validate_coverage": validate_coverage,
        "detect_drift": detect_drift,
        "reconcile": reconcile,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_requirements", "generate_policies"),
        edge("generate_policies", "validate_coverage"),
        edge("validate_coverage", "detect_drift"),
        edge("reconcile", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_drift"
        #   routed by _should_reconcile() — wrap with cond(...)
    ]

    entry = "collect_requirements"
