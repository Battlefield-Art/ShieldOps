# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the insider_threat agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.insider_threat.models import InsiderThreatState
from shieldops.agents.insider_threat.nodes import (
    assess_risk,
    build_behavioral_baseline,
    collect_user_signals,
    detect_deviations,
    generate_report,
    investigate,
)
from shieldops.agents.insider_threat.tools import InsiderThreatToolkit
from shieldops.agents.runtime import END, Agent, edge


class InsiderThreatAgent(Agent):
    """Declarative spec for ``insider_threat`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "insider_threat"
    state_model = InsiderThreatState
    toolkit_factory = InsiderThreatToolkit

    nodes = {
        "collect_user_signals": collect_user_signals,
        "build_behavioral_baseline": build_behavioral_baseline,
        "detect_deviations": detect_deviations,
        "assess_risk": assess_risk,
        "investigate": investigate,
        "report": generate_report,
    }

    edges = [
        edge("collect_user_signals", "build_behavioral_baseline"),
        edge("build_behavioral_baseline", "detect_deviations"),
        edge("detect_deviations", "assess_risk"),
        edge("investigate", "report"),
        edge("report", END),
        # TODO: conditional edge from "assess_risk"
        #   routed by _has_high_risk_users() — wrap with cond(...)
    ]

    entry = "collect_user_signals"
