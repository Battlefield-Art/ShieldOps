# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_communicator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_communicator.models import IncidentCommunicatorState
from shieldops.agents.incident_communicator.nodes import (
    draft_messages,
    identify_stakeholders,
    report,
    select_channels,
    send_notifications,
    track_acks,
)
from shieldops.agents.incident_communicator.tools import IncidentCommunicatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentCommunicatorAgent(Agent):
    """Declarative spec for ``incident_communicator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_communicator"
    state_model = IncidentCommunicatorState
    toolkit_factory = IncidentCommunicatorToolkit

    nodes = {
        "identify_stakeholders": identify_stakeholders,
        "draft_messages": draft_messages,
        "select_channels": select_channels,
        "send_notifications": send_notifications,
        "track_acks": track_acks,
        "report": report,
    }

    edges = [
        edge("track_acks", "report"),
        edge("report", END),
        # TODO: cond(...) from "identify_stakeholders"
        # TODO: cond(...) from "draft_messages"
        # TODO: cond(...) from "select_channels"
        # TODO: cond(...) from "send_notifications"
    ]

    entry = "identify_stakeholders"
