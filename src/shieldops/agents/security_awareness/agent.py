# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_awareness agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_awareness.models import SecurityAwarenessState
from shieldops.agents.security_awareness.nodes import (
    assess_baseline,
    recommend,
    report,
    run_simulations,
    score_risk,
    track_training,
)
from shieldops.agents.security_awareness.tools import SecurityAwarenessToolkit


class SecurityAwarenessAgent(Agent):
    """Declarative spec for ``security_awareness`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_awareness"
    state_model = SecurityAwarenessState
    toolkit_factory = SecurityAwarenessToolkit

    nodes = {
        "assess_baseline": assess_baseline,
        "run_simulations": run_simulations,
        "track_training": track_training,
        "score_risk": score_risk,
        "recommend": recommend,
        "report": report,
    }

    edges = [
        edge("run_simulations", "track_training"),
        edge("track_training", "score_risk"),
        edge("score_risk", "recommend"),
        edge("recommend", "report"),
        edge("report", END),
        # TODO: cond(...) from "assess_baseline" via _route_after_baseline()
    ]

    entry = "assess_baseline"
