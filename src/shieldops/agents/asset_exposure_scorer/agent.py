# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the asset_exposure_scorer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.asset_exposure_scorer.models import AssetExposureScorerState
from shieldops.agents.asset_exposure_scorer.nodes import (
    check_vulns,
    discover_assets,
    fingerprint_services,
    generate_report,
    score_exposure,
    track_changes,
)
from shieldops.agents.asset_exposure_scorer.tools import AssetExposureScorerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AssetExposureScorerAgent(Agent):
    """Declarative spec for ``asset_exposure_scorer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "asset_exposure_scorer"
    state_model = AssetExposureScorerState
    toolkit_factory = AssetExposureScorerToolkit

    nodes = {
        "discover_assets": discover_assets,
        "fingerprint_services": fingerprint_services,
        "check_vulns": check_vulns,
        "score_exposure": score_exposure,
        "track_changes": track_changes,
        "report": generate_report,
    }

    edges = [
        edge("discover_assets", "fingerprint_services"),
        edge("fingerprint_services", "check_vulns"),
        edge("check_vulns", "score_exposure"),
        edge("score_exposure", "track_changes"),
        edge("track_changes", "report"),
        edge("report", END),
    ]

    entry = "discover_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AssetExposureScorerRunner`` keep working via this alias; new code
# should use ``AssetExposureScorerAgent`` directly through AgentRuntime.
AssetExposureScorerRunner = AssetExposureScorerAgent
