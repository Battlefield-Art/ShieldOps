# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the learning agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.learning.models import LearningState
from shieldops.agents.learning.nodes import (
    analyze_patterns,
    gather_outcomes,
    recommend_playbooks,
    recommend_thresholds,
    synthesize_improvements,
)
from shieldops.agents.learning.tools import LearningToolkit
from shieldops.agents.runtime import END, Agent, edge


class LearningAgent(Agent):
    """Declarative spec for ``learning`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "learning"
    state_model = LearningState
    toolkit_factory = LearningToolkit

    nodes = {
        "gather_outcomes": gather_outcomes,
        "analyze_patterns": analyze_patterns,
        "recommend_playbooks": recommend_playbooks,
        "recommend_thresholds": recommend_thresholds,
        "synthesize_improvements": synthesize_improvements,
    }

    edges = [
        edge("gather_outcomes", "analyze_patterns"),
        edge("recommend_thresholds", "synthesize_improvements"),
        edge("synthesize_improvements", END),
        # TODO: conditional edge from "analyze_patterns"
        #   routed by should_recommend_playbooks() — wrap with cond(...)
        # TODO: conditional edge from "recommend_playbooks"
        #   routed by should_recommend_thresholds() — wrap with cond(...)
    ]

    entry = "gather_outcomes"
