# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the risk_quantification_platform agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.risk_quantification_platform.models import RiskQuantificationState
from shieldops.agents.risk_quantification_platform.nodes import (
    assess_threats,
    calculate_risk,
    generate_report,
    identify_assets,
    model_loss,
    prioritize,
)
from shieldops.agents.risk_quantification_platform.tools import RiskQuantificationPlatformToolkit
from shieldops.agents.runtime import END, Agent, edge


class RiskQuantificationPlatformAgent(Agent):
    """Declarative spec for ``risk_quantification_platform`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "risk_quantification_platform"
    state_model = RiskQuantificationState
    toolkit_factory = RiskQuantificationPlatformToolkit

    nodes = {
        "identify_assets": identify_assets,
        "assess_threats": assess_threats,
        "model_loss": model_loss,
        "calculate_risk": calculate_risk,
        "prioritize": prioritize,
        "generate_report": generate_report,
    }

    edges = [
        edge("identify_assets", "assess_threats"),
        edge("assess_threats", "model_loss"),
        edge("model_loss", "calculate_risk"),
        edge("prioritize", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "calculate_risk"
        #   routed by _should_prioritize() — wrap with cond(...)
    ]

    entry = "identify_assets"
