# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the governance_dashboard agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.governance_dashboard.models import GovernanceDashboardState
from shieldops.agents.governance_dashboard.nodes import (
    assess_policies,
    collect_metrics,
    executive_summary,
    generate_insights,
    report,
    score_risk,
)
from shieldops.agents.governance_dashboard.tools import GovernanceDashboardToolkit
from shieldops.agents.runtime import END, Agent, edge


class GovernanceDashboardAgent(Agent):
    """Declarative spec for ``governance_dashboard`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "governance_dashboard"
    state_model = GovernanceDashboardState
    toolkit_factory = GovernanceDashboardToolkit

    nodes = {
        "collect_metrics": collect_metrics,
        "assess_policies": assess_policies,
        "score_risk": score_risk,
        "generate_insights": generate_insights,
        "executive_summary": executive_summary,
        "report": report,
    }

    edges = [
        edge("executive_summary", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_metrics" via _check_error()
        # TODO: cond(...) from "assess_policies" via _check_error()
        # TODO: cond(...) from "score_risk" via _check_error()
        # TODO: cond(...) from "generate_insights" via _check_error()
    ]

    entry = "collect_metrics"
