# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the container_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.container_security.models import ContainerSecurityState
from shieldops.agents.container_security.nodes import (
    analyze_runtime,
    detect_anomalies,
    enforce_admission,
    generate_report,
    remediate,
    scan_images,
)
from shieldops.agents.container_security.tools import ContainerSecurityToolkit
from shieldops.agents.runtime import END, Agent, edge


class ContainerSecurityAgent(Agent):
    """Declarative spec for ``container_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "container_security"
    state_model = ContainerSecurityState
    toolkit_factory = ContainerSecurityToolkit

    nodes = {
        "scan_images": scan_images,
        "analyze_runtime": analyze_runtime,
        "detect_anomalies": detect_anomalies,
        "enforce_admission": enforce_admission,
        "remediate": remediate,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_images", "analyze_runtime"),
        edge("analyze_runtime", "detect_anomalies"),
        edge("detect_anomalies", "enforce_admission"),
        edge("remediate", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "enforce_admission"
        #   routed by _has_threats() — wrap with cond(...)
    ]

    entry = "scan_images"
