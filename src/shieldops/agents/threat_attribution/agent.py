# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the threat_attribution agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.threat_attribution.models import ThreatAttributionState
from shieldops.agents.threat_attribution.nodes import (
    assess_confidence,
    collect_evidence,
    generate_report,
    map_ttps,
    profile_actor,
)
from shieldops.agents.threat_attribution.tools import ThreatAttributionToolkit


class ThreatAttributionAgent(Agent):
    """Declarative spec for ``threat_attribution`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "threat_attribution"
    state_model = ThreatAttributionState
    toolkit_factory = ThreatAttributionToolkit

    nodes = {
        "collect_evidence": collect_evidence,
        "map_ttps": map_ttps,
        "profile_actor": profile_actor,
        "assess_confidence": assess_confidence,
        "generate_report": generate_report,
    }

    edges = [
        edge("generate_report", END),
        # TODO: cond(...) from "collect_evidence" via _route_or_report()
        # TODO: cond(...) from "map_ttps" via _route_or_report()
        # TODO: cond(...) from "profile_actor" via _route_or_report()
        # TODO: cond(...) from "assess_confidence" via _route_or_report()
    ]

    entry = "collect_evidence"
