# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the post_incident_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.post_incident_analyzer.models import PostIncidentAnalyzerState
from shieldops.agents.post_incident_analyzer.nodes import (
    action_items,
    gather_timeline,
    impact_assessment,
    lessons_learned,
    report,
    root_cause_analysis,
)
from shieldops.agents.post_incident_analyzer.tools import PostIncidentAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class PostIncidentAnalyzerAgent(Agent):
    """Declarative spec for ``post_incident_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "post_incident_analyzer"
    state_model = PostIncidentAnalyzerState
    toolkit_factory = PostIncidentAnalyzerToolkit

    nodes = {
        "gather_timeline": gather_timeline,
        "root_cause_analysis": root_cause_analysis,
        "impact_assessment": impact_assessment,
        "lessons_learned": lessons_learned,
        "action_items": action_items,
        "report": report,
    }

    edges = [
        edge("report", END),
        # TODO: cond(...) from "gather_timeline" via _route_or_report()
        # TODO: cond(...) from "root_cause_analysis" via _route_or_report()
        # TODO: cond(...) from "impact_assessment" via _route_or_report()
        # TODO: cond(...) from "lessons_learned" via _route_or_report()
        # TODO: cond(...) from "action_items" via _route_or_report()
    ]

    entry = "gather_timeline"
