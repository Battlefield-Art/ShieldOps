# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the network_segmentation agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.network_segmentation.models import NetworkSegmentationState
from shieldops.agents.network_segmentation.nodes import (
    assess_risk,
    detect_violations,
    discover_zones,
    enforce_policies,
    map_traffic,
    report,
)
from shieldops.agents.network_segmentation.tools import NetworkSegmentationToolkit
from shieldops.agents.runtime import END, Agent, edge


class NetworkSegmentationAgent(Agent):
    """Declarative spec for ``network_segmentation`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "network_segmentation"
    state_model = NetworkSegmentationState
    toolkit_factory = NetworkSegmentationToolkit

    nodes = {
        "discover_zones": discover_zones,
        "map_traffic": map_traffic,
        "detect_violations": detect_violations,
        "assess_risk": assess_risk,
        "enforce_policies": enforce_policies,
        "report": report,
    }

    edges = [
        edge("discover_zones", "map_traffic"),
        edge("map_traffic", "detect_violations"),
        edge("detect_violations", "assess_risk"),
        edge("enforce_policies", "report"),
        edge("report", END),
        # TODO: conditional edge from "assess_risk"
        #   routed by _has_violations() — wrap with cond(...)
    ]

    entry = "discover_zones"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``NetworkSegmentationRunner`` keep working via this alias; new code
# should use ``NetworkSegmentationAgent`` directly through AgentRuntime.
NetworkSegmentationRunner = NetworkSegmentationAgent
