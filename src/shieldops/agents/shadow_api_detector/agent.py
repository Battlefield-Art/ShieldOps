# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the shadow_api_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.shadow_api_detector.models import ShadowAPIDetectorState
from shieldops.agents.shadow_api_detector.nodes import (
    analyze_endpoints,
    auto_document,
    classify_risk,
    detect_shadow,
    discover_traffic,
    generate_report,
)
from shieldops.agents.shadow_api_detector.tools import ShadowAPIDetectorToolkit


class ShadowApiDetectorAgent(Agent):
    """Declarative spec for ``shadow_api_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "shadow_api_detector"
    state_model = ShadowAPIDetectorState
    toolkit_factory = ShadowAPIDetectorToolkit

    nodes = {
        "discover_traffic": discover_traffic,
        "analyze_endpoints": analyze_endpoints,
        "detect_shadow": detect_shadow,
        "classify_risk": classify_risk,
        "auto_document": auto_document,
        "report": generate_report,
    }

    edges = [
        edge("discover_traffic", "analyze_endpoints"),
        edge("analyze_endpoints", "detect_shadow"),
        edge("detect_shadow", "classify_risk"),
        edge("classify_risk", "auto_document"),
        edge("auto_document", "report"),
        edge("report", END),
    ]

    entry = "discover_traffic"
