# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the insider_risk_scorer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.insider_risk_scorer.models import InsiderRiskScorerState
from shieldops.agents.insider_risk_scorer.nodes import (
    analyze_behavior,
    collect_signals,
    detect_anomalies,
    generate_alerts,
    generate_report,
    score_risk,
)
from shieldops.agents.insider_risk_scorer.tools import InsiderRiskScorerToolkit
from shieldops.agents.runtime import END, Agent, edge


class InsiderRiskScorerAgent(Agent):
    """Declarative spec for ``insider_risk_scorer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "insider_risk_scorer"
    state_model = InsiderRiskScorerState
    toolkit_factory = InsiderRiskScorerToolkit

    nodes = {
        "collect_signals": collect_signals,
        "analyze_behavior": analyze_behavior,
        "score_risk": score_risk,
        "detect_anomalies": detect_anomalies,
        "generate_alerts": generate_alerts,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_signals", "analyze_behavior"),
        edge("analyze_behavior", "score_risk"),
        edge("score_risk", "detect_anomalies"),
        edge("generate_alerts", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_anomalies"
        #   routed by _should_alert() — wrap with cond(...)
    ]

    entry = "collect_signals"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``InsiderRiskScorerRunner`` keep working via this alias; new code
# should use ``InsiderRiskScorerAgent`` directly through AgentRuntime.
InsiderRiskScorerRunner = InsiderRiskScorerAgent
