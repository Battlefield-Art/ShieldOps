# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the evolution agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.evolution.models import EvolutionState
from shieldops.agents.evolution.nodes import (
    _analyze,
    _deploy,
    _evolve,
    _measure,
    _propagate,
    _report,
    _validate,
)
from shieldops.agents.evolution.tools import EvolutionToolkit
from shieldops.agents.runtime import END, Agent, edge


class EvolutionAgent(Agent):
    """Declarative spec for ``evolution`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "evolution"
    state_model = EvolutionState
    toolkit_factory = EvolutionToolkit

    nodes = {
        "measure_fitness": _measure,
        "analyze_patterns": _analyze,
        "evolve_prompts": _evolve,
        "propagate_learnings": _propagate,
        "deploy_changes": _deploy,
        "validate_evolution": _validate,
        "report": _report,
    }

    edges = [
        edge("analyze_patterns", "evolve_prompts"),
        edge("evolve_prompts", "propagate_learnings"),
        edge("deploy_changes", "validate_evolution"),
        edge("validate_evolution", "report"),
        edge("report", END),
        # TODO: conditional edge from "measure_fitness"
        #   routed by _has_candidates() — wrap with cond(...)
        # TODO: conditional edge from "propagate_learnings"
        #   routed by _has_mutations() — wrap with cond(...)
    ]

    entry = "measure_fitness"
