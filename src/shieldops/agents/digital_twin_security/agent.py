# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the digital_twin_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.digital_twin_security.models import DigitalTwinSecurityState
from shieldops.agents.digital_twin_security.nodes import (
    analyze_results,
    configure_environment,
    create_twin,
    execute_simulations,
    generate_report,
    validate_posture,
)
from shieldops.agents.digital_twin_security.tools import DigitalTwinSecurityToolkit
from shieldops.agents.runtime import END, Agent, edge


class DigitalTwinSecurityAgent(Agent):
    """Declarative spec for ``digital_twin_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "digital_twin_security"
    state_model = DigitalTwinSecurityState
    toolkit_factory = DigitalTwinSecurityToolkit

    nodes = {
        "create_twin": create_twin,
        "configure_environment": configure_environment,
        "execute_simulations": execute_simulations,
        "analyze_results": analyze_results,
        "validate_posture": validate_posture,
        "generate_report": generate_report,
    }

    edges = [
        edge("create_twin", "configure_environment"),
        edge("configure_environment", "execute_simulations"),
        edge("execute_simulations", "analyze_results"),
        edge("analyze_results", "validate_posture"),
        edge("validate_posture", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "create_twin"
