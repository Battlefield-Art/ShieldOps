# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the credential_hygiene_auditor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.credential_hygiene_auditor.models import CredentialHygieneAuditorState
from shieldops.agents.credential_hygiene_auditor.nodes import (
    assess_hygiene,
    detect_violations,
    generate_report,
    inventory_credentials,
    recommend_fixes,
    score_risk,
)
from shieldops.agents.credential_hygiene_auditor.tools import CredentialHygieneAuditorToolkit
from shieldops.agents.runtime import END, Agent, edge


class CredentialHygieneAuditorAgent(Agent):
    """Declarative spec for ``credential_hygiene_auditor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "credential_hygiene_auditor"
    state_model = CredentialHygieneAuditorState
    toolkit_factory = CredentialHygieneAuditorToolkit

    nodes = {
        "inventory_credentials": inventory_credentials,
        "assess_hygiene": assess_hygiene,
        "detect_violations": detect_violations,
        "score_risk": score_risk,
        "recommend_fixes": recommend_fixes,
        "generate_report": generate_report,
    }

    edges = [
        edge("detect_violations", "score_risk"),
        edge("recommend_fixes", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "inventory_credentials" via _check_error()
        # TODO: cond(...) from "assess_hygiene" via _check_error()
        # TODO: cond(...) from "score_risk" via should_recommend_fixes()
    ]

    entry = "inventory_credentials"
