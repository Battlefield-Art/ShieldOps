# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the training_data_validator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.training_data_validator.models import TrainingDataValidatorState
from shieldops.agents.training_data_validator.nodes import (
    analyze_bias,
    check_labels,
    detect_poisoning,
    profile_data,
    report,
    validate_provenance,
)
from shieldops.agents.training_data_validator.tools import TrainingDataValidatorToolkit


class TrainingDataValidatorAgent(Agent):
    """Declarative spec for ``training_data_validator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "training_data_validator"
    state_model = TrainingDataValidatorState
    toolkit_factory = TrainingDataValidatorToolkit

    nodes = {
        "profile_data": profile_data,
        "check_labels": check_labels,
        "detect_poisoning": detect_poisoning,
        "analyze_bias": analyze_bias,
        "validate_provenance": validate_provenance,
        "report": report,
    }

    edges = [
        edge("validate_provenance", "report"),
        edge("report", END),
        # TODO: cond(...) from "profile_data" via _check_error()
        # TODO: cond(...) from "check_labels" via _check_error()
        # TODO: cond(...) from "detect_poisoning" via _check_error()
        # TODO: cond(...) from "analyze_bias" via _check_error()
    ]

    entry = "profile_data"
