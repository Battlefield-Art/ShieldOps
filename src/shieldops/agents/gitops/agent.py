# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the gitops agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.gitops.models import GitOpsState
from shieldops.agents.gitops.nodes import (
    apply_reconciliation,
    detect_drift,
    plan_reconciliation,
    verify_and_report,
)
from shieldops.agents.gitops.tools import GitOpsToolkit
from shieldops.agents.runtime import END, Agent, edge


class GitopsAgent(Agent):
    """Declarative spec for ``gitops`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "gitops"
    state_model = GitOpsState
    toolkit_factory = GitOpsToolkit

    nodes = {
        "detect_drift": detect_drift,
        "plan_reconciliation": plan_reconciliation,
        "apply_reconciliation": apply_reconciliation,
        "verify_and_report": verify_and_report,
    }

    edges = [
        edge("detect_drift", "plan_reconciliation"),
        edge("apply_reconciliation", "verify_and_report"),
        edge("verify_and_report", END),
        # TODO: cond(...) from "plan_reconciliation" via needs_approval()
    ]

    entry = "detect_drift"
