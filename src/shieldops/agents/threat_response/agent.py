# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the threat_response agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.threat_response.models import ThreatResponseState
from shieldops.agents.threat_response.nodes import (
    classify_threat,
    execute_containment,
    execute_eradication,
    generate_report,
    select_playbook,
    verify_remediation,
)
from shieldops.agents.threat_response.tools import ThreatResponseToolkit


class ThreatResponseAgent(Agent):
    """Declarative spec for ``threat_response`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "threat_response"
    state_model = ThreatResponseState
    toolkit_factory = ThreatResponseToolkit

    nodes = {
        "classify_threat": classify_threat,
        "select_playbook": select_playbook,
        "execute_containment": execute_containment,
        "execute_eradication": execute_eradication,
        "verify_remediation": verify_remediation,
        "report": generate_report,
    }

    edges = [
        edge("classify_threat", "select_playbook"),
        edge("select_playbook", "execute_containment"),
        edge("execute_containment", "execute_eradication"),
        edge("execute_eradication", "verify_remediation"),
        edge("verify_remediation", "report"),
        edge("report", END),
    ]

    entry = "classify_threat"
