# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_architecture_reviewer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_architecture_reviewer.models import SecurityArchitectureReviewerState
from shieldops.agents.security_architecture_reviewer.nodes import (
    analyze_components,
    collect_design,
    evaluate_controls,
    identify_risks,
    recommend,
    report,
)
from shieldops.agents.security_architecture_reviewer.tools import (
    SecurityArchitectureReviewerToolkit,
)


class SecurityArchitectureReviewerAgent(Agent):
    """Declarative spec for ``security_architecture_reviewer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_architecture_reviewer"
    state_model = SecurityArchitectureReviewerState
    toolkit_factory = SecurityArchitectureReviewerToolkit

    nodes = {
        "collect_design": collect_design,
        "analyze_components": analyze_components,
        "identify_risks": identify_risks,
        "evaluate_controls": evaluate_controls,
        "recommend": recommend,
        "report": report,
    }

    edges = [
        edge("recommend", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_design" via _check_error()
        # TODO: cond(...) from "analyze_components" via _check_error()
        # TODO: cond(...) from "identify_risks" via _check_error()
        # TODO: cond(...) from "evaluate_controls" via _check_error()
    ]

    entry = "collect_design"
