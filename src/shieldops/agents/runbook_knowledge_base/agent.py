# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the runbook_knowledge_base agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runbook_knowledge_base.models import RunbookKnowledgeBaseState
from shieldops.agents.runbook_knowledge_base.nodes import (
    build_search,
    extract_patterns,
    feedback,
    index_runbooks,
    recommend,
    report,
)
from shieldops.agents.runbook_knowledge_base.tools import RunbookKnowledgeBaseToolkit
from shieldops.agents.runtime import END, Agent, edge


class RunbookKnowledgeBaseAgent(Agent):
    """Declarative spec for ``runbook_knowledge_base`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "runbook_knowledge_base"
    state_model = RunbookKnowledgeBaseState
    toolkit_factory = RunbookKnowledgeBaseToolkit

    nodes = {
        "index_runbooks": index_runbooks,
        "extract_patterns": extract_patterns,
        "build_search": build_search,
        "recommend": recommend,
        "feedback": feedback,
        "report": report,
    }

    edges = [
        edge("feedback", "report"),
        edge("report", END),
        # TODO: cond(...) from "index_runbooks" via _check_error()
        # TODO: cond(...) from "extract_patterns" via _check_error()
        # TODO: cond(...) from "build_search" via _check_error()
        # TODO: cond(...) from "recommend" via _check_error()
    ]

    entry = "index_runbooks"
