# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the secrets_in_code_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.secrets_in_code_detector.models import SecretsInCodeDetectorState
from shieldops.agents.secrets_in_code_detector.nodes import (
    assess_exposure,
    discover_repositories,
    generate_report,
    prioritize_findings,
    scan_patterns,
    verify_secrets,
)
from shieldops.agents.secrets_in_code_detector.tools import SecretsInCodeDetectorToolkit


class SecretsInCodeDetectorAgent(Agent):
    """Declarative spec for ``secrets_in_code_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "secrets_in_code_detector"
    state_model = SecretsInCodeDetectorState
    toolkit_factory = SecretsInCodeDetectorToolkit

    nodes = {
        "discover_repositories": discover_repositories,
        "scan_patterns": scan_patterns,
        "verify_secrets": verify_secrets,
        "assess_exposure": assess_exposure,
        "prioritize": prioritize_findings,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_repositories", "scan_patterns"),
        edge("scan_patterns", "verify_secrets"),
        edge("verify_secrets", "assess_exposure"),
        edge("assess_exposure", "prioritize"),
        edge("prioritize", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "discover_repositories"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecretsInCodeDetectorRunner`` keep working via this alias; new code
# should use ``SecretsInCodeDetectorAgent`` directly through AgentRuntime.
SecretsInCodeDetectorRunner = SecretsInCodeDetectorAgent
