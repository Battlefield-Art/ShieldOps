# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_prediction_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_prediction_engine.models import IncidentPredictionEngineState
from shieldops.agents.incident_prediction_engine.nodes import (
    alert,
    collect_signals,
    extract_features,
    rank_predictions,
    report,
    run_models,
)
from shieldops.agents.incident_prediction_engine.tools import IncidentPredictionEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentPredictionEngineAgent(Agent):
    """Declarative spec for ``incident_prediction_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_prediction_engine"
    state_model = IncidentPredictionEngineState
    toolkit_factory = IncidentPredictionEngineToolkit

    nodes = {
        "collect_signals": collect_signals,
        "extract_features": extract_features,
        "run_models": run_models,
        "rank_predictions": rank_predictions,
        "alert": alert,
        "report": report,
    }

    edges = [
        edge("alert", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_signals" via _check_error()
        # TODO: cond(...) from "extract_features" via _check_error()
        # TODO: cond(...) from "run_models" via _check_error()
        # TODO: cond(...) from "rank_predictions" via _check_error()
    ]

    entry = "collect_signals"
