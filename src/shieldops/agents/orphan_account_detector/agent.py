# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the orphan_account_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.orphan_account_detector.models import OrphanAccountDetectorState
from shieldops.agents.orphan_account_detector.nodes import (
    classify_risk,
    cross_reference_hr,
    identify_orphans,
    remediate,
    report,
    scan_accounts,
)
from shieldops.agents.orphan_account_detector.tools import OrphanAccountDetectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class OrphanAccountDetectorAgent(Agent):
    """Declarative spec for ``orphan_account_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "orphan_account_detector"
    state_model = OrphanAccountDetectorState
    toolkit_factory = OrphanAccountDetectorToolkit

    nodes = {
        "scan_accounts": scan_accounts,
        "cross_reference_hr": cross_reference_hr,
        "identify_orphans": identify_orphans,
        "classify_risk": classify_risk,
        "remediate": remediate,
        "report": report,
    }

    edges = [
        edge("remediate", "report"),
        edge("report", END),
        # TODO: cond(...) from "scan_accounts" via _check_error()
        # TODO: cond(...) from "cross_reference_hr" via _check_error()
        # TODO: cond(...) from "identify_orphans" via _check_error()
        # TODO: cond(...) from "classify_risk" via _check_error()
    ]

    entry = "scan_accounts"
