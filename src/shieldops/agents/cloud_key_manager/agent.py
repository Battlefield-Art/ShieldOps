# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_key_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_key_manager.models import CloudKeyManagerState
from shieldops.agents.cloud_key_manager.nodes import (
    assess_risk,
    audit_rotation,
    check_usage,
    discover_keys,
    enforce_policy,
    generate_report,
)
from shieldops.agents.cloud_key_manager.tools import CloudKeyManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudKeyManagerAgent(Agent):
    """Declarative spec for ``cloud_key_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_key_manager"
    state_model = CloudKeyManagerState
    toolkit_factory = CloudKeyManagerToolkit

    nodes = {
        "discover_keys": discover_keys,
        "audit_rotation": audit_rotation,
        "check_usage": check_usage,
        "assess_risk": assess_risk,
        "enforce_policy": enforce_policy,
        "report": generate_report,
    }

    edges = [
        edge("discover_keys", "audit_rotation"),
        edge("audit_rotation", "check_usage"),
        edge("check_usage", "assess_risk"),
        edge("assess_risk", "enforce_policy"),
        edge("enforce_policy", "report"),
        edge("report", END),
    ]

    entry = "discover_keys"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudKeyManagerRunner`` keep working via this alias; new code
# should use ``CloudKeyManagerAgent`` directly through AgentRuntime.
CloudKeyManagerRunner = CloudKeyManagerAgent
