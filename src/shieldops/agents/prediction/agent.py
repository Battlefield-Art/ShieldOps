# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the prediction agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.prediction.models import PredictionState
from shieldops.agents.prediction.nodes import (
    assess_risk,
    collect_baselines,
    correlate_changes,
    detect_trends,
    generate_predictions,
)
from shieldops.agents.prediction.tools import PredictionToolkit
from shieldops.agents.runtime import END, Agent, edge


class PredictionAgent(Agent):
    """Declarative spec for ``prediction`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "prediction"
    state_model = PredictionState
    toolkit_factory = PredictionToolkit

    nodes = {
        "collect_baselines": collect_baselines,
        "detect_trends": detect_trends,
        "correlate_changes": correlate_changes,
        "assess_risk": assess_risk,
        "generate_predictions": generate_predictions,
    }

    edges = [
        edge("collect_baselines", "detect_trends"),
        edge("detect_trends", "correlate_changes"),
        edge("correlate_changes", "assess_risk"),
        edge("generate_predictions", END),
        # TODO: conditional edge from "assess_risk"
        #   routed by should_generate() — wrap with cond(...)
    ]

    entry = "collect_baselines"
