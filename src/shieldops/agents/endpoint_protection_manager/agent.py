# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the endpoint_protection_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.endpoint_protection_manager.models import EndpointProtectionManagerState
from shieldops.agents.endpoint_protection_manager.nodes import (
    assess_patches,
    check_agents,
    generate_report,
    inventory_endpoints,
    remediate_gaps,
    scan_malware,
)
from shieldops.agents.endpoint_protection_manager.tools import EndpointProtectionManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class EndpointProtectionManagerAgent(Agent):
    """Declarative spec for ``endpoint_protection_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "endpoint_protection_manager"
    state_model = EndpointProtectionManagerState
    toolkit_factory = EndpointProtectionManagerToolkit

    nodes = {
        "inventory_endpoints": inventory_endpoints,
        "check_agents": check_agents,
        "assess_patches": assess_patches,
        "scan_malware": scan_malware,
        "remediate_gaps": remediate_gaps,
        "report": generate_report,
    }

    edges = [
        edge("inventory_endpoints", "check_agents"),
        edge("check_agents", "assess_patches"),
        edge("assess_patches", "scan_malware"),
        edge("scan_malware", "remediate_gaps"),
        edge("remediate_gaps", "report"),
        edge("report", END),
    ]

    entry = "inventory_endpoints"
