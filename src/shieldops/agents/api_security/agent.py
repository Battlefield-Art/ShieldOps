# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the api_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.api_security.models import APISecurityState
from shieldops.agents.api_security.nodes import (
    analyze_traffic,
    detect_abuse,
    detect_vulnerabilities,
    discover_endpoints,
    enforce_policies,
    generate_report,
)
from shieldops.agents.api_security.tools import APISecurityToolkit
from shieldops.agents.runtime import END, Agent, edge


class ApiSecurityAgent(Agent):
    """Declarative spec for ``api_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "api_security"
    state_model = APISecurityState
    toolkit_factory = APISecurityToolkit

    nodes = {
        "discover_endpoints": discover_endpoints,
        "analyze_traffic": analyze_traffic,
        "detect_vulnerabilities": detect_vulnerabilities,
        "detect_abuse": detect_abuse,
        "enforce_policies": enforce_policies,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_endpoints", "analyze_traffic"),
        edge("analyze_traffic", "detect_vulnerabilities"),
        edge("detect_vulnerabilities", "detect_abuse"),
        edge("enforce_policies", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_abuse"
        #   routed by _needs_enforcement() — wrap with cond(...)
    ]

    entry = "discover_endpoints"
