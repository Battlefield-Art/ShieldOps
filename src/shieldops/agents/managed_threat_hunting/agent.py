# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the managed_threat_hunting agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.managed_threat_hunting.models import ManagedThreatHuntingState
from shieldops.agents.managed_threat_hunting.nodes import (
    analyze_findings,
    collect_telemetry,
    escalate_threats,
    execute_hunts,
    generate_hunt_leads,
    report,
)
from shieldops.agents.managed_threat_hunting.tools import ManagedThreatHuntingToolkit
from shieldops.agents.runtime import END, Agent, edge


class ManagedThreatHuntingAgent(Agent):
    """Declarative spec for ``managed_threat_hunting`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "managed_threat_hunting"
    state_model = ManagedThreatHuntingState
    toolkit_factory = ManagedThreatHuntingToolkit

    nodes = {
        "generate_hunt_leads": generate_hunt_leads,
        "collect_telemetry": collect_telemetry,
        "execute_hunts": execute_hunts,
        "analyze_findings": analyze_findings,
        "escalate_threats": escalate_threats,
        "report": report,
    }

    edges = [
        edge("generate_hunt_leads", "collect_telemetry"),
        edge("collect_telemetry", "execute_hunts"),
        edge("execute_hunts", "analyze_findings"),
        edge("escalate_threats", "report"),
        edge("report", END),
        # TODO: conditional edge from "analyze_findings"
        #   routed by should_escalate() — wrap with cond(...)
    ]

    entry = "generate_hunt_leads"
