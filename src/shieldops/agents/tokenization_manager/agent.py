# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the tokenization_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.tokenization_manager.models import TokenizationManagerState
from shieldops.agents.tokenization_manager.nodes import (
    discover_fields,
    generate_tokens,
    map_vault,
    report,
    rotate,
    validate_integrity,
)
from shieldops.agents.tokenization_manager.tools import TokenizationManagerToolkit


class TokenizationManagerAgent(Agent):
    """Declarative spec for ``tokenization_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "tokenization_manager"
    state_model = TokenizationManagerState
    toolkit_factory = TokenizationManagerToolkit

    nodes = {
        "discover_fields": discover_fields,
        "generate_tokens": generate_tokens,
        "map_vault": map_vault,
        "validate_integrity": validate_integrity,
        "rotate": rotate,
        "report": report,
    }

    edges = [
        edge("rotate", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_fields" via _check_error()
        # TODO: cond(...) from "generate_tokens" via _check_error()
        # TODO: cond(...) from "map_vault" via _check_error()
        # TODO: cond(...) from "validate_integrity" via _check_error()
    ]

    entry = "discover_fields"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``TokenizationManagerRunner`` keep working via this alias; new code
# should use ``TokenizationManagerAgent`` directly through AgentRuntime.
TokenizationManagerRunner = TokenizationManagerAgent
