# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the social_engineering_detector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.social_engineering_detector.models import SocialEngineeringDetectorState
from shieldops.agents.social_engineering_detector.nodes import (
    analyze_patterns,
    assess_risk,
    classify_attack,
    collect_signals,
    recommend,
    report,
)
from shieldops.agents.social_engineering_detector.tools import SocialEngineeringDetectorToolkit


class SocialEngineeringDetectorAgent(Agent):
    """Declarative spec for ``social_engineering_detector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "social_engineering_detector"
    state_model = SocialEngineeringDetectorState
    toolkit_factory = SocialEngineeringDetectorToolkit

    nodes = {
        "collect_signals": collect_signals,
        "analyze_patterns": analyze_patterns,
        "classify_attack": classify_attack,
        "assess_risk": assess_risk,
        "recommend": recommend,
        "report": report,
    }

    edges = [
        edge("recommend", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_signals" via _check_error()
        # TODO: cond(...) from "analyze_patterns" via _check_error()
        # TODO: cond(...) from "classify_attack" via _check_error()
        # TODO: cond(...) from "assess_risk" via _check_error()
    ]

    entry = "collect_signals"
