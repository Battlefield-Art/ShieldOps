# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the dependency_graph_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.dependency_graph_analyzer.models import DependencyGraphAnalyzerState
from shieldops.agents.dependency_graph_analyzer.nodes import (
    analyze_depth,
    build_graph,
    detect_cycles,
    find_bottlenecks,
    report,
    score,
)
from shieldops.agents.dependency_graph_analyzer.tools import DependencyGraphAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class DependencyGraphAnalyzerAgent(Agent):
    """Declarative spec for ``dependency_graph_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "dependency_graph_analyzer"
    state_model = DependencyGraphAnalyzerState
    toolkit_factory = DependencyGraphAnalyzerToolkit

    nodes = {
        "build_graph": build_graph,
        "analyze_depth": analyze_depth,
        "find_bottlenecks": find_bottlenecks,
        "detect_cycles": detect_cycles,
        "score": score,
        "report": report,
    }

    edges = [
        edge("score", "report"),
        edge("report", END),
        # TODO: cond(...) from "build_graph" via _check_error()
        # TODO: cond(...) from "analyze_depth" via _check_error()
        # TODO: cond(...) from "find_bottlenecks" via _check_error()
        # TODO: cond(...) from "detect_cycles" via _check_error()
    ]

    entry = "build_graph"
