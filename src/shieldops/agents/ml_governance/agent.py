# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ml_governance agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ml_governance.models import MLGovernanceState
from shieldops.agents.ml_governance.nodes import (
    assess_risk,
    audit_models,
    evaluate_fairness,
    finalize_evaluation,
    plan_actions,
)
from shieldops.agents.ml_governance.tools import MLGovernanceToolkit
from shieldops.agents.runtime import END, Agent, edge


class MlGovernanceAgent(Agent):
    """Declarative spec for ``ml_governance`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ml_governance"
    state_model = MLGovernanceState
    toolkit_factory = MLGovernanceToolkit

    nodes = {
        "audit_models": audit_models,
        "evaluate_fairness": evaluate_fairness,
        "assess_risk": assess_risk,
        "plan_actions": plan_actions,
        "finalize_evaluation": finalize_evaluation,
    }

    edges = [
        edge("evaluate_fairness", "assess_risk"),
        edge("plan_actions", "finalize_evaluation"),
        edge("finalize_evaluation", END),
        # TODO: conditional edge from "audit_models"
        #   routed by should_evaluate() — wrap with cond(...)
        # TODO: conditional edge from "assess_risk"
        #   routed by should_act() — wrap with cond(...)
    ]

    entry = "audit_models"
