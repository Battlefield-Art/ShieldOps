# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the api_rate_limiter agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.api_rate_limiter.models import APIRateLimiterState
from shieldops.agents.api_rate_limiter.nodes import (
    classify_threats,
    detect_abuse,
    enforce_limits,
    generate_report,
    ingest_requests,
    profile_clients,
)
from shieldops.agents.api_rate_limiter.tools import APIRateLimiterToolkit
from shieldops.agents.runtime import END, Agent, edge


class ApiRateLimiterAgent(Agent):
    """Declarative spec for ``api_rate_limiter`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "api_rate_limiter"
    state_model = APIRateLimiterState
    toolkit_factory = APIRateLimiterToolkit

    nodes = {
        "ingest_requests": ingest_requests,
        "profile_clients": profile_clients,
        "detect_abuse": detect_abuse,
        "classify_threats": classify_threats,
        "enforce_limits": enforce_limits,
        "generate_report": generate_report,
    }

    edges = [
        edge("ingest_requests", "profile_clients"),
        edge("profile_clients", "detect_abuse"),
        edge("classify_threats", "enforce_limits"),
        edge("enforce_limits", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_abuse"
        #   routed by _has_abuse() — wrap with cond(...)
    ]

    entry = "ingest_requests"
