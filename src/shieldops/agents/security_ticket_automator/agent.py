# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_ticket_automator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_ticket_automator.models import SecurityTicketAutomatorState
from shieldops.agents.security_ticket_automator.nodes import (
    assign_owner,
    create_ticket,
    detect_issue,
    enrich_context,
    generate_report,
    track_sla,
)
from shieldops.agents.security_ticket_automator.tools import SecurityTicketAutomatorToolkit


class SecurityTicketAutomatorAgent(Agent):
    """Declarative spec for ``security_ticket_automator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_ticket_automator"
    state_model = SecurityTicketAutomatorState
    toolkit_factory = SecurityTicketAutomatorToolkit

    nodes = {
        "detect_issue": detect_issue,
        "enrich_context": enrich_context,
        "create_ticket": create_ticket,
        "assign_owner": assign_owner,
        "track_sla": track_sla,
        "generate_report": generate_report,
    }

    edges = [
        edge("detect_issue", "enrich_context"),
        edge("enrich_context", "create_ticket"),
        edge("create_ticket", "assign_owner"),
        edge("track_sla", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assign_owner"
        #   routed by _should_track_sla() — wrap with cond(...)
    ]

    entry = "detect_issue"
