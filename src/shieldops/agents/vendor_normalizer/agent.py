# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the vendor_normalizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.vendor_normalizer.models import VendorNormalizerState
from shieldops.agents.vendor_normalizer.nodes import (
    detect_schema,
    emit_unified,
    enrich_context,
    ingest_telemetry,
    map_to_ocsf,
    validate_normalization,
)
from shieldops.agents.vendor_normalizer.tools import VendorNormalizerToolkit


class VendorNormalizerAgent(Agent):
    """Declarative spec for ``vendor_normalizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "vendor_normalizer"
    state_model = VendorNormalizerState
    toolkit_factory = VendorNormalizerToolkit

    nodes = {
        "ingest_telemetry": ingest_telemetry,
        "detect_schema": detect_schema,
        "map_to_ocsf": map_to_ocsf,
        "validate_normalization": validate_normalization,
        "enrich_context": enrich_context,
        "emit_unified": emit_unified,
    }

    edges = [
        edge("ingest_telemetry", "detect_schema"),
        edge("detect_schema", "map_to_ocsf"),
        edge("map_to_ocsf", "validate_normalization"),
        edge("enrich_context", "emit_unified"),
        edge("emit_unified", END),
        # TODO: conditional edge from "validate_normalization"
        #   routed by _validation_router() — wrap with cond(...)
    ]

    entry = "ingest_telemetry"
