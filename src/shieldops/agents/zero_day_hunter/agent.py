# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the zero_day_hunter agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.zero_day_hunter.models import ZeroDayHunterState
from shieldops.agents.zero_day_hunter.nodes import (
    analyze_exploits,
    assess_exposure,
    deploy_mitigations,
    develop_signatures,
    generate_report,
    monitor_feeds,
)
from shieldops.agents.zero_day_hunter.tools import ZeroDayHunterToolkit


class ZeroDayHunterAgent(Agent):
    """Declarative spec for ``zero_day_hunter`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "zero_day_hunter"
    state_model = ZeroDayHunterState
    toolkit_factory = ZeroDayHunterToolkit

    nodes = {
        "monitor_feeds": monitor_feeds,
        "analyze_exploits": analyze_exploits,
        "assess_exposure": assess_exposure,
        "develop_signatures": develop_signatures,
        "deploy_mitigations": deploy_mitigations,
        "generate_report": generate_report,
    }

    edges = [
        edge("monitor_feeds", "analyze_exploits"),
        edge("analyze_exploits", "assess_exposure"),
        edge("assess_exposure", "develop_signatures"),
        edge("deploy_mitigations", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "develop_signatures"
        #   routed by _should_mitigate() — wrap with cond(...)
    ]

    entry = "monitor_feeds"
