# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_telemetry_aggregator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_telemetry_aggregator.models import SecurityTelemetryAggregatorState
from shieldops.agents.security_telemetry_aggregator.nodes import (
    collect_telemetry,
    correlate_events,
    enrich_context,
    generate_report,
    normalize_signals,
    route_alerts,
)
from shieldops.agents.security_telemetry_aggregator.tools import SecurityTelemetryAggregatorToolkit


class SecurityTelemetryAggregatorAgent(Agent):
    """Declarative spec for ``security_telemetry_aggregator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_telemetry_aggregator"
    state_model = SecurityTelemetryAggregatorState
    toolkit_factory = SecurityTelemetryAggregatorToolkit

    nodes = {
        "collect_telemetry": collect_telemetry,
        "normalize_signals": normalize_signals,
        "correlate_events": correlate_events,
        "enrich_context": enrich_context,
        "route_alerts": route_alerts,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_telemetry", "normalize_signals"),
        edge("enrich_context", "route_alerts"),
        edge("route_alerts", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "normalize_signals" via _should_correlate()
        # TODO: cond(...) from "correlate_events" via _should_enrich()
    ]

    entry = "collect_telemetry"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityTelemetryAggregatorRunner`` keep working via this alias; new code
# should use ``SecurityTelemetryAggregatorAgent`` directly through AgentRuntime.
SecurityTelemetryAggregatorRunner = SecurityTelemetryAggregatorAgent
