# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the privacy_rights_automator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.privacy_rights_automator.models import PrivacyRightsAutomatorState
from shieldops.agents.privacy_rights_automator.nodes import (
    classify_pii,
    generate_report,
    locate_data,
    process_action,
    receive_request,
    verify_completion,
)
from shieldops.agents.privacy_rights_automator.tools import PrivacyRightsAutomatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class PrivacyRightsAutomatorAgent(Agent):
    """Declarative spec for ``privacy_rights_automator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "privacy_rights_automator"
    state_model = PrivacyRightsAutomatorState
    toolkit_factory = PrivacyRightsAutomatorToolkit

    nodes = {
        "receive_request": receive_request,
        "locate_data": locate_data,
        "classify_pii": classify_pii,
        "process_action": process_action,
        "verify_completion": verify_completion,
        "generate_report": generate_report,
    }

    edges = [
        edge("receive_request", "locate_data"),
        edge("locate_data", "classify_pii"),
        edge("process_action", "verify_completion"),
        edge("verify_completion", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "classify_pii"
        #   routed by _should_process() — wrap with cond(...)
    ]

    entry = "receive_request"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``PrivacyRightsAutomatorRunner`` keep working via this alias; new code
# should use ``PrivacyRightsAutomatorAgent`` directly through AgentRuntime.
PrivacyRightsAutomatorRunner = PrivacyRightsAutomatorAgent
