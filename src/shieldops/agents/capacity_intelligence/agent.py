# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the capacity_intelligence agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.capacity_intelligence.models import CapacityIntelligenceState
from shieldops.agents.capacity_intelligence.nodes import (
    collect_utilization,
    forecast_demand,
    identify_bottlenecks,
    optimize_resources,
    plan_scaling,
    report,
)
from shieldops.agents.capacity_intelligence.tools import CapacityIntelligenceToolkit
from shieldops.agents.runtime import END, Agent, edge


class CapacityIntelligenceAgent(Agent):
    """Declarative spec for ``capacity_intelligence`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "capacity_intelligence"
    state_model = CapacityIntelligenceState
    toolkit_factory = CapacityIntelligenceToolkit

    nodes = {
        "collect_utilization": collect_utilization,
        "forecast_demand": forecast_demand,
        "identify_bottlenecks": identify_bottlenecks,
        "optimize_resources": optimize_resources,
        "plan_scaling": plan_scaling,
        "report": report,
    }

    edges = [
        edge("plan_scaling", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_utilization" via _check_error()
        # TODO: cond(...) from "forecast_demand" via _check_error()
        # TODO: cond(...) from "identify_bottlenecks" via _check_error()
        # TODO: cond(...) from "optimize_resources" via _check_error()
    ]

    entry = "collect_utilization"
