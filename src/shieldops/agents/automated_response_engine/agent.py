# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the automated_response_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.automated_response_engine.models import AutomatedResponseEngineState
from shieldops.agents.automated_response_engine.nodes import (
    assess_incident,
    execute_actions,
    generate_report,
    plan_remediation,
    select_playbook,
    validate_response,
)
from shieldops.agents.automated_response_engine.tools import AutomatedResponseEngineToolkit
from shieldops.agents.runtime import END, Agent, edge


class AutomatedResponseEngineAgent(Agent):
    """Declarative spec for ``automated_response_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "automated_response_engine"
    state_model = AutomatedResponseEngineState
    toolkit_factory = AutomatedResponseEngineToolkit

    nodes = {
        "assess_incident": assess_incident,
        "select_playbook": select_playbook,
        "plan_remediation": plan_remediation,
        "execute_actions": execute_actions,
        "validate_response": validate_response,
        "generate_report": generate_report,
    }

    edges = [
        edge("assess_incident", "select_playbook"),
        edge("plan_remediation", "execute_actions"),
        edge("validate_response", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "select_playbook" via _should_plan()
        # TODO: cond(...) from "execute_actions" via _should_validate()
    ]

    entry = "assess_incident"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AutomatedResponseEngineRunner`` keep working via this alias; new code
# should use ``AutomatedResponseEngineAgent`` directly through AgentRuntime.
AutomatedResponseEngineRunner = AutomatedResponseEngineAgent
