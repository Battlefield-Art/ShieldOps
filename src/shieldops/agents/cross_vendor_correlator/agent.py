# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cross_vendor_correlator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cross_vendor_correlator.models import CrossVendorCorrelatorState
from shieldops.agents.cross_vendor_correlator.nodes import (
    build_kill_chain,
    correlate_by_entity,
    create_situations,
    generate_report,
    ingest_vendor_alerts,
    normalize_to_ocsf,
)
from shieldops.agents.cross_vendor_correlator.tools import CrossVendorCorrelatorToolkit
from shieldops.agents.runtime import END, Agent, edge


class CrossVendorCorrelatorAgent(Agent):
    """Declarative spec for ``cross_vendor_correlator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cross_vendor_correlator"
    state_model = CrossVendorCorrelatorState
    toolkit_factory = CrossVendorCorrelatorToolkit

    nodes = {
        "ingest_vendor_alerts": ingest_vendor_alerts,
        "normalize_to_ocsf": normalize_to_ocsf,
        "correlate_by_entity": correlate_by_entity,
        "build_kill_chain": build_kill_chain,
        "create_situations": create_situations,
        "generate_report": generate_report,
    }

    edges = [
        edge("normalize_to_ocsf", "correlate_by_entity"),
        edge("build_kill_chain", "create_situations"),
        edge("create_situations", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "ingest_vendor_alerts"
        #   routed by _has_alerts() — wrap with cond(...)
        # TODO: conditional edge from "correlate_by_entity"
        #   routed by _has_correlations() — wrap with cond(...)
    ]

    entry = "ingest_vendor_alerts"
