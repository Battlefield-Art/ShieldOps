# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the email_gateway_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.email_gateway_analyzer.models import GatewayAnalyzerState
from shieldops.agents.email_gateway_analyzer.nodes import (
    analyze_headers,
    check_reputation,
    collect_records,
    detect_spoofing,
    generate_report,
    validate_auth,
)
from shieldops.agents.email_gateway_analyzer.tools import EmailGatewayAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class EmailGatewayAnalyzerAgent(Agent):
    """Declarative spec for ``email_gateway_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "email_gateway_analyzer"
    state_model = GatewayAnalyzerState
    toolkit_factory = EmailGatewayAnalyzerToolkit

    nodes = {
        "collect_records": collect_records,
        "validate_auth": validate_auth,
        "analyze_headers": analyze_headers,
        "check_reputation": check_reputation,
        "detect_spoofing": detect_spoofing,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_records", "validate_auth"),
        edge("validate_auth", "analyze_headers"),
        edge("analyze_headers", "check_reputation"),
        edge("check_reputation", "detect_spoofing"),
        edge("detect_spoofing", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "collect_records"
