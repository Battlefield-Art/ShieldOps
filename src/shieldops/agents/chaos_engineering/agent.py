# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the chaos_engineering agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.chaos_engineering.models import ChaosEngineeringState
from shieldops.agents.chaos_engineering.nodes import (
    abort_experiment,
    analyze_results,
    generate_report,
    inject_fault,
    observe_impact,
    plan_experiment,
    validate_safety,
)
from shieldops.agents.chaos_engineering.tools import ChaosEngineeringToolkit
from shieldops.agents.runtime import END, Agent, edge


class ChaosEngineeringAgent(Agent):
    """Declarative spec for ``chaos_engineering`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "chaos_engineering"
    state_model = ChaosEngineeringState
    toolkit_factory = ChaosEngineeringToolkit

    nodes = {
        "plan_experiment": plan_experiment,
        "validate_safety": validate_safety,
        "inject_fault": inject_fault,
        "observe_impact": observe_impact,
        "analyze_results": analyze_results,
        "generate_report": generate_report,
        "abort_experiment": abort_experiment,
    }

    edges = [
        edge("plan_experiment", "validate_safety"),
        edge("inject_fault", "observe_impact"),
        edge("observe_impact", "analyze_results"),
        edge("analyze_results", "generate_report"),
        edge("abort_experiment", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "validate_safety"
        #   routed by route_after_safety() — wrap with cond(...)
    ]

    entry = "plan_experiment"
