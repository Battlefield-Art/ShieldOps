# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the auto_ticket_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.auto_ticket_manager.models import AutoTicketManagerState
from shieldops.agents.auto_ticket_manager.nodes import (
    assign_owners,
    classify_tickets,
    create_tickets,
    generate_report,
    receive_findings,
    track_sla,
)
from shieldops.agents.auto_ticket_manager.tools import AutoTicketManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AutoTicketManagerAgent(Agent):
    """Declarative spec for ``auto_ticket_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "auto_ticket_manager"
    state_model = AutoTicketManagerState
    toolkit_factory = AutoTicketManagerToolkit

    nodes = {
        "receive_findings": receive_findings,
        "classify_tickets": classify_tickets,
        "create_tickets": create_tickets,
        "assign_owners": assign_owners,
        "track_sla": track_sla,
        "generate_report": generate_report,
    }

    edges = [
        edge("classify_tickets", "create_tickets"),
        edge("create_tickets", "assign_owners"),
        edge("assign_owners", "track_sla"),
        edge("track_sla", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "receive_findings"
        #   routed by _has_findings() — wrap with cond(...)
    ]

    entry = "receive_findings"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AutoTicketManagerRunner`` keep working via this alias; new code
# should use ``AutoTicketManagerAgent`` directly through AgentRuntime.
AutoTicketManagerRunner = AutoTicketManagerAgent
