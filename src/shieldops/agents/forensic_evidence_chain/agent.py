# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the forensic_evidence_chain agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.forensic_evidence_chain.models import ForensicEvidenceChainState
from shieldops.agents.forensic_evidence_chain.nodes import (
    chain_custody,
    collect_evidence,
    generate_report,
    hash_artifacts,
    package_for_legal,
    validate_integrity,
)
from shieldops.agents.forensic_evidence_chain.tools import ForensicEvidenceChainToolkit
from shieldops.agents.runtime import END, Agent, edge


class ForensicEvidenceChainAgent(Agent):
    """Declarative spec for ``forensic_evidence_chain`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "forensic_evidence_chain"
    state_model = ForensicEvidenceChainState
    toolkit_factory = ForensicEvidenceChainToolkit

    nodes = {
        "collect_evidence": collect_evidence,
        "hash_artifacts": hash_artifacts,
        "chain_custody": chain_custody,
        "validate_integrity": validate_integrity,
        "package_for_legal": package_for_legal,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_evidence", "hash_artifacts"),
        edge("chain_custody", "validate_integrity"),
        edge("package_for_legal", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "hash_artifacts" via _should_chain_custody()
        # TODO: cond(...) from "validate_integrity" via _should_package()
    ]

    entry = "collect_evidence"
