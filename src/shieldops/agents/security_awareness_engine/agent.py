# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_awareness_engine agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_awareness_engine.models import SecurityAwarenessEngineState
from shieldops.agents.security_awareness_engine.nodes import (
    analyze_phishing,
    assess_baseline,
    evaluate_training,
    generate_plan,
    generate_report,
    identify_risks,
)
from shieldops.agents.security_awareness_engine.tools import SecurityAwarenessEngineToolkit


class SecurityAwarenessEngineAgent(Agent):
    """Declarative spec for ``security_awareness_engine`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_awareness_engine"
    state_model = SecurityAwarenessEngineState
    toolkit_factory = SecurityAwarenessEngineToolkit

    nodes = {
        "assess_baseline": assess_baseline,
        "analyze_phishing": analyze_phishing,
        "evaluate_training": evaluate_training,
        "identify_risks": identify_risks,
        "generate_plan": generate_plan,
        "report": generate_report,
    }

    edges = [
        edge("assess_baseline", "analyze_phishing"),
        edge("analyze_phishing", "evaluate_training"),
        edge("evaluate_training", "identify_risks"),
        edge("identify_risks", "generate_plan"),
        edge("generate_plan", "report"),
        edge("report", END),
    ]

    entry = "assess_baseline"
