# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_permission_optimizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_permission_optimizer.models import CloudPermissionOptimizerState
from shieldops.agents.cloud_permission_optimizer.nodes import (
    analyze_usage,
    calculate_least_privilege,
    collect_permissions,
    detect_excess,
    generate_report,
    recommend_changes,
)
from shieldops.agents.cloud_permission_optimizer.tools import CloudPermissionOptimizerToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudPermissionOptimizerAgent(Agent):
    """Declarative spec for ``cloud_permission_optimizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_permission_optimizer"
    state_model = CloudPermissionOptimizerState
    toolkit_factory = CloudPermissionOptimizerToolkit

    nodes = {
        "collect_permissions": collect_permissions,
        "analyze_usage": analyze_usage,
        "detect_excess": detect_excess,
        "calculate_least_privilege": calculate_least_privilege,
        "recommend_changes": recommend_changes,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_permissions", "analyze_usage"),
        edge("analyze_usage", "detect_excess"),
        edge("detect_excess", "calculate_least_privilege"),
        edge("recommend_changes", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "calculate_least_privilege"
        #   routed by _should_recommend() — wrap with cond(...)
    ]

    entry = "collect_permissions"
