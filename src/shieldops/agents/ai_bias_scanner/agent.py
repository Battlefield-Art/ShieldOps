# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the ai_bias_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.ai_bias_scanner.models import AIBiasScannerState
from shieldops.agents.ai_bias_scanner.nodes import (
    assess_fairness,
    collect_data,
    compute_metrics,
    identify_groups,
    recommend,
    report,
)
from shieldops.agents.ai_bias_scanner.tools import AIBiasScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AiBiasScannerAgent(Agent):
    """Declarative spec for ``ai_bias_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "ai_bias_scanner"
    state_model = AIBiasScannerState
    toolkit_factory = AIBiasScannerToolkit

    nodes = {
        "collect_data": collect_data,
        "identify_groups": identify_groups,
        "compute_metrics": compute_metrics,
        "assess_fairness": assess_fairness,
        "recommend": recommend,
        "report": report,
    }

    edges = [
        edge("recommend", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_data" via _check_error()
        # TODO: cond(...) from "identify_groups" via _check_error()
        # TODO: cond(...) from "compute_metrics" via _check_error()
        # TODO: cond(...) from "assess_fairness" via _check_error()
    ]

    entry = "collect_data"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AIBiasScannerRunner`` keep working via this alias; new code
# should use ``AiBiasScannerAgent`` directly through AgentRuntime.
AIBiasScannerRunner = AiBiasScannerAgent
