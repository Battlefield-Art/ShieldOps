# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the remediation_orchestrator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.remediation_orchestrator.models import RemediationOrchestratorState
from shieldops.agents.remediation_orchestrator.nodes import (
    classify_and_route,
    create_tickets,
    dispatch_remediation,
    generate_report,
    receive_findings,
    track_progress,
)
from shieldops.agents.remediation_orchestrator.tools import RemediationOrchestratorToolkit
from shieldops.agents.runtime import END, Agent, edge


class RemediationOrchestratorAgent(Agent):
    """Declarative spec for ``remediation_orchestrator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "remediation_orchestrator"
    state_model = RemediationOrchestratorState
    toolkit_factory = RemediationOrchestratorToolkit

    nodes = {
        "receive_findings": receive_findings,
        "classify_and_route": classify_and_route,
        "create_tickets": create_tickets,
        "dispatch_remediation": dispatch_remediation,
        "track_progress": track_progress,
        "generate_report": generate_report,
    }

    edges = [
        edge("receive_findings", "classify_and_route"),
        edge("classify_and_route", "create_tickets"),
        edge("create_tickets", "dispatch_remediation"),
        edge("dispatch_remediation", "track_progress"),
        edge("track_progress", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "receive_findings"
