# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the deception_network_manager agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.deception_network_manager.models import DeceptionNetworkManagerState
from shieldops.agents.deception_network_manager.nodes import (
    analyze_behavior,
    classify_attacker,
    deploy_decoys,
    generate_intel,
    generate_report,
    monitor_interactions,
)
from shieldops.agents.deception_network_manager.tools import DeceptionNetworkManagerToolkit
from shieldops.agents.runtime import END, Agent, edge


class DeceptionNetworkManagerAgent(Agent):
    """Declarative spec for ``deception_network_manager`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "deception_network_manager"
    state_model = DeceptionNetworkManagerState
    toolkit_factory = DeceptionNetworkManagerToolkit

    nodes = {
        "deploy_decoys": deploy_decoys,
        "monitor_interactions": monitor_interactions,
        "analyze_behavior": analyze_behavior,
        "classify_attacker": classify_attacker,
        "generate_intel": generate_intel,
        "generate_report": generate_report,
    }

    edges = [
        edge("deploy_decoys", "monitor_interactions"),
        edge("monitor_interactions", "analyze_behavior"),
        edge("classify_attacker", "generate_intel"),
        edge("generate_intel", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "analyze_behavior"
        #   routed by _should_classify() — wrap with cond(...)
    ]

    entry = "deploy_decoys"
