# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_posture_scorer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_posture_scorer.models import SecurityPostureScorerState
from shieldops.agents.security_posture_scorer.nodes import (
    benchmark,
    calculate_scores,
    collect_signals,
    generate_report,
    trend_analysis,
    weight_categories,
)
from shieldops.agents.security_posture_scorer.tools import SecurityPostureScorerToolkit


class SecurityPostureScorerAgent(Agent):
    """Declarative spec for ``security_posture_scorer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_posture_scorer"
    state_model = SecurityPostureScorerState
    toolkit_factory = SecurityPostureScorerToolkit

    nodes = {
        "collect_signals": collect_signals,
        "weight_categories": weight_categories,
        "calculate_scores": calculate_scores,
        "benchmark": benchmark,
        "trend_analysis": trend_analysis,
        "report": generate_report,
    }

    edges = [
        edge("collect_signals", "weight_categories"),
        edge("weight_categories", "calculate_scores"),
        edge("calculate_scores", "benchmark"),
        edge("benchmark", "trend_analysis"),
        edge("trend_analysis", "report"),
        edge("report", END),
    ]

    entry = "collect_signals"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SecurityPostureScorerRunner`` keep working via this alias; new code
# should use ``SecurityPostureScorerAgent`` directly through AgentRuntime.
SecurityPostureScorerRunner = SecurityPostureScorerAgent
