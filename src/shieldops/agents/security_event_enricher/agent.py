# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_event_enricher agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_event_enricher.models import SecurityEventEnricherState
from shieldops.agents.security_event_enricher.nodes import (
    enrich_threat,
    generate_report,
    lookup_context,
    receive_events,
    route_events,
    score_priority,
)
from shieldops.agents.security_event_enricher.tools import SecurityEventEnricherToolkit


class SecurityEventEnricherAgent(Agent):
    """Declarative spec for ``security_event_enricher`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_event_enricher"
    state_model = SecurityEventEnricherState
    toolkit_factory = SecurityEventEnricherToolkit

    nodes = {
        "receive_events": receive_events,
        "lookup_context": lookup_context,
        "enrich_threat": enrich_threat,
        "score_priority": score_priority,
        "route_events": route_events,
        "generate_report": generate_report,
    }

    edges = [
        edge("receive_events", "lookup_context"),
        edge("lookup_context", "enrich_threat"),
        edge("enrich_threat", "score_priority"),
        edge("route_events", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "score_priority"
        #   routed by _should_route() — wrap with cond(...)
    ]

    entry = "receive_events"
