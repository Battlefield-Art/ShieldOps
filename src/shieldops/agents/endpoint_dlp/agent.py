# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the endpoint_dlp agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.endpoint_dlp.models import EndpointDLPState
from shieldops.agents.endpoint_dlp.nodes import (
    classify_sensitivity,
    detect_data_movement,
    enforce_policies,
    investigate_violations,
    monitor_endpoints,
    report,
)
from shieldops.agents.endpoint_dlp.tools import EndpointDLPToolkit
from shieldops.agents.runtime import END, Agent, edge


class EndpointDlpAgent(Agent):
    """Declarative spec for ``endpoint_dlp`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "endpoint_dlp"
    state_model = EndpointDLPState
    toolkit_factory = EndpointDLPToolkit

    nodes = {
        "monitor_endpoints": monitor_endpoints,
        "detect_data_movement": detect_data_movement,
        "classify_sensitivity": classify_sensitivity,
        "enforce_policies": enforce_policies,
        "investigate_violations": investigate_violations,
        "report": report,
    }

    edges = [
        edge("monitor_endpoints", "detect_data_movement"),
        edge("detect_data_movement", "classify_sensitivity"),
        edge("classify_sensitivity", "enforce_policies"),
        edge("enforce_policies", "investigate_violations"),
        edge("investigate_violations", "report"),
        edge("report", END),
    ]

    entry = "monitor_endpoints"
