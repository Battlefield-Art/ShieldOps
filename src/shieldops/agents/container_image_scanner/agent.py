# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the container_image_scanner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.container_image_scanner.models import ContainerImageScannerState
from shieldops.agents.container_image_scanner.nodes import (
    analyze_layers,
    check_compliance,
    discover_images,
    generate_report,
    prioritize_findings,
    scan_vulnerabilities,
)
from shieldops.agents.container_image_scanner.tools import ContainerImageScannerToolkit
from shieldops.agents.runtime import END, Agent, edge


class ContainerImageScannerAgent(Agent):
    """Declarative spec for ``container_image_scanner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "container_image_scanner"
    state_model = ContainerImageScannerState
    toolkit_factory = ContainerImageScannerToolkit

    nodes = {
        "discover_images": discover_images,
        "analyze_layers": analyze_layers,
        "scan_vulnerabilities": scan_vulnerabilities,
        "check_compliance": check_compliance,
        "prioritize": prioritize_findings,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_images", "analyze_layers"),
        edge("analyze_layers", "scan_vulnerabilities"),
        edge("scan_vulnerabilities", "check_compliance"),
        edge("check_compliance", "prioritize"),
        edge("prioritize", "generate_report"),
        edge("generate_report", END),
    ]

    entry = "discover_images"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ContainerImageScannerRunner`` keep working via this alias; new code
# should use ``ContainerImageScannerAgent`` directly through AgentRuntime.
ContainerImageScannerRunner = ContainerImageScannerAgent
