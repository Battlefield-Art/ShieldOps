# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_pipeline agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_pipeline.models import SecurityPipelineState
from shieldops.agents.security_pipeline.nodes import (
    collect_findings,
    dispatch_discovery,
    dispatch_pentest,
    dispatch_remediation,
    generate_report,
    plan_pipeline,
    verify_results,
)
from shieldops.agents.security_pipeline.tools import SecurityPipelineToolkit


class SecurityPipelineAgent(Agent):
    """Declarative spec for ``security_pipeline`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_pipeline"
    state_model = SecurityPipelineState
    toolkit_factory = SecurityPipelineToolkit

    nodes = {
        "plan_pipeline": plan_pipeline,
        "dispatch_discovery": dispatch_discovery,
        "dispatch_pentest": dispatch_pentest,
        "collect_findings": collect_findings,
        "dispatch_remediation": dispatch_remediation,
        "verify_results": verify_results,
        "generate_report": generate_report,
    }

    edges = [
        edge("plan_pipeline", "dispatch_discovery"),
        edge("dispatch_discovery", "dispatch_pentest"),
        edge("dispatch_pentest", "collect_findings"),
        edge("dispatch_remediation", "verify_results"),
        edge("verify_results", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "collect_findings"
        #   routed by _has_findings() — wrap with cond(...)
    ]

    entry = "plan_pipeline"
