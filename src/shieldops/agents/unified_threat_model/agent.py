# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the unified_threat_model agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.unified_threat_model.models import UnifiedThreatModelState
from shieldops.agents.unified_threat_model.nodes import (
    analyze_controls,
    calculate_risk,
    define_scope,
    generate_report,
    identify_threats,
    prioritize_mitigations,
)
from shieldops.agents.unified_threat_model.tools import UnifiedThreatModelToolkit


class UnifiedThreatModelAgent(Agent):
    """Declarative spec for ``unified_threat_model`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "unified_threat_model"
    state_model = UnifiedThreatModelState
    toolkit_factory = UnifiedThreatModelToolkit

    nodes = {
        "define_scope": define_scope,
        "identify_threats": identify_threats,
        "analyze_controls": analyze_controls,
        "calculate_risk": calculate_risk,
        "prioritize_mitigations": prioritize_mitigations,
        "generate_report": generate_report,
    }

    edges = [
        edge("identify_threats", "analyze_controls"),
        edge("analyze_controls", "calculate_risk"),
        edge("prioritize_mitigations", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "define_scope"
        #   routed by _should_identify() — wrap with cond(...)
        # TODO: conditional edge from "calculate_risk"
        #   routed by _should_prioritize() — wrap with cond(...)
    ]

    entry = "define_scope"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``UnifiedThreatModelRunner`` keep working via this alias; new code
# should use ``UnifiedThreatModelAgent`` directly through AgentRuntime.
UnifiedThreatModelRunner = UnifiedThreatModelAgent
