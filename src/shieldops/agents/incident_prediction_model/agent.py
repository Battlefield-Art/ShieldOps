# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the incident_prediction_model agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.incident_prediction_model.models import IncidentPredictionModelState
from shieldops.agents.incident_prediction_model.nodes import (
    analyze_patterns,
    assess_confidence,
    build_predictions,
    collect_signals,
    generate_report,
    recommend_preventions,
)
from shieldops.agents.incident_prediction_model.tools import IncidentPredictionModelToolkit
from shieldops.agents.runtime import END, Agent, edge


class IncidentPredictionModelAgent(Agent):
    """Declarative spec for ``incident_prediction_model`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "incident_prediction_model"
    state_model = IncidentPredictionModelState
    toolkit_factory = IncidentPredictionModelToolkit

    nodes = {
        "collect_signals": collect_signals,
        "analyze_patterns": analyze_patterns,
        "build_predictions": build_predictions,
        "assess_confidence": assess_confidence,
        "recommend_preventions": recommend_preventions,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_signals", "analyze_patterns"),
        edge("build_predictions", "assess_confidence"),
        edge("recommend_preventions", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "analyze_patterns" via _should_predict()
        # TODO: cond(...) from "assess_confidence" via _should_prevent()
    ]

    entry = "collect_signals"
