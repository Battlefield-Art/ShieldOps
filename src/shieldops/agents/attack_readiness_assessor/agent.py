# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the attack_readiness_assessor agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.attack_readiness_assessor.models import AttackReadinessAssessorState
from shieldops.agents.attack_readiness_assessor.nodes import (
    assess_detection,
    assess_prevention,
    assess_response,
    calculate_readiness,
    generate_report,
    select_scenarios,
)
from shieldops.agents.attack_readiness_assessor.tools import AttackReadinessAssessorToolkit
from shieldops.agents.runtime import END, Agent, edge


class AttackReadinessAssessorAgent(Agent):
    """Declarative spec for ``attack_readiness_assessor`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "attack_readiness_assessor"
    state_model = AttackReadinessAssessorState
    toolkit_factory = AttackReadinessAssessorToolkit

    nodes = {
        "select_scenarios": select_scenarios,
        "assess_prevention": assess_prevention,
        "assess_detection": assess_detection,
        "assess_response": assess_response,
        "calculate_readiness": calculate_readiness,
        "generate_report": generate_report,
    }

    edges = [
        edge("assess_prevention", "assess_detection"),
        edge("assess_detection", "assess_response"),
        edge("assess_response", "calculate_readiness"),
        edge("calculate_readiness", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "select_scenarios"
        #   routed by _has_scenarios() — wrap with cond(...)
    ]

    entry = "select_scenarios"
