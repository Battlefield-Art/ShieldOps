# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the soc_metrics_dashboard agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.soc_metrics_dashboard.models import SocMetricsDashboardState
from shieldops.agents.soc_metrics_dashboard.nodes import (
    benchmark,
    collect_data,
    compute_kpis,
    identify_trends,
    recommend,
    report,
)
from shieldops.agents.soc_metrics_dashboard.tools import SocMetricsDashboardToolkit


class SocMetricsDashboardAgent(Agent):
    """Declarative spec for ``soc_metrics_dashboard`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "soc_metrics_dashboard"
    state_model = SocMetricsDashboardState
    toolkit_factory = SocMetricsDashboardToolkit

    nodes = {
        "collect_data": collect_data,
        "compute_kpis": compute_kpis,
        "identify_trends": identify_trends,
        "benchmark": benchmark,
        "recommend": recommend,
        "report": report,
    }

    edges = [
        edge("recommend", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_data" via _check_error()
        # TODO: cond(...) from "compute_kpis" via _check_error()
        # TODO: cond(...) from "identify_trends" via _check_error()
        # TODO: cond(...) from "benchmark" via _check_error()
    ]

    entry = "collect_data"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``SocMetricsDashboardRunner`` keep working via this alias; new code
# should use ``SocMetricsDashboardAgent`` directly through AgentRuntime.
SocMetricsDashboardRunner = SocMetricsDashboardAgent
