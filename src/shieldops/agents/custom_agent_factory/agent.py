# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the custom_agent_factory agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.custom_agent_factory.models import CustomAgentFactoryState
from shieldops.agents.custom_agent_factory.nodes import (
    design_agent,
    generate_code,
    parse_requirements,
    register_agent,
    report,
    validate_agent,
)
from shieldops.agents.custom_agent_factory.tools import CustomAgentFactoryToolkit
from shieldops.agents.runtime import END, Agent, edge


class CustomAgentFactoryAgent(Agent):
    """Declarative spec for ``custom_agent_factory`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "custom_agent_factory"
    state_model = CustomAgentFactoryState
    toolkit_factory = CustomAgentFactoryToolkit

    nodes = {
        "parse_requirements": parse_requirements,
        "design_agent": design_agent,
        "generate_code": generate_code,
        "validate_agent": validate_agent,
        "register_agent": register_agent,
        "report": report,
    }

    edges = [
        edge("parse_requirements", "design_agent"),
        edge("design_agent", "generate_code"),
        edge("generate_code", "validate_agent"),
        edge("validate_agent", "register_agent"),
        edge("register_agent", "report"),
        edge("report", END),
    ]

    entry = "parse_requirements"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CustomAgentFactoryRunner`` keep working via this alias; new code
# should use ``CustomAgentFactoryAgent`` directly through AgentRuntime.
CustomAgentFactoryRunner = CustomAgentFactoryAgent
