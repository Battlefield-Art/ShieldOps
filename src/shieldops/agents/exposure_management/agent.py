# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the exposure_management agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.exposure_management.models import ExposureManagementState
from shieldops.agents.exposure_management.nodes import (
    assess_exposures,
    discover_attack_surface,
    enumerate_assets,
    prioritize_risks,
    recommend_remediation,
    report,
)
from shieldops.agents.exposure_management.tools import ExposureManagementToolkit
from shieldops.agents.runtime import END, Agent, edge


class ExposureManagementAgent(Agent):
    """Declarative spec for ``exposure_management`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "exposure_management"
    state_model = ExposureManagementState
    toolkit_factory = ExposureManagementToolkit

    nodes = {
        "discover_attack_surface": discover_attack_surface,
        "enumerate_assets": enumerate_assets,
        "assess_exposures": assess_exposures,
        "prioritize_risks": prioritize_risks,
        "recommend_remediation": recommend_remediation,
        "report": report,
    }

    edges = [
        edge("assess_exposures", "prioritize_risks"),
        edge("recommend_remediation", "report"),
        edge("report", END),
        # TODO: conditional edge from "discover_attack_surface"
        #   routed by should_enumerate() — wrap with cond(...)
        # TODO: conditional edge from "enumerate_assets"
        #   routed by should_assess() — wrap with cond(...)
        # TODO: conditional edge from "prioritize_risks"
        #   routed by should_remediate() — wrap with cond(...)
    ]

    entry = "discover_attack_surface"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``ExposureManagementRunner`` keep working via this alias; new code
# should use ``ExposureManagementAgent`` directly through AgentRuntime.
ExposureManagementRunner = ExposureManagementAgent
