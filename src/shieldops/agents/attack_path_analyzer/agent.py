# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the attack_path_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.attack_path_analyzer.models import AttackPathAnalyzerState
from shieldops.agents.attack_path_analyzer.nodes import (
    calculate_risk,
    discover_assets,
    identify_paths,
    map_relationships,
    recommend_mitigations,
    report,
)
from shieldops.agents.attack_path_analyzer.tools import AttackPathAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class AttackPathAnalyzerAgent(Agent):
    """Declarative spec for ``attack_path_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "attack_path_analyzer"
    state_model = AttackPathAnalyzerState
    toolkit_factory = AttackPathAnalyzerToolkit

    nodes = {
        "discover_assets": discover_assets,
        "map_relationships": map_relationships,
        "identify_paths": identify_paths,
        "calculate_risk": calculate_risk,
        "recommend_mitigations": recommend_mitigations,
        "report": report,
    }

    edges = [
        edge("recommend_mitigations", "report"),
        edge("report", END),
        # TODO: cond(...) from "discover_assets" via _check_error()
        # TODO: cond(...) from "map_relationships" via _check_error()
        # TODO: cond(...) from "identify_paths" via _check_error()
        # TODO: cond(...) from "calculate_risk" via _check_error()
    ]

    entry = "discover_assets"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``AttackPathAnalyzerRunner`` keep working via this alias; new code
# should use ``AttackPathAnalyzerAgent`` directly through AgentRuntime.
AttackPathAnalyzerRunner = AttackPathAnalyzerAgent
