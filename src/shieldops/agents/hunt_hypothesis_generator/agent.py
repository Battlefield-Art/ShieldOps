# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the hunt_hypothesis_generator agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.hunt_hypothesis_generator.models import HuntHypothesisGeneratorState
from shieldops.agents.hunt_hypothesis_generator.nodes import (
    analyze_intel,
    create_queries,
    generate_hypotheses,
    identify_gaps,
    prioritize,
    report,
)
from shieldops.agents.hunt_hypothesis_generator.tools import HuntHypothesisGeneratorToolkit
from shieldops.agents.runtime import END, Agent, edge


class HuntHypothesisGeneratorAgent(Agent):
    """Declarative spec for ``hunt_hypothesis_generator`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "hunt_hypothesis_generator"
    state_model = HuntHypothesisGeneratorState
    toolkit_factory = HuntHypothesisGeneratorToolkit

    nodes = {
        "analyze_intel": analyze_intel,
        "identify_gaps": identify_gaps,
        "generate_hypotheses": generate_hypotheses,
        "prioritize": prioritize,
        "create_queries": create_queries,
        "report": report,
    }

    edges = [
        edge("create_queries", "report"),
        edge("report", END),
        # TODO: cond(...) from "analyze_intel" via _check_error()
        # TODO: cond(...) from "identify_gaps" via _check_error()
        # TODO: cond(...) from "generate_hypotheses" via _check_error()
        # TODO: cond(...) from "prioritize" via _check_error()
    ]

    entry = "analyze_intel"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``HuntHypothesisGeneratorRunner`` keep working via this alias; new code
# should use ``HuntHypothesisGeneratorAgent`` directly through AgentRuntime.
HuntHypothesisGeneratorRunner = HuntHypothesisGeneratorAgent
