# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_database_protector agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_database_protector.models import CloudDatabaseProtectorState
from shieldops.agents.cloud_database_protector.nodes import (
    audit_access,
    check_encryption,
    detect_anomalies,
    discover_databases,
    enforce_policies,
    generate_report,
)
from shieldops.agents.cloud_database_protector.tools import CloudDatabaseProtectorToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudDatabaseProtectorAgent(Agent):
    """Declarative spec for ``cloud_database_protector`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_database_protector"
    state_model = CloudDatabaseProtectorState
    toolkit_factory = CloudDatabaseProtectorToolkit

    nodes = {
        "discover_databases": discover_databases,
        "audit_access": audit_access,
        "check_encryption": check_encryption,
        "detect_anomalies": detect_anomalies,
        "enforce_policies": enforce_policies,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_databases", "audit_access"),
        edge("audit_access", "check_encryption"),
        edge("check_encryption", "detect_anomalies"),
        edge("enforce_policies", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "detect_anomalies"
        #   routed by _should_enforce() — wrap with cond(...)
    ]

    entry = "discover_databases"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``CloudDatabaseProtectorRunner`` keep working via this alias; new code
# should use ``CloudDatabaseProtectorAgent`` directly through AgentRuntime.
CloudDatabaseProtectorRunner = CloudDatabaseProtectorAgent
