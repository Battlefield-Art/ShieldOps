# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the attack_surface agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.attack_surface.models import AttackSurfaceState
from shieldops.agents.attack_surface.nodes import (
    analyze_exposures,
    discover_assets,
    finalize_scan,
    plan_remediation,
    prioritize_findings,
)
from shieldops.agents.attack_surface.tools import AttackSurfaceToolkit
from shieldops.agents.runtime import END, Agent, edge


class AttackSurfaceAgent(Agent):
    """Declarative spec for ``attack_surface`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "attack_surface"
    state_model = AttackSurfaceState
    toolkit_factory = AttackSurfaceToolkit

    nodes = {
        "discover_assets": discover_assets,
        "analyze_exposures": analyze_exposures,
        "prioritize_findings": prioritize_findings,
        "plan_remediation": plan_remediation,
        "finalize_scan": finalize_scan,
    }

    edges = [
        edge("analyze_exposures", "prioritize_findings"),
        edge("plan_remediation", "finalize_scan"),
        edge("finalize_scan", END),
        # TODO: conditional edge from "discover_assets"
        #   routed by should_analyze() — wrap with cond(...)
        # TODO: conditional edge from "prioritize_findings"
        #   routed by should_remediate() — wrap with cond(...)
    ]

    entry = "discover_assets"
