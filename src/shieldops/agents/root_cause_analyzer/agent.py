# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the root_cause_analyzer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.root_cause_analyzer.models import RootCauseAnalyzerState
from shieldops.agents.root_cause_analyzer.nodes import (
    build_graph,
    collect_signals,
    rank_causes,
    recommend_fixes,
    report,
    trace_causality,
)
from shieldops.agents.root_cause_analyzer.tools import RootCauseAnalyzerToolkit
from shieldops.agents.runtime import END, Agent, edge


class RootCauseAnalyzerAgent(Agent):
    """Declarative spec for ``root_cause_analyzer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "root_cause_analyzer"
    state_model = RootCauseAnalyzerState
    toolkit_factory = RootCauseAnalyzerToolkit

    nodes = {
        "collect_signals": collect_signals,
        "build_graph": build_graph,
        "trace_causality": trace_causality,
        "rank_causes": rank_causes,
        "recommend_fixes": recommend_fixes,
        "report": report,
    }

    edges = [
        edge("recommend_fixes", "report"),
        edge("report", END),
        # TODO: cond(...) from "collect_signals" via _check_error()
        # TODO: cond(...) from "build_graph" via _check_error()
        # TODO: cond(...) from "trace_causality" via _check_error()
        # TODO: cond(...) from "rank_causes" via _check_error()
    ]

    entry = "collect_signals"
