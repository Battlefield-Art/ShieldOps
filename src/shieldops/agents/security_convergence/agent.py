# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_convergence agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_convergence.models import SecurityConvergenceState
from shieldops.agents.security_convergence.nodes import (
    collect_posture,
    coordinate_response,
    evaluate_defense,
    finalize_evaluation,
    unify_signals,
)
from shieldops.agents.security_convergence.tools import SecurityConvergenceToolkit


class SecurityConvergenceAgent(Agent):
    """Declarative spec for ``security_convergence`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_convergence"
    state_model = SecurityConvergenceState
    toolkit_factory = SecurityConvergenceToolkit

    nodes = {
        "collect_posture": collect_posture,
        "unify_signals": unify_signals,
        "evaluate_defense": evaluate_defense,
        "coordinate_response": coordinate_response,
        "finalize_evaluation": finalize_evaluation,
    }

    edges = [
        edge("unify_signals", "evaluate_defense"),
        edge("evaluate_defense", "coordinate_response"),
        edge("coordinate_response", "finalize_evaluation"),
        edge("finalize_evaluation", END),
        # TODO: conditional edge from "collect_posture"
        #   routed by should_continue() — wrap with cond(...)
    ]

    entry = "collect_posture"
