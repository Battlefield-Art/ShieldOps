# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the cloud_posture agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.cloud_posture.models import CloudPostureState
from shieldops.agents.cloud_posture.nodes import (
    assess_benchmarks,
    detect_misconfigs,
    generate_report,
    prioritize_risks,
    remediate,
    scan_cloud,
)
from shieldops.agents.cloud_posture.tools import CloudPostureToolkit
from shieldops.agents.runtime import END, Agent, edge


class CloudPostureAgent(Agent):
    """Declarative spec for ``cloud_posture`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "cloud_posture"
    state_model = CloudPostureState
    toolkit_factory = CloudPostureToolkit

    nodes = {
        "scan_cloud": scan_cloud,
        "assess_benchmarks": assess_benchmarks,
        "detect_misconfigs": detect_misconfigs,
        "prioritize_risks": prioritize_risks,
        "remediate": remediate,
        "generate_report": generate_report,
    }

    edges = [
        edge("scan_cloud", "assess_benchmarks"),
        edge("assess_benchmarks", "detect_misconfigs"),
        edge("detect_misconfigs", "prioritize_risks"),
        edge("remediate", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "prioritize_risks"
        #   routed by _should_remediate() — wrap with cond(...)
    ]

    entry = "scan_cloud"
