# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the security_workflow_optimizer agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.security_workflow_optimizer.models import SecurityWorkflowOptimizerState
from shieldops.agents.security_workflow_optimizer.nodes import (
    analyze_patterns,
    collect_workflows,
    generate_report,
    identify_bottlenecks,
    optimize_paths,
    validate_improvements,
)
from shieldops.agents.security_workflow_optimizer.tools import SecurityWorkflowOptimizerToolkit


class SecurityWorkflowOptimizerAgent(Agent):
    """Declarative spec for ``security_workflow_optimizer`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "security_workflow_optimizer"
    state_model = SecurityWorkflowOptimizerState
    toolkit_factory = SecurityWorkflowOptimizerToolkit

    nodes = {
        "collect_workflows": collect_workflows,
        "analyze_patterns": analyze_patterns,
        "identify_bottlenecks": identify_bottlenecks,
        "optimize_paths": optimize_paths,
        "validate_improvements": validate_improvements,
        "generate_report": generate_report,
    }

    edges = [
        edge("collect_workflows", "analyze_patterns"),
        edge("analyze_patterns", "identify_bottlenecks"),
        edge("validate_improvements", "generate_report"),
        edge("generate_report", END),
        # TODO: cond(...) from "identify_bottlenecks" via _should_optimize()
        # TODO: cond(...) from "optimize_paths" via _should_validate()
    ]

    entry = "collect_workflows"
