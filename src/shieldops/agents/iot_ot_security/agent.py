# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the iot_ot_security agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.iot_ot_security.models import IoTOTSecurityState
from shieldops.agents.iot_ot_security.nodes import (
    assess_vulnerabilities,
    detect_anomalies,
    discover_devices,
    enforce_segmentation,
    generate_report,
    profile_behavior,
)
from shieldops.agents.iot_ot_security.tools import IoTOTSecurityToolkit
from shieldops.agents.runtime import END, Agent, edge


class IotOtSecurityAgent(Agent):
    """Declarative spec for ``iot_ot_security`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "iot_ot_security"
    state_model = IoTOTSecurityState
    toolkit_factory = IoTOTSecurityToolkit

    nodes = {
        "discover_devices": discover_devices,
        "profile_behavior": profile_behavior,
        "detect_anomalies": detect_anomalies,
        "assess_vulnerabilities": assess_vulnerabilities,
        "enforce_segmentation": enforce_segmentation,
        "generate_report": generate_report,
    }

    edges = [
        edge("discover_devices", "profile_behavior"),
        edge("profile_behavior", "detect_anomalies"),
        edge("detect_anomalies", "assess_vulnerabilities"),
        edge("enforce_segmentation", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "assess_vulnerabilities"
        #   routed by _needs_segmentation() — wrap with cond(...)
    ]

    entry = "discover_devices"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``IoTOTSecurityRunner`` keep working via this alias; new code
# should use ``IotOtSecurityAgent`` directly through AgentRuntime.
IoTOTSecurityRunner = IotOtSecurityAgent
