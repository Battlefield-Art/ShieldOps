# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the threat_scenario_runner agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.threat_scenario_runner.models import ThreatScenarioRunnerState
from shieldops.agents.threat_scenario_runner.nodes import (
    evaluate_controls,
    execute_steps,
    generate_verdict,
    load_scenario,
    report,
    setup_environment,
)
from shieldops.agents.threat_scenario_runner.tools import ThreatScenarioRunnerToolkit


class ThreatScenarioRunnerAgent(Agent):
    """Declarative spec for ``threat_scenario_runner`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "threat_scenario_runner"
    state_model = ThreatScenarioRunnerState
    toolkit_factory = ThreatScenarioRunnerToolkit

    nodes = {
        "load_scenario": load_scenario,
        "setup_environment": setup_environment,
        "execute_steps": execute_steps,
        "evaluate_controls": evaluate_controls,
        "generate_verdict": generate_verdict,
        "report": report,
    }

    edges = [
        edge("load_scenario", "setup_environment"),
        edge("setup_environment", "execute_steps"),
        edge("execute_steps", "evaluate_controls"),
        edge("evaluate_controls", "generate_verdict"),
        edge("generate_verdict", "report"),
        edge("report", END),
    ]

    entry = "load_scenario"
