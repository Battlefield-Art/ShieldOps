# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the intelligent_automation agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.intelligent_automation.models import IntelligentAutomationState
from shieldops.agents.intelligent_automation.nodes import (
    assess_situation,
    execute_automation,
    finalize_execution,
    select_strategy,
    validate_outcome,
)
from shieldops.agents.intelligent_automation.tools import IntelligentAutomationToolkit
from shieldops.agents.runtime import END, Agent, edge


class IntelligentAutomationAgent(Agent):
    """Declarative spec for ``intelligent_automation`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "intelligent_automation"
    state_model = IntelligentAutomationState
    toolkit_factory = IntelligentAutomationToolkit

    nodes = {
        "assess_situation": assess_situation,
        "select_strategy": select_strategy,
        "execute_automation": execute_automation,
        "validate_outcome": validate_outcome,
        "finalize_execution": finalize_execution,
    }

    edges = [
        edge("select_strategy", "execute_automation"),
        edge("execute_automation", "validate_outcome"),
        edge("validate_outcome", "finalize_execution"),
        edge("finalize_execution", END),
        # TODO: cond(...) from "assess_situation" via should_continue()
    ]

    entry = "assess_situation"
