# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the agent_governance agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.agent_governance.models import AgentGovernanceState
from shieldops.agents.agent_governance.nodes import (
    assess_capabilities,
    audit_compliance,
    discover_agents,
    enforce_boundaries,
    evaluate_escalations,
    generate_report,
)
from shieldops.agents.agent_governance.tools import AgentGovernanceToolkit
from shieldops.agents.runtime import END, Agent, edge


class AgentGovernanceAgent(Agent):
    """Declarative spec for ``agent_governance`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "agent_governance"
    state_model = AgentGovernanceState
    toolkit_factory = AgentGovernanceToolkit

    nodes = {
        "discover_agents": discover_agents,
        "assess_capabilities": assess_capabilities,
        "enforce_boundaries": enforce_boundaries,
        "evaluate_escalations": evaluate_escalations,
        "audit_compliance": audit_compliance,
        "report": generate_report,
    }

    edges = [
        edge("discover_agents", "assess_capabilities"),
        edge("assess_capabilities", "enforce_boundaries"),
        edge("enforce_boundaries", "evaluate_escalations"),
        edge("evaluate_escalations", "audit_compliance"),
        edge("audit_compliance", "report"),
        edge("report", END),
    ]

    entry = "discover_agents"
