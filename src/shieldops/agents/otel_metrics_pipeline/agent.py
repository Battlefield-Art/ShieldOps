# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the otel_metrics_pipeline agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.otel_metrics_pipeline.models import OTelMetricsPipelineState
from shieldops.agents.otel_metrics_pipeline.nodes import (
    configure_pipeline,
    discover_endpoints,
    optimize_cardinality,
    validate_coverage,
)
from shieldops.agents.otel_metrics_pipeline.tools import OTelMetricsPipelineToolkit
from shieldops.agents.runtime import END, Agent, edge


class OtelMetricsPipelineAgent(Agent):
    """Declarative spec for ``otel_metrics_pipeline`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "otel_metrics_pipeline"
    state_model = OTelMetricsPipelineState
    toolkit_factory = OTelMetricsPipelineToolkit

    nodes = {
        "discover": discover_endpoints,
        "configure": configure_pipeline,
        "optimize": optimize_cardinality,
        "validate": validate_coverage,
    }

    edges = [
        edge("discover", "configure"),
        edge("configure", "optimize"),
        edge("optimize", "validate"),
        edge("validate", END),
    ]

    entry = "discover"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``OTelMetricsPipelineRunner`` keep working via this alias; new code
# should use ``OtelMetricsPipelineAgent`` directly through AgentRuntime.
OTelMetricsPipelineRunner = OtelMetricsPipelineAgent
