# RFC #247: agent.py replaces runner.py + graph.py (generated by scripts/codemods/agent_runtime.py)
"""Declarative ``Agent`` spec for the trust_relationship_mapper agent.

Generated by ``scripts/codemods/agent_runtime.py`` from the legacy
``runner.py`` + ``graph.py`` pair. See RFC #247.
"""

from __future__ import annotations

from shieldops.agents.runtime import END, Agent, edge
from shieldops.agents.trust_relationship_mapper.models import TrustRelationshipMapperState
from shieldops.agents.trust_relationship_mapper.nodes import (
    analyze_delegation_chains,
    assess_risk,
    detect_trust_abuse,
    discover_trust_boundaries,
    generate_report,
    map_federation,
)
from shieldops.agents.trust_relationship_mapper.tools import TrustRelationshipMapperToolkit


class TrustRelationshipMapperAgent(Agent):
    """Declarative spec for ``trust_relationship_mapper`` — mounted by AgentRuntime."""

    _shieldops_runtime_migrated = True

    name = "trust_relationship_mapper"
    state_model = TrustRelationshipMapperState
    toolkit_factory = TrustRelationshipMapperToolkit

    nodes = {
        "discover_trust_boundaries": discover_trust_boundaries,
        "map_federation": map_federation,
        "analyze_delegation_chains": analyze_delegation_chains,
        "detect_trust_abuse": detect_trust_abuse,
        "assess_risk": assess_risk,
        "generate_report": generate_report,
    }

    edges = [
        edge("map_federation", "analyze_delegation_chains"),
        edge("analyze_delegation_chains", "detect_trust_abuse"),
        edge("assess_risk", "generate_report"),
        edge("generate_report", END),
        # TODO: conditional edge from "discover_trust_boundaries"
        #   routed by _has_boundaries() — wrap with cond(...)
        # TODO: conditional edge from "detect_trust_abuse"
        #   routed by _has_abuses() — wrap with cond(...)
    ]

    entry = "discover_trust_boundaries"


# RFC #247 PR-6 legacy alias — runner.py was deleted. Existing callers that
# imported ``TrustRelationshipMapperRunner`` keep working via this alias; new code
# should use ``TrustRelationshipMapperAgent`` directly through AgentRuntime.
TrustRelationshipMapperRunner = TrustRelationshipMapperAgent
