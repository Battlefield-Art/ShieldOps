<?xml version="1.0"?>
<!--
    ShieldOps ClickHouse cluster configuration.

    Defines a single 3-shard / 1-replica cluster named `shieldops_cluster`
    consumed by the production Terraform stack (infrastructure/terraform/aws/
    production/clickhouse.tf). Node discovery uses internal Route53 hostnames
    (clickhouse-{1,2,3}.shieldops.internal) and a 3-node ZooKeeper ensemble
    (zk-{1,2,3}.shieldops.internal) deployed via Kubernetes.

    Per-node macros (shard, replica, cluster) are written by the EC2 user-data
    script into /etc/clickhouse-server/config.d/macros.xml at boot.
-->
<clickhouse>
    <logger>
        <level>information</level>
        <log>/var/log/clickhouse-server/clickhouse-server.log</log>
        <errorlog>/var/log/clickhouse-server/clickhouse-server.err.log</errorlog>
        <size>1000M</size>
        <count>10</count>
    </logger>

    <http_port>8123</http_port>
    <tcp_port>9000</tcp_port>
    <interserver_http_port>9009</interserver_http_port>
    <listen_host>0.0.0.0</listen_host>

    <max_connections>4096</max_connections>
    <keep_alive_timeout>10</keep_alive_timeout>
    <max_concurrent_queries>256</max_concurrent_queries>

    <path>/var/lib/clickhouse/</path>
    <tmp_path>/var/lib/clickhouse/tmp/</tmp_path>
    <user_files_path>/var/lib/clickhouse/user_files/</user_files_path>
    <format_schema_path>/var/lib/clickhouse/format_schemas/</format_schema_path>

    <users_config>users.xml</users_config>
    <default_profile>default</default_profile>
    <default_database>shieldops</default_database>

    <timezone>UTC</timezone>
    <mlock_executable>false</mlock_executable>

    <!--
        Cluster topology — 3 shards, 1 replica per shard. Each shard lives on
        a distinct AZ via its EC2 placement (see clickhouse.tf for AZ mapping).
    -->
    <remote_servers>
        <shieldops_cluster>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>clickhouse-1.shieldops.internal</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>clickhouse-2.shieldops.internal</host>
                    <port>9000</port>
                </replica>
            </shard>
            <shard>
                <internal_replication>true</internal_replication>
                <replica>
                    <host>clickhouse-3.shieldops.internal</host>
                    <port>9000</port>
                </replica>
            </shard>
        </shieldops_cluster>
    </remote_servers>

    <!-- ZooKeeper ensemble for ReplicatedMergeTree coordination. -->
    <zookeeper>
        <node>
            <host>zk-1.shieldops.internal</host>
            <port>2181</port>
        </node>
        <node>
            <host>zk-2.shieldops.internal</host>
            <port>2181</port>
        </node>
        <node>
            <host>zk-3.shieldops.internal</host>
            <port>2181</port>
        </node>
        <session_timeout_ms>30000</session_timeout_ms>
    </zookeeper>

    <!-- Distributed DDL coordination. -->
    <distributed_ddl>
        <path>/clickhouse/task_queue/ddl</path>
        <profile>default</profile>
    </distributed_ddl>

    <!-- Macros are overridden per-node by /etc/clickhouse-server/config.d/macros.xml -->
    <macros>
        <cluster>shieldops_cluster</cluster>
    </macros>

    <merge_tree>
        <parts_to_delay_insert>300</parts_to_delay_insert>
        <parts_to_throw_insert>600</parts_to_throw_insert>
        <max_suspicious_broken_parts>10</max_suspicious_broken_parts>
    </merge_tree>

    <query_log>
        <database>system</database>
        <table>query_log</table>
        <flush_interval_milliseconds>7500</flush_interval_milliseconds>
    </query_log>
</clickhouse>
